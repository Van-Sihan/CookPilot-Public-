# 변경 기록

작업을 요청받아 코드를 고칠 때마다 여기에 한 항목씩 쌓습니다.
최신 항목이 위로 옵니다.

각 항목은 이렇게 적습니다.

- **요청** — 무엇을 해 달라고 했는지
- **한 일** — 실제로 바뀐 것
- **건드린 파일** — 새로 만듦 / 고침 / 지움
- **확인** — 어떻게 검증했는지
- **남긴 것** — 일부러 안 한 부분, 다음에 이어서 할 일

---

## 2026-08-23 — 닉네임 바꾸기 · 블로그 글쓰기 · 요리 완성에서 바로 올리기

**요청**
남은 셋을 끝까지.

**한 일**

_수파베이스에 그림 자리를 만들었습니다_

- `posts.image_url` 칸과 `post-covers` 스토리지 버킷
- 그림을 글 안에 base64 로 담지 않은 까닭 — 한 장에 300KB 다. 목록에서
  열여섯 편을 부르면 그만큼이 다 딸려 오고 표가 금세 부푼다.
  **파일은 파일 두는 곳에, 표에는 주소만**
- 버킷은 공개로 둡니다(커뮤니티 글에 붙는 그림이라 로그인 없이 보여야 함).
  대신 **올리는 쪽을 좁혔습니다** — `(storage.foldername(name))[1] = auth.uid()`,
  즉 자기 uuid 폴더에만 올릴 수 있습니다. 안 묶어 두면 로그인한 아무나
  남의 표지를 덮어씁니다
- PNG 만, 2MB 까지

_닉네임 바꾸기 (`/account`)_

- 도메인은 가입 때와 **같은 규칙**(`checkDisplayName`)을 씁니다. 두 벌로 두면
  가입은 되는데 못 바꾸는 이름이 생깁니다
- 고칠 줄을 `eq("id", 내 id)` 로 못 박습니다. RLS 가 막아 주지만 여기서도
  좁혀 둡니다 — 정책이 한 번 느슨해지면 이 코드가 남의 이름을 바꾸는 코드가 됩니다
- 이메일은 보여 주기만 하고 못 고칩니다. 바꾸려면 확인 메일을 다시 받아야 하는데
  그건 이 화면이 할 일이 아닙니다

_블로그 글쓰기 (`/write`)_

- 로그인 안 했으면 **들어올 때** 막습니다(`/login?next=/write`). 다 적고 나서
  저장할 때 막으면 쓴 글이 날아갑니다
- 글쓴이를 우리가 안 적습니다. 표가 `author_id default auth.uid()` 이고
  그 칸에 넣을 권한이 없어서, 안 넣으면 자동으로 박힙니다.
  우리가 적으면 남의 이름으로 글을 쓸 수 있게 됩니다
- 그림 주소는 **우리가 올린 것만** 받습니다(`/post-covers/` 를 지나는
  https 주소). 남의 주소를 적어 넣으면 그 서버가 우리 화면에 아무 그림이나 띄웁니다

_요리 완성 → 글쓰기 (미리 채움)_

- `?from=cook` 으로 들어오면 방금 만든 요리로 채웁니다 —
  제목·딱지·걸리는 시간·본문(재료와 순서)
- **요약은 일부러 비웁니다.** 거기까지 지어내면 사람이 쓴 글이 아니라
  기계가 쓴 글이 됩니다. 목록에서 눈에 띄는 자리라 더 그렇습니다
- 딱지는 요리 이름을 보고 짚어 둡니다. 기본값 하나로 두면 모든 글에 같은
  딱지가 붙어 목록에서 갈래가 안 보입니다
- **사진은 레시피 카드입니다.** 요리 사진은 우리가 못 갖지만 카드는 그려 줄 수
  있습니다. 그리는 함수를 [[recipe-card]] 로 떼어 요리 완성 화면과 **같은 것**을
  씁니다 — 두 벌로 두면 내려받은 그림과 글에 붙은 그림이 달라집니다

_글이 갈 곳도 만들었습니다_

- 글을 써도 `/posts/[id]` 가 예시 열여섯 편만 봐서 **404 가 났습니다.**
  수파베이스 글도 읽게 이었습니다
- 사람이 쓴 글은 구조가 잡힌 레시피가 없어 옆 칸(재료·순서)을 안 세웁니다.
  한 컴포넌트에 둘을 다 넣으면 "있을 수도 없을 수도 있는 값" 이 늘어납니다
- 커뮤니티 **최신 탭 맨 앞**에 놓입니다. 스무 번째에 있으면 올라간 줄 모르고
  다시 씁니다

**건드린 파일**

새로 만듦
- `supabase/migrations/20260823140000_post_images_and_covers.sql`
- `lib/domain/post-draft.ts` · `lib/domain/recipe-to-post.ts`
- `lib/usecase/write-post.ts` · `lib/usecase/rename-me.ts`
- `lib/adapter/supabase-post-gateway.ts` · `supabase-profile-gateway.ts` ·
  `supabase-post-reader.ts` · `browser-cover-upload.ts`
- `lib/recipe-card.ts` — 표지 그리기를 떼어 냄
- `app/actions/post.ts` · `post-state.ts`
- `app/write/page.tsx` · `app/account/page.tsx`
- `components/write/write-form.tsx` · `account-form.tsx`
- `components/post/live-post-view.tsx`
- `lib/write-content.ts`

고침
- `app/posts/[id]/page.tsx` · `app/community/page.tsx` ·
  `components/community/post-cards.tsx` · `home-me.tsx` ·
  `components/cook/done-shell.tsx` · `lib/site-content.ts` ·
  `lib/cook-content.ts` · `app/globals.css`

**확인**

- 마이그레이션을 **실제로 올렸습니다**(`db push`). `posts.image_url` 확인,
  버킷은 없는 파일 요청이 "Object not found"(= 버킷은 있음) 로 답하는 것으로 확인
- 시험용 계정을 하나 만들어 **브라우저를 몰아 끝까지** 돌렸습니다
  - 로그인 → `/account` 에서 지금 이름이 칸에 채워짐 → 바꾸니 "닉네임을 바꿨습니다 · 바꾼닉네임"
  - `/write?from=cook` → 제목·딱지("밑반찬")·시간(5)·본문 자동 채움
  - 표지 만들기 → 스토리지에 올라가고 공개 주소가 폼에 붙음
  - 저장 → `/posts/<uuid>` 로 넘어가고 제목·글쓴이·본문·카드 그림이 다 나옴
  - 커뮤니티 최신 탭에 그 글이 나타남
- `tsc` · `eslint` · `next build` 통과
- 시험으로 만든 글 3편과 표지 3장은 **지웠습니다**(글 9편 = 시드 그대로)

**막혔던 것**

- 첫 시도에서 글은 저장됐는데 화면이 404 였습니다. 두 가지가 겹쳤습니다.
  ① `/posts/[id]` 가 표를 안 봄 ② `set_post_published` 를 **틀린 칸 이름**
  (`post`/`next`, 진짜는 `post_id`/`publish`)으로 불러 안 펴낸 글로 남음.
  ②는 실패를 조용히 삼키고 있어서 더 안 보였습니다 — 이제 실패하면 돌려보냅니다

**남긴 것**

- **시험용 계정이 남아 있습니다** — `cookpilot.test+e2e@example.com`.
  대시보드 [Authentication → Users] 에서 지우시면 됩니다. 서비스 키가 없어
  제가 못 지웁니다
- 글 고치기·지우기 화면은 없습니다. 표와 정책은 이미 있어서 화면만 붙이면 됩니다
- 좋아요는 아직 숫자만 보여 줍니다
- 예시 글 열여섯 편은 여전히 파일에 있습니다. 표로 옮기는 일은 따로 해야 합니다
---

## 2026-08-23 — 기능 점검 목록 2·3차: 정렬·서재·유튜브/냉장고·표지 인포그래픽

**요청**
15가지 목록 중 2차(정렬·표시)와 3·4차 일부.

**한 일**

_어긋난 것들_

- **요리 고르기 화면의 두 밑줄이 70px 어긋나 있었습니다.** 왼쪽 기둥 로고 밑줄은
  y=198, 가운데 1234 밑줄은 y=128. 화면이 두 조각으로 잘려 보였습니다.
  숫자를 `--head-line` 한 곳에만 적고 양쪽이 나눠 쓰게 했습니다
- **요리 화면의 "물어보기" 와 "타이머" 가 32px 어긋나 있었습니다.** 두 기둥의
  위 여백을 따로 주고 있었습니다. 둘 다 0으로 두고 바깥 틀이 한 번에 줍니다
- 두 곳 다 **픽셀을 재서** 찾았습니다. 눈으로는 "지저분하다" 까지만 보입니다

_표시_

- 계량 환산의 "무게(g)는 재료마다…" 안내문 삭제
- "이 걸음 읽어 주기" → **스피커 아이콘**. 불 앞에서는 읽는 것보다 알아보는 것이
  빠릅니다. 눈에 안 보이는 이름표를 달아 읽어 주는 기계에도 뜻이 전해집니다
- 메인 첫 글 카드를 **세로형**으로. 사진을 21:9로 넓게 깔고 글을 아래에 놓습니다.
  가로로 나란히 두면 사진이 세로로 눌려 요리가 안 보였습니다
- 브랜드 레시피 단추: "준비 중" → **"광고 모집중"**. 준비 중은 우리가 못 만든
  것이고, 모집중은 살 사람을 기다리는 자리라 뜻이 다릅니다

_서재 (`/shelf` 새로 만듦)_

- **가판대에 책을 세워 진열합니다.** 목록으로 늘어놓지 않은 까닭 — 서재는
  "몇 개 저장했나" 를 세는 자리가 아니라 "무엇을 만들었나" 를 돌아보는 자리입니다
- 책등에 요리 이름을 세로로 적고, 인분과 날짜를 아래에 붙입니다.
  손을 얹으면 책을 뽑아 든 것처럼 위로 뜹니다
- 책등을 누르면 그 레시피로 **다시 장보기부터** 시작합니다.
  레시피가 안 담긴 옛 책은 흐리게 두고 잠급니다
- 홈 · 요리 완성 · 왼쪽 기둥 세 곳의 "서재 (준비 중)" 을 진짜 링크로 바꿨습니다

_유튜브 · 냉장고 (진짜로 돕니다)_

- 카드 단추를 누르면 **카드 아래에서 적는 칸이 펼쳐집니다.** 다른 화면으로
  보내지 않는 까닭 — 하는 일이 한 줄 적는 것뿐인데 화면을 옮기면
  왜 왔는지 잊고, 돌아올 길도 만들어야 합니다
- 유튜브는 주소를 제미나이에 **그대로 실어 보냅니다.** 모델이 영상을 직접 보고
  옮겨 적습니다. **원작자 채널을 함께 받아 출처에 남깁니다** — 남의 영상에서
  옮겨 놓고 우리 것인 양 두면 안 됩니다
- 주소가 유튜브인지 **먼저 봅니다.** 아무 주소나 넘기면 모델이 못 여는 영상에서도
  그럴듯한 레시피를 지어냅니다. 원작자를 밝히겠다고 만든 기능이 거짓말을 합니다
- 냉장고는 쉼표·줄바꿈·가운뎃점·슬래시를 모두 칸막이로 봅니다. 한 가지만 받으면
  "두부 계란 김치" 가 재료 하나로 들어갑니다. 두 가지 미만이면 돌려보냅니다
- 인분을 **껍데기로 끌어올렸습니다.** 마이크 칸이 쥐고 있어서, 카드로 만들면
  화면에 보이는 숫자와 실제 인분이 어긋났을 것입니다
- 브랜드 카드는 예시 셋을 펼쳐 보여 줍니다. 진짜 광고가 아니라 **잠가 두고**
  예시라고 밝힙니다

_표지를 인포그래픽으로_

- 요리 이름만 크게 얹던 그림을 **레시피 카드**로 바꿨습니다 —
  로고·COOKPILOT·요리명·인분·걸리는 시간·재료(두 칸)·1번부터 끝까지의 순서
- **AI 표지는 유료 안내로 바꿨습니다.** 잠가 두고 "준비 중" 이라고만 적으면
  왜 안 되는지 알 수 없습니다. 무엇 때문에 막혔고 대신 무엇이 만들어지는지
  둘 다 적었습니다
- 처음 그린 것은 세 군데가 어긋났습니다 — 높이가 모자라 순서가 잘리고,
  재료 이름과 분량이 겹치고, 꼬리말이 본문 위에 그려졌습니다.
  **재 보고 나서 높이를 정하도록** 고쳤고, 분량은 칸 오른쪽에 붙이고
  이름은 남는 자리만큼만 그린 뒤 넘치면 잘라 냅니다

_브랜드 레시피 내용_

- "태양초 고추장으로 3분 만에 만드는 비빔장" 은 **사진이 비빔밥인데 글은
  양념장**이었습니다. 사진에 맞춰 **나물 비빔밥**으로 고쳤습니다 —
  본문·레시피·후기·CSV·시드를 모두 맞추고 파인콘에 다시 올렸습니다

**건드린 파일**

새로 만듦
- `app/shelf/page.tsx` · `components/shelf/shelf-stand.tsx` · `lib/shelf-content.ts`

고침
- `lib/domain/recipe.ts` — `readYoutubeUrl` · `readFridgeItems`
- `lib/usecase/plan-recipe.ts` — `planFromYoutube` · `planFromFridge`
- `lib/adapter/gemini-recipe-gateway.ts` — 두 길 추가, 출처 갈아 끼우는 구멍
- `components/pick/pick-cards.tsx` (다시 씀) · `pick-shell.tsx` · `voice-console.tsx`
- `components/cook/done-shell.tsx` — 표지 인포그래픽, 유료 안내
- `components/cook/cook-shell.tsx` · `components/icons.tsx` — 스피커 아이콘
- `components/pick/measure-tool.tsx` · `components/pick/pick-rail.tsx`
- `components/community/home-me.tsx` — 서재 바로가기
- `lib/site-content.ts` · `lib/cook-content.ts` · `lib/post-content.ts`
- `samples/reviews.csv` · `supabase/seed.reviews.sql`
- `app/globals.css`

**확인**

- `tsc` · `eslint` · `next build` 통과
- 두 정렬은 고친 뒤 **픽셀로 다시 쟀습니다**
- 서재·브랜드 칸·표지를 눈으로 확인. 표지는 실제로 그려 낸 PNG 를 열어 봤습니다
- 비빔밥으로 고친 뒤 파인콘에 다시 올려(101건) "비빔밥 밥이 질면" 으로 검색 →
  세 건 모두 그 글의 후기

**남긴 것 (다음 차례)**

- **닉네임 변경 기능**
- **블로그 글쓰기** 버튼과 화면
- **요리 완성 → 커뮤니티 글쓰기** (요리 정보·사진 자동 채움 후 저장)

  셋 다 수파베이스에 글을 쓰는 일이라 한 덩어리로 묶어 다음에 합니다
- 유튜브·냉장고를 **진짜 키로 돌려 본 적이 없습니다.** 배선만 맞춰 두었습니다
- 서재 책등은 이름이 길면 두 칸으로 접힙니다. 세로쓰기라 오른쪽부터 읽는 것이
  맞지만 어색해 보이면 말씀해 주세요
---

## 2026-08-23 — 말로 하는 "다음"·타이머가 안 듣던 것, 커뮤니티 검색

**요청**
기능 점검에서 나온 것들 중 고장 난 셋 — 음성 타이머 미작동, 음성 "다음" 에
화면 미갱신, 커뮤니티 검색 미작동.

**한 일**

_"다음" 과 타이머 — 규칙이 사람 말투를 못 받아 주고 있었습니다_

- 짐작하지 않고 **재현부터 했습니다.** WebSocket 을 가짜로 바꿔치기해서 원하는
  인식 결과를 그대로 흘려 넣는 하네스를 만들었습니다. 진짜 제미나이에 안 붙으니
  네트워크나 모델 기분과 상관없이 우리 코드만 볼 수 있습니다
- 배선은 멀쩡했습니다. 문제는 **알아듣는 규칙**이었습니다. 실제 말투 20가지로
  때려 보니 **11가지가 실패**했습니다

| 실패하던 말 | 까닭 |
|---|---|
| "네 다음", "응 다음", "자 다음", "오케이 다음" | 앞의 군말 |
| "다음 해줘", "다음 부탁해", "다음 좀" | 뒤의 군말 |
| "다음 단계 알려줘", "다음으로", "다음으로 가줘" | 조사·서술어 |
| "넘어가자" | 규칙에 없던 말 |

  사람은 "다음" 이라고만 말하지 않습니다. 거의 늘 앞에 "네/자" 를 붙이거나
  뒤에 "해줘" 를 붙입니다. 그런데 규칙은 `^다음$` 에 가까웠습니다
- **군말을 떼고 알맹이만 보는 방식**으로 바꿨습니다. 앞뒤에서 더 뗄 것이
  없을 때까지 되풀이해 벗깁니다
- 다만 **물음은 여전히 명령이 아닙니다.** "다음에 뭐 넣어요?" 에 화면이 넘어가면
  안 됩니다. 물음말(뭐·어떻게·몇·나요…)이 있으면 짧아도 안 받습니다
- 타이머는 규칙 자체는 괜찮았지만 **말로 한 숫자**를 못 읽었습니다.
  "삼분 타이머" 가 안 걸립니다 — 인식기가 숫자를 한글로 옮겨 적을 때가 있습니다.
  한/두/세…서른까지 표를 넣었습니다. "한 시간 반" 도 받습니다
- 알람·걸어줘·설정 같은 말도 타이머 요청으로 받아 줍니다

_검색_

- 아예 안 만들어져 있었습니다. 폼에 `action` 이 없어 아무 데도 안 갔습니다
- 주소로 합니다(`?q=계란찜`). 자바스크립트가 한 줄도 안 들고, **결과 주소를
  복사해 남에게 보낼 수 있습니다**
- **띄어쓰기와 대소문자를 무시**합니다. "비빔 국수" 와 "비빔국수" 는 사람에게
  같은 말인데 그대로 견주면 하나는 안 걸립니다. 검색이 한 번 헛돌면
  사람은 그 뒤로 검색칸을 안 씁니다
- 제목·셰프·딱지·요약을 함께 뒤집니다. 제목만 뒤지면 셰프 이름으로 못 찾습니다
- **찾을 때는 탭을 안 봅니다.** "인기" 탭에서 찾았다고 최신 글을 빼면 없는 줄 압니다
- 몇 개를 찾았는지와 "검색 지우기" 를 목록 맨 위에 답니다

**건드린 파일**

- `lib/domain/cook-progress.ts` — 군말 떼기, 물음 거르기, 말로 한 숫자
- `lib/domain/community-tab.ts` — `resolveQuery` · `matchesQuery`
- `lib/site-content.ts` — `postsByQuery`
- `app/community/page.tsx` · `components/community/community-hero.tsx` ·
  `components/community/post-cards.tsx` · `app/globals.css`

**확인**

- 규칙: 실제 말투 **44가지 시험표**로 확인. 다음 24/24, 물음 거르기 5/5, 타이머 15/15
- 하네스로 화면까지: "네 다음" → 둘째 걸음, "다음 단계 알려줘" → 셋째 걸음,
  "이전으로" → 둘째 걸음, **조각조각 나뉜** "3분/타이머/해줘" → 타이머 생성,
  "삼분 타이머 맞춰줘" → 타이머 생성, "다음에 뭐 넣어요?" → **안 넘어감**
- 검색: 계란찜 1건 · 비빔 2건 · 불꽃요리사 1건 · 브랜드 4건 · zzz 0건
- `tsc` · `eslint` · `next build` 통과

**남긴 것**

- 진짜 제미나이 음성으로는 아직 못 해 봤습니다. 하네스는 인식 결과를 우리가
  정해서 넣는 것이라, 실제 인식기가 뭐라고 옮겨 적는지는 다른 문제입니다
- 검색이 본문·재료까지는 안 뒤집니다. 카드에 있는 값만 봅니다
---

## 2026-08-23 — 챗봇 이름·아이콘, 자료 올리기 단추 제거, 유리 결을 걷어낸 새 면

**요청**
"커뮤니티에 물어보기" 대신 챗봇이 드러나는 이름과 아이콘으로. 자료 올리기 단추는
없앨 것. 그리고 박스에 그라데이션을 쓰니 2000년대 초 디자인 같으니,
'Atmospheric Precision & Ember' 규칙대로 디자인만 고칠 것.

**한 일**

_이름과 아이콘_

- "커뮤니티에 물어보기" → **"AI 챗봇에게 물어보기"**
- 말풍선 아이콘을 걷고 **로봇 얼굴(`bot`)** 을 새로 그렸습니다. 안테나 한 획이
  말풍선과 기계를 가릅니다. 홈의 문과 챗봇 화면 제목 양쪽에 붙습니다

_자료 올리기 단추 제거_

- 화면에서 통째로 걷어냈습니다. 상태·손잡이·CSS 까지 남기지 않았습니다.
  개발자가 한 번 하는 일이지 물어보러 온 사람이 볼 단추가 아닙니다
- **API 는 남겼습니다.** CSV 를 고치면 `POST /api/kitchen/index` 로 다시 올립니다

_디자인: 유리를 걷고 면으로_

앞 판은 상자마다 흰 그라데이션을 덮고 큰 그림자를 깔고 뒤를 흐렸습니다.
그렇게 하면 면이 나뉘긴 하는데 유리를 흉내 낸 티가 납니다. 반대로 갔습니다.

| | 앞 판 | 이번 판 |
|---|---|---|
| 상자 바탕 | 흰 그라데이션 + 반투명 | **단색 한 겹** (`--surf-1`) |
| 깊이 | 큰 그림자 두 겹 | **면 밝기 차이만** (bg → surf-1 → surf-2) |
| 뒤 흐리기 | `backdrop-filter: blur(14px)` | **없음** |
| 경계선 | 흰색 알파 | **색**(`#2A2523`) — 알파는 뒤에 뭐가 있냐에 따라 밝기가 흔들립니다 |
| 나누는 선 | 실선 | **양끝이 사라지는 선**(`border-image`) |
| 단추 | 세로 그라데이션 + 잉걸불 그림자 | **단색 + 윗변 빛 1px** |

- 윗변의 머리카락 같은 빛은 남겼습니다. 종이 두께를 만드는 것이 그 한 줄입니다.
  다만 양끝이 사라지게 바꿨습니다 — 실선이면 모서리에서 뚝 끊깁니다
- 배경의 잉걸불 후광(radial-gradient)은 그대로입니다. 규칙에도 남아 있는 항목이고,
  그건 면이 아니라 공기입니다
- 세리프 제목 자간을 `-.03em` 으로 당겼습니다. 한글은 절반(`-.015em`)만 —
  라틴만큼 당기면 붙어 버립니다
- 화면 위아래 여백을 `clamp(72px, 10vw, 128px)` 까지 벌렸습니다

_덤으로 잡은 것_

- `header { }` 를 그냥 짚어 둔 규칙이 **화면마다 있는 모든 `<header>`** 에
  걸리고 있었습니다. 챗봇 화면 제목 아래에 쓸데없는 줄이 하나 그어져 있었고,
  그것도 그라데이션 바탕이었습니다. `header:has(.nav)` 로 좁혔습니다
- 남긴 세로 그라데이션은 다섯인데 전부 박스가 아닙니다 — 카드 사진 아래 어둠막,
  소리 그래프 막대 셋, 레시피 표지 그림

**건드린 파일**

- `app/globals.css` — 유리 블록(212줄)을 면 블록으로 통째로 교체, 흩어져 있던
  그라데이션·그림자·블러 정리, `header` 범위 좁힘, 자료 올리기 CSS 삭제
- `components/icons.tsx` — `bot` 아이콘
- `components/ask/ask-shell.tsx` — 자료 올리기 조각 삭제
- `components/community/ask-link.tsx` · `app/ask/page.tsx` — 이름·아이콘
- `lib/ask-content.ts` — 이름, 자료 올리기 문구 삭제

**확인**

- `tsc` · `eslint` · `next build` 통과
- 홈·챗봇·글 화면 세 장을 눈으로 확인
- 남은 `backdrop-filter` · `--sheen` · `--lift` · `--ember-wash` 0개
- 쓸데없는 줄은 눈이 아니라 **픽셀을 찍어** 찾았습니다. y=264 에 `#2A2523`
  1px 이 있었고, DOM 을 훑어 `HEADER.ask-head` 가 범인임을 확인했습니다

**남긴 것**

- 소개 페이지(`/`)와 로그인·시작 화면은 눈으로 다시 안 봤습니다. 같은 토큰을
  쓰므로 함께 바뀌었을 텐데, 어긋난 데가 있으면 알려 주세요
- 챗봇에 실제 질문을 아직 못 던져 봤습니다
---

## 2026-08-23 — 챗봇이 시작 화면의 제미나이 키를 쓰도록, 파인콘 연동 완료

**요청**
메인 홈페이지에서 넣은 API 키를 바탕으로 챗봇이 답하게 할 것. 파인콘 연동 먼저.

**한 일**

_파인콘 연동 (실제로 돌아갑니다)_

- 후기 101건을 인덱스에 올렸습니다. `cookpilot-chatbot` · cosine · 1024차원
- 검색 품질을 확인했습니다
  - "계란찜이 안 부푸는 이유" → 0.487 / 0.401 / 0.396, **다섯 건 모두 계란찜 후기**
  - "에어프라이어로 만든 거 어때" → 세 건 모두 두부강정
  - "튀기지 않고 바삭하게" → 0.374 "튀기지 않아도 바삭하다"
- **`llama-text-embed-v2` 는 한국어가 잘 됩니다.** 사용자 말이 맞았습니다

_고친 것 셋_

- **임베딩 건수 한도.** 101건을 한 번에 보내 거절당했습니다
  `Input length '101' exceeded inputs limit of 96 for model 'llama-text-embed-v2'`
  80건씩 나눠 보냅니다. 한도(96)에 딱 맞추지 않은 것은 모델을 바꾸면 한도도
  달라지기 때문입니다
- **자료 올리기가 제미나이 키까지 요구하던 것.** 올리는 데는 파인콘만 있으면
  되는데 셋을 한 덩어리로 묶어 뒀습니다. 갈랐습니다
- **`PINECONE_HOST` 를 받습니다.** 교재가 쓰는 이름입니다. 이름(`PINECONE_INDEX`)
  으로 넣어도 되고, 주소만 넣으면 거기서 이름을 뽑아냅니다

_키가 브라우저에서 옵니다_

- 앞 판은 `GOOGLE_API_KEY` 를 서버 환경 변수에서 읽었습니다. 이제 시작 화면에서
  넣어 둔 **자기 키**를 브라우저가 요청마다 실어 보냅니다
- **키가 우리 서버를 한 번 거칩니다.** 랭체인 체인이 서버에서 한 줄기로 돌아야
  하기 때문입니다. 받은 키는 그 요청 안에서만 쓰고 담지도 적지도 않습니다
- 숨기지 않고 화면 아래에 적었습니다. 시작 화면의 "키는 서버로 보내지 않습니다"
  안내도 사실에 맞게 고쳤습니다 — 요리 화면은 여전히 브라우저가 직접 구글에
  가고, 물어보기 화면만 서버를 거칩니다
- 환경 변수 `GOOGLE_API_KEY` 는 뒷받침으로 남겨 뒀습니다. 브라우저 키가 먼저입니다

**건드린 파일**

- `lib/adapter/langchain-rag.ts` — `langchainRag(googleKey)`, 배치 올리기,
  `PINECONE_HOST` 받기, `searchOnly()`(답 없이 검색만 — 검색이 나쁜 것과 답이
  나쁜 것을 갈라 보려고 둡니다)
- `app/api/chat/route.ts` — 몸통에서 키를 받아 도메인 `checkApiKey` 로 모양만 보고 넘김
- `app/api/kitchen/index/route.ts` — `?q=` 를 붙이면 검색만 해 봄
- `components/ask/ask-shell.tsx` — 키를 읽어 함께 보냄, 없으면 입력칸 잠금
- `lib/ask-content.ts` · `lib/site-content.ts` · `app/globals.css`

**확인**

- `tsc` · `eslint` · `next build` 통과
- 자료 올리기 → `{"ok":true,"count":101,"skipped":0}` (실제 파인콘)
- 검색 세 질문 모두 관련 후기가 상위에 옴 (위 참고)
- 키 없이 물어보기 → `{"reason":"key"}`, 0.012초 (일찍 막힘)
- 엉터리 키로 물어보기 → `{"reason":"key"}`, **1.75초** —
  파인콘 검색까지 다녀온 뒤 제미나이에서 거절당한 것입니다. 배선 확인

**남긴 것**

- **진짜 키로 답까지 받아 본 적이 아직 없습니다.** 사용자 브라우저에 있는 키라
  제가 꺼낼 수 없습니다. `/ask` 에서 한 번 물어봐 주셔야 합니다
- 수파베이스 마이그레이션을 아직 안 올렸습니다. 그래서 대화가 저장되지 않고
  새로 고치면 사라집니다 — 답 자체는 나옵니다(저장 실패는 조용히 넘어갑니다)
- 교재와 다르게 제가 정한 것이 둘 남아 있습니다: 표 이름·RLS 설계, 계층 분리

**막혔던 것**

- 검색이 0.076 으로 엉뚱하게 나와 랭체인 버그를 의심했는데, **제 테스트
  명령이 한국어를 cp949 로 넘기고 있었습니다.** 앱은 처음부터 멀쩡했습니다.
  파인콘에 직접 붙어 저장된 벡터를 꺼내 견줘 보고(자기 유사도 0.985,
  직접 쿼리 0.487) 앱만 이상하다는 것을 확인한 뒤, 도착한 글자를 되돌려
  받아서야 알았습니다. `ERRORS.md` 에 적었습니다
---

## 2026-08-23 — 챕터 10 을 교재대로: 랭체인 LCEL · 후기 CSV 100건 · 수파베이스 표 넷

**요청**
llama-text-embed-v2 는 한국어가 된다(직접 확인함). 샘플 리뷰 데이터는 수파베이스
마이그레이션용으로 무엇을 쓸지 정하고, 교재대로 랭체인을 쓰도록 고칠 것.

**한 일**

_먼저, 앞 항목의 잘못을 바로잡습니다_

- `llama-text-embed-v2` 가 한국어를 못 다룬다고 적었는데 **틀렸습니다.**
  파인콘 문서의 표에 "Multilingual: No" 라고 적혀 있어 그대로 옮겼는데,
  사용자가 직접 넣어 본 결과가 그 표보다 맞습니다. 교재가 고른 모델도 이것이라
  그대로 씁니다. 코드 주석도 고쳤습니다

_샘플 데이터 — 무엇을 쓸 것인가_

- 교재의 `samples/review.csv`(상품 리뷰 100건) 자리에 **요리 후기 101건**을
  넣었습니다. `samples/reviews.csv`
- 칸 골격은 교재를 따랐고(별점·제목·내용·글쓴이·날짜·도움됨·확인 여부),
  `dish_id` 와 `dish` 를 더했습니다. 답에 "어느 요리 후기인가" 를 붙이려면
  이 두 칸이 필요합니다
- 커뮤니티 글 열여섯 편에 6~7건씩 고르게 나눴습니다. 한쪽에 몰리면 검색이
  그쪽으로만 걸립니다
- 별점을 1~5 로 흩었습니다. 다 5점이면 "별로였다는 후기 있어?" 에 답할 것이 없습니다

_수파베이스 마이그레이션 — 표 넷_

`20260823090000_create_reviews_and_chats.sql`

| 표 | 무엇 | 읽기 | 쓰기 |
|---|---|---|---|
| `reviews` | 후기 원본 101건 | 누구나 | 막음 (시드로만) |
| `chats` | 대화 한 뭉치 | **막음** | 누구나 |
| `messages` | 대화 안의 말 | **막음** | 누구나 |
| `message_feedback` | 답이 도움됐는지 | 막음 | 누구나 |

- **로그인을 요구하지 않습니다**(교재 요구 사항). 그래서 RLS 짜는 방식이
  다른 표들과 다릅니다 — `auth.uid()` 가 없으니 "자기 것만 본다" 를
  데이터베이스가 확인할 방법이 없습니다
- 그래서 **읽는 정책을 아예 안 만들었습니다.** RLS 는 정책이 없으면 막는 쪽이
  기본이라 `select * from messages` 는 한 줄도 안 나옵니다. 대신 대화 id 를
  넘겨야만 그 대화를 돌려주는 `chat_messages(uuid)` 함수 하나만 열어 뒀습니다
- 이러면 목록을 훑어 남의 대화를 긁어 가는 일은 막힙니다. 다만 uuid 를
  알아낸 사람은 그 대화를 볼 수 있습니다 — **추측하기 어렵다는 것에 기대는
  방식**이고, 진짜 비밀을 담을 자리가 아닙니다. 마이그레이션에 그렇게 적어 뒀습니다
- 시드는 CSV 에서 만들어 냈습니다(`supabase/seed.reviews.sql`). 손으로 두 벌을
  맞추면 언젠가 어긋납니다

_랭체인으로 갈아엎기_

- 앞서 손으로 짰던 파인콘 REST 호출과 제미나이 호출을 **전부 걷어내고**
  교재 246쪽의 LCEL 체인으로 바꿨습니다
- `PineconeEmbeddings` → 임베딩을 파인콘이 자기 서버에서 해 줍니다(무료).
  OpenAI 임베딩을 쓰면 그만큼 값이 붙습니다
- `PineconeStore` + `asRetriever({ k: 5 })` → 벡터 저장·검색
- `ChatPromptTemplate` + `MessagesPlaceholder` → `{context}`·`{input}` 과 지난 대화
- `ChatGoogleGenerativeAI` + `StringOutputParser` → 답 만들고 글자 꺼내기
- `RunnableSequence` 두 칸 → 교재와 같은 모양. 두 번째 칸에서 `docs` 를
  같이 들고 나오는 것이 요점입니다. 답만 받으면 근거를 화면에 못 붙입니다
- CSV 읽기도 랭체인 `CSVLoader` 로 합니다. 다만 CSVLoader 는 칸 값을
  metadata 에 안 담고 `id: r001\ndish: …` 처럼 글자로 이어 붙여 놓습니다.
  그대로 쓰면 출처를 못 붙여서, 그 글자를 다시 칸으로 풀어 냅니다

_키가 서버로 옮겨 갔습니다_

- 앞 판은 제미나이 키를 브라우저에 두고 구글을 직접 불렀습니다. 이제는
  **파인콘 키도 제미나이 키도 서버**에 있습니다(`GOOGLE_API_KEY`)
- 교재와 같은 방식이고, 체인이 서버에서 한 줄기로 돌아야 랭체인을 쓰는 뜻이 있습니다
- **요리 화면(`/pick`·`/cook`)은 그대로 브라우저 키를 씁니다.** 두 화면이 서로
  다른 키를 쓰게 된 셈이라, 나중에 하나로 모을지 정해야 합니다

**건드린 파일**

새로 만듦

- `samples/reviews.csv` — 후기 101건
- `supabase/migrations/20260823090000_create_reviews_and_chats.sql`
- `supabase/seed.reviews.sql` — CSV 에서 만들어 낸 시드
- `lib/domain/review.ts` — 후기의 모양, CSV 한 줄 읽기, **검색에 걸릴 글자 만들기**
  (`reviewText` 가 이 챗봇의 검색 품질을 절반쯤 쥡니다)
- `lib/domain/ask.ts` — 물음 검사, 대화 한 마디, 지난 대화 줄이기, 출처 접기
- `lib/adapter/langchain-rag.ts` — LCEL 체인. `server-only`
- `lib/adapter/csv-reviews.ts` — CSVLoader. `server-only`
- `lib/adapter/supabase-chat-log.ts` — 대화 담기·꺼내기. `server-only`
- `lib/adapter/browser-chat-id-store.ts` — 대화 id 만 브라우저에
- `app/api/chat/route.ts` · `app/api/chat/[id]/route.ts`

고침

- `lib/usecase/ask-kitchen.ts` — `RagChain`·`ChatLog`·`ChatIdStore` 로 다시 씀
- `app/api/kitchen/index/route.ts` — CSV → 랭체인 Document → 파인콘
- `components/ask/ask-shell.tsx` — 서버 체인에 맞춰 다시 씀
- `lib/ask-content.ts` · `components/community/ask-link.tsx`
- `package.json` — `@langchain/core`·`@langchain/pinecone`·`@langchain/google-genai`·
  `@langchain/community`·`@pinecone-database/pinecone@^5`·`d3-dsv`

지움

- `lib/adapter/pinecone-knowledge.ts` · `gemini-answer-gateway.ts` ·
  `http-knowledge-search.ts` · `lib/domain/knowledge.ts` ·
  `app/api/kitchen/search/route.ts` — 랭체인이 대신하므로 통째로 걷어냈습니다

**확인**

- `tsc` · `eslint` · `next build` 통과
- `GET /api/kitchen/index` — CSV 101건 읽힘, **버린 줄 0**, 요리별 6~7건
- 키가 없을 때 어떻게 되는지 먼저 봤습니다
  - 올리기 → `{"reason":"setup","detail":"PINECONE_API_KEY, PINECONE_INDEX, GOOGLE_API_KEY"}`
  - 물어보기 → `{"reason":"key"}`
  - 빈 물음 → `{"reason":"empty"}` (도메인이 걸러 냅니다)
  - 없는 대화 id → `{"turns":[]}` — 404 를 안 주는 까닭은 "이 id 는 있다" 가
    새어 나가면 대화를 헤아려 볼 수 있게 되기 때문입니다
- 마이그레이션과 시드를 pglast(libpg_query)로 파싱해 문법 확인
- React Compiler 가 "효과 안에서 setState 하지 말라" 며 막은 곳이 하나 있었는데,
  억누르지 않고 `useSyncExternalStore` 로 바꿨습니다. 다른 저장소들과 같은 방식입니다

**남긴 것**

- **파인콘 인덱스와 키가 아직 없습니다.** 그래서 실제 왕복을 한 번도 못 해 봤습니다.
  인덱스는 **일반 dense 인덱스, 1024차원**이어야 합니다 — 랭체인
  `PineconeEmbeddings` 가 임베딩을 따로 하기 때문에, 통합 임베딩 인덱스가 아닙니다
- `.env.local` 에 `PINECONE_API_KEY` · `PINECONE_INDEX` · `GOOGLE_API_KEY` 필요
- 마이그레이션을 아직 안 올렸습니다(`supabase db push`). 8월 22일 것도 그대로입니다
- `message_feedback` 표만 만들고 화면에 👍/👎 단추는 안 붙였습니다
- 사이드바(이전 대화 목록·새 대화)는 안 만들었습니다. 대화는 한 번에 하나뿐입니다
- 자료 올리기 API 에 자물쇠가 없습니다
---

## 2026-08-23 — 챕터 10: 커뮤니티 글에 물어보는 RAG 챗봇

**요청**
챕터 10(쇼핑 리뷰 분석 챗봇)을 우리 프로젝트에 맞춰 만들기.
주제는 요리 후기·레시피 챗봇, 스택은 Pinecone + 제미나이로 정함.

**한 일**

_왜 이 주제인가_

- 교재의 "쇼핑 리뷰" 자리에 **우리 커뮤니티 글**을 넣었습니다. 어제 만든 글
  16편(본문·레시피·후기)이 그대로 자료가 됩니다. 지어낼 것이 없습니다
- 잘라 보니 **112조각**입니다 — 요약 16, 본문 32, 재료 16, 순서 16, 후기 32.
  가장 긴 조각 298자, 가장 짧은 48자

_RAG 두 걸음_

- 물으면 두 곳에 다녀옵니다. ① 우리 서버 → 물음과 가까운 조각(파인콘)
  ② 구글 → 그 조각만 쥐여 주고 받은 답(제미나이)
- **한 번에 안 하는 까닭은 키입니다.** 파인콘 키는 우리 계정 것이라 서버에
  있어야 하고, 제미나이 키는 각자 것이라 브라우저에 있어야 합니다.
  시작 화면에 "키는 이 브라우저 안에만 남는다" 고 적어 두었고 그 약속은
  여기서도 지켜져야 합니다. 느려 보이지만 키를 안 옮기는 값입니다
- 조각이 하나도 안 걸리면 **모델을 부르지 않습니다.** 근거 없이 답하게 두면
  그 순간 RAG 가 아니라 그냥 챗봇이 됩니다
- 지난 대화는 뒤쪽 6마디만 딸려 보냅니다. 전부 보내면 값이 오르고 언젠가는
  넣을 수 있는 글자 수를 넘깁니다 (교재 p.246 의 그 이야기입니다)

_자르는 규칙_

- 본문은 **소제목 단위**로 묶습니다. 같은 소제목 아래 문단은 한 이야기를
  나눠 적은 것이라 떼어 놓으면 둘 다 반쪽이 됩니다 — "물기를 닦으라" 와
  "안 닦으면 삶아진다" 는 붙어 있어야 답이 됩니다
- 후기는 반대로 **한 줄씩** 자릅니다. 다른 사람이 다른 이야기를 한 것이라
  묶으면 한 조각 안에서 말이 섞입니다
- 조각마다 앞에 `[요리 이름]` 을 붙입니다. 문단만 떼어 놓으면 무슨 요리
  이야기인지 사라집니다 — "불을 다루는 법" 은 스테이크에도 볶음밥에도 있습니다

_교재와 다르게 한 것_

- ~~**임베딩 모델은 `multilingual-e5-large` 여야 합니다.** 파인콘 기본값인
  `llama-text-embed-v2` 는 여러 나라 말을 다루지 못합니다.~~
  **→ 틀렸습니다.** `llama-text-embed-v2` 도 한국어가 걸립니다(사용자가 직접 확인).
  파인콘 문서의 표를 그대로 믿고 적은 것이 잘못이었습니다. 8월 23일 항목 참고
- **LangChain.js 를 안 썼습니다.** 체인이 네트워크를 사이에 두고 서버와
  브라우저로 갈려 있어서, 랭체인으로 묶으려면 오히려 코드가 늡니다.
  실제로 하는 일은 파인콘 검색 한 번과 제미나이 호출 한 번입니다
- 답변 생성은 OpenAI gpt-5-nano 대신 제미나이입니다. 크레딧 충전이 없습니다

**건드린 파일**

새로 만듦

- `lib/domain/knowledge.ts` — 조각의 모양, 자르는 규칙, 물음 검사
- `lib/usecase/ask-kitchen.ts` — RAG 두 걸음. `KnowledgeSearch` · `AnswerGateway`
  인터페이스, 대화 기록 줄이기, 출처 추리기
- `lib/adapter/pinecone-knowledge.ts` — 파인콘 REST(올리기·찾기). **`server-only`**
  로 못 박아 두어서 브라우저 쪽에서 가져다 쓰면 빌드가 거기서 멈춥니다
- `lib/adapter/gemini-answer-gateway.ts` — 조각을 쥐여 주고 답 받기(브라우저)
- `lib/adapter/http-knowledge-search.ts` — 브라우저에서 우리 API 를 부르는 쪽
- `app/api/kitchen/search/route.ts` — 조각 찾기. 파인콘 키를 감추는 문
- `app/api/kitchen/index/route.ts` — 자료 올리기(POST) · 미리 보기(GET)
- `lib/ask-content.ts` · `app/ask/page.tsx` · `components/ask/ask-shell.tsx`
- `components/community/ask-link.tsx` — 홈 왼쪽 기둥에서 들어가는 문

고침

- `app/community/page.tsx` — 위 문을 마이크 단추 아래에 놓음
- `app/globals.css` — `.ask-*` · `.ask-door*`
- `package.json` — `server-only` 추가

**확인**

- `tsc` · `eslint` · `next build` 통과
- `GET /api/kitchen/index` 로 자르기 결과를 값으로 확인 — 112조각,
  종류별 분포와 길이 모두 뜻대로
- **키가 없을 때 어떻게 되는지**를 먼저 봤습니다.
  올리기는 `{"reason":"setup","detail":"PINECONE_API_KEY, PINECONE_HOST"}`,
  찾기는 `{"reason":"key"}`. 500 을 던지지 않고 무엇이 빠졌는지 말해 줍니다
- `/ask` 화면 확인. 제미나이 키가 없으면 맨 위에 "키 넣으러 가기" 가 뜨고
  입력칸이 잠깁니다

**남긴 것**

- **파인콘 계정과 인덱스는 아직 없습니다.** 사용자가 만들고
  `PINECONE_API_KEY` · `PINECONE_HOST` 를 `.env.local` 에 넣어야 돕니다.
  인덱스는 반드시 `multilingual-e5-large`, `field_map.text` 는 `chunk_text`
- **실제 질문을 던져 본 적이 없습니다.** 키가 없어서 파인콘·제미나이 왕복을
  한 번도 못 해 봤습니다. 키를 넣으신 뒤에 같이 확인해야 합니다
- 대화 기록을 수파베이스에 안 담습니다. 새로 고치면 사라집니다.
  교재는 `chat_messages` 표를 만듭니다 — 다음에 할 일입니다
- 자료 올리기 API 에 자물쇠가 없습니다. 지금은 우리가 쓴 예시 글을 덮어쓰기만
  해서 괜찮지만, 남의 글이 들어오면 반드시 막아야 합니다
- 사이드바(이전 대화 목록·새 대화)는 안 만들었습니다
---

## 2026-08-23 — 홈 제목·설명을 검색칸 왼쪽 선에 맞춤

**요청**
CookPilot 글자를 Search bar 기준으로 왼쪽에 반듯하게 정렬.

**한 일**

- 가운데 있던 제목과 한 줄 설명을 왼쪽으로 옮겼습니다. 이제 제목·설명·검색칸·
  왼쪽 기둥·카드가 모두 같은 세로선(171px)에서 시작합니다
- 위만 가운데였던 것이 문제였습니다. 아래 목록은 왼쪽 정렬인데 위가 가운데라
  화면의 축이 둘로 갈려 보였습니다
- 설명 줄에만 남긴 `max-width` 는 자리를 잡으려는 것이 아니라 글줄 길이 때문입니다

**건드린 파일**

- `app/globals.css` — `.cm-hero` · `.cm-title` · `.cm-lead`

**확인**

- 브라우저에서 픽셀로 쟀습니다. 제목 171 · 설명 171 · 검색칸 171 · 왼쪽 기둥 171 ·
  머리글 171 · 셰프 순위 171 · 탭 글자 171. 오른쪽 끝도 검색칸과 카드가 1255 로 같음
- `tsc` · `eslint` · `next build` 통과

**남긴 것**

- 없습니다. 자리만 옮겼습니다

---

## 2026-08-22 — 카드를 누르면 열리는 글 화면, 진짜 요리 사진, 레이아웃 정렬

**요청**
카드를 누르고 들어가면 실제 음식 이미지·레시피·내용·댓글로 진짜 음식 커뮤니티
블로그처럼 보이게. 그다음 나머지 박스 레이아웃을 똑바로 정렬하고, 특히 SEARCH
바를 좌우로 늘리거나 해서 전체가 깔끔하게 맞도록 점검.

**한 일**

_글 하나 보기 (`/posts/[id]`)_

- 카드를 누르면 그 글이 열립니다. 큰 사진, 제목, 쓴 사람과 좋아요, 소제목이 붙은
  본문, 꼬리표, 오른쪽에 레시피 칸, 아래에 후기까지 한 화면입니다
- 레시피 칸에는 재료(집에 있는 것 표시), 번호 붙은 순서, 몇 인분·얼마나 걸리는지가
  들어갑니다. 맨 아래 **"이 레시피로 요리 시작"** 을 누르면 그 레시피를 그대로
  들고 장보기 → 요리로 넘어갑니다. 커뮤니티 글의 레시피와 AI 가 만들어 주는
  레시피가 **같은 모양(도메인의 `Recipe`)** 이라 이게 됩니다
- 하룻밤 재우는 레시피는 다 더하면 925분입니다. 그 자리에서 몇 시간인지 셈해야
  하는 값이라 "약 15시간 25분" 으로 나누어 적습니다
- 없는 주소로 들어오면 404 입니다. 탭과 달리 **다른 글로 바꿔 보여 주지 않습니다** —
  사람은 자기가 누른 글을 읽고 있다고 잘못 알게 됩니다

_후기(댓글)_

- 적고 누르면 목록에 붙고, 새로 고쳐도 남고, 다른 글에는 안 보입니다
- **이 브라우저에만 담깁니다.** 남에게는 아직 안 보입니다. 그 사실을 입력칸 밑에
  적어 두었습니다 — 올라간 줄 알았는데 아니었던 쪽이 더 나쁩니다
- 빈 글과 한 글자는 막습니다. 500자를 넘으면 말없이 자르지 않고 돌려보냅니다.
  위를 막는 까닭은 예의가 아니라 저장소입니다 — 한 사람이 십만 자를 붙여넣으면
  브라우저 저장 공간이 차서 **다른 값까지** 못 담게 됩니다

_진짜 요리 사진 16장_

- CSS 로 그리던 접시를 **진짜 사진**으로 갈아 끼웠습니다. 목록 카드와 글 화면
  양쪽에 붙습니다
- 사진은 위키미디어 공용에서 받았습니다. **허락(라이선스)이 분명한 것만** 골랐고,
  찍은 사람과 허락문 링크를 글마다 사진 밑에 내보냅니다. CC BY / CC BY-SA 는
  찍은 사람을 밝히는 것이 조건입니다
- 출처 값은 손으로 옮겨 적지 않고 커먼즈 API 에 **파일 이름으로** 물어 받았습니다.
  처음에는 검색 차례(첫 번째, 두 번째…)로 집어 왔는데, **부를 때마다 순서가
  흔들려** 사진과 찍은 사람이 어긋났습니다. 틀린 출처는 안 적느니만 못합니다
- 16장 4.1MB. 가로 1400px 을 넘기지 않고 품질 82로 줄였습니다

_레이아웃 정렬_

- 검색칸을 본문 폭에 맞춰 좌우로 늘렸습니다. 제목·설명만 가운데로 좁힙니다.
  셋을 한꺼번에 640px 로 묶어 두어서 검색칸 좌우 끝이 아래 목록과 어긋났습니다
- 왼쪽 기둥의 탭 글자가 머리글·셰프 순위보다 16px 안으로 밀려 있었습니다.
  목록을 왼쪽으로만 14px 끌어내 세로선을 맞췄습니다. 오른쪽은 그대로 둡니다 —
  양쪽을 다 끌어내면 눌린 탭의 바탕이 기둥 밖으로 삐져나갑니다
- "자주 찾는 태그" 에만 꼬리표 그림이 붙어 글자가 혼자 밀려 있어서 뺐습니다.
  이제 머리글 셋이 같은 자리에서 시작합니다

**건드린 파일**

새로 만듦

- `lib/domain/post.ts` — 글 한 편의 속(사진 출처·본문·레시피·댓글), 댓글 검사,
  사진 경로 규칙, id 로 찾기
- `lib/usecase/discuss-post.ts` — 댓글 남기기. `CommentStore` 인터페이스
- `lib/adapter/browser-comment-store.ts` — 그 저장소를 localStorage 로 채운 것
- `lib/post-content.ts` — 열여섯 편의 본문·레시피·댓글·사진 출처
- `lib/post-copy.ts` — 글 화면에 적히는 말. 본문과 따로 둔 까닭은 무게입니다.
  후기 칸은 브라우저에서 도는데 같은 파일이면 본문 전부가 딸려 갑니다
- `app/posts/[id]/page.tsx`
- `components/post/post-article.tsx` · `post-recipe.tsx` · `post-talk.tsx` ·
  `cook-this.tsx`
- `public/food/*.jpg` (16장) · `public/food/CREDITS.md`

고침

- `lib/domain/recipe.ts` — 출처에 `community` 갈래 추가, `totalMinutes` ·
  `splitMinutes`
- `components/shop/shop-shell.tsx` · `lib/cook-content.ts` — 커뮤니티에서 온
  레시피의 출처 줄
- `components/community/post-cards.tsx` — 접시 그림 → 사진, 링크를 `/posts/<id>` 로
- `components/community/community-side.tsx` — 태그 머리글 그림 뺌
- `lib/site-content.ts` — `findPost(id)`
- `components/icons.tsx` — `chat` 아이콘
- `app/layout.tsx` — `metadataBase`. 링크를 붙였을 때 요리 사진이 같이 뜨려면
  절대 주소가 필요합니다. 배포 주소는 `NEXT_PUBLIC_SITE_URL` 로 넣으면 됩니다
- `app/globals.css` — `.cm-shot*`(사진), `.pd-*`(글 화면) 추가, 검색칸·탭 정렬

**확인**

- `tsc` · `eslint` · `next build` 모두 통과
- 후기 칸은 브라우저를 띄워 값으로 확인했습니다 — 처음 2줄 → 빈 글 막힘("내용을
  적어 주세요") → 한 글자 막힘 → 제대로 쓰면 3줄로 늘고 앞뒤 공백이 다듬어짐 →
  입력칸 비워짐 → localStorage 에 담김 → 새로 고쳐도 남음 → **다른 글에는 안 보임**
- 없는 주소(`/posts/nope`) 404 확인
- 390px 폭으로 화면 크기를 흉내 내어 `scrollWidth === 390`, 가로로 삐져나가는
  요소 없음
- 사진 16장을 한 장에 모아 붙여 눈으로 확인했습니다. 요리와 안 맞는 것 두 장
  (스테이크·두부)은 다른 사진으로 바꿨습니다

**남긴 것**

- **후기는 아직 이 브라우저에만 담깁니다.** 남에게 보이게 하려면
  `browser-comment-store.ts` 만 수파베이스 것으로 갈아 끼우면 됩니다.
  `comments` 표와 RLS 정책은 아직 없습니다
- 좋아요 숫자는 아직 보여 주기만 합니다. 누를 수 없습니다
- 꼬리표는 누를 곳이 없어 표시로만 뒀습니다
- 글은 여전히 `site-content.ts` · `post-content.ts` 의 예시입니다.
  수파베이스 `posts` 표에서 읽어 오는 일은 아직입니다
- 사진이 없는 글을 새로 올리면 깨진 그림이 뜹니다. `photoPath()` 가 규칙 하나로
  경로를 만들 뿐이라 없는 파일을 걸러 내지 않습니다 — 글을 DB 에서 가져오게 될 때
  같이 손봐야 합니다
---

## 2026-08-22 — 홈 제목을 CookPilot 으로, 글쓴이를 닉네임으로, 탭마다 다른 샘플 글

**요청**
메인 페이지 제목을 Community 대신 CookPilot 으로. 샘플 글의 박서진·신민재 같은
이름을 닉네임으로. 둘러보기 네 탭(인기/최신/브랜드 레시피/SNS 유행)을 눌렀을 때
각 테마에 맞는 샘플 글이 나오게.

**한 일**

- 홈의 큰 제목을 `site.name` 으로 바꿨습니다. "CookPilot" 이라고 또 적지 않은 까닭 —
  이름이 바뀌는 날 두 군데를 고치면 한쪽이 옛 이름으로 남습니다
- 글쓴이와 셰프 순위를 전부 닉네임으로 바꿨습니다. 불꽃요리사·디저트요정·십오분한끼·
  냉털장인·오븐없이굽기·자취9년차·야식대장·마라중독자·기름없이바삭.
  **사람 이름을 지어 쓰지 않는 편이 낫습니다** — 실제로 있는 이름과 겹칠 수 있고,
  커뮤니티에서 부를 이름은 원래 닉네임입니다
- 샘플 글을 16편으로 다시 짜서 **탭마다 겹치지 않게** 나눴습니다
  - 인기 — 스테이크·까르보나라·카카오 스피어·계란찜
  - 최신 — 비빔국수·카레·포카치아·두부강정 (좋아요가 적은 새 글들)
  - 브랜드 레시피 — 브랜드가 자기 제품으로 만든 레시피 넷
  - SNS 유행 — 마라탕후루·두바이 초콜릿·로제 떡볶이·크로플
- 마라탕후루만 인기와 유행 양쪽에 걸립니다. 좋아요가 가장 많으니(4.1k)
  인기에서 빠지면 오히려 이상합니다

**건드린 파일**

- `lib/site-content.ts` — `communityCopy.title`, `communityPosts`, `rankedChefs`

**확인**

- 네 탭을 각각 열어 걸리는 글을 확인했습니다. 겹치는 것은 마라탕후루 하나뿐입니다
- 옛 이름 아홉 개(김도현·박서진·이수민·정하늘·한지우·신민재·최윤아·오세영·장태석)가
  화면에 하나도 안 남은 것을 확인
- `npx tsc --noEmit`, `npx eslint .`, `npx next build` 모두 통과

**남긴 것**

- **브랜드 레시피의 브랜드 이름은 지어낸 것입니다** — 한들식품·고운밥상·청수식품·달소금.
  진짜 회사 이름(오뚜기·청정원 같은)을 붙이면, 그 회사가 쓰지도 않은 글을 쓴 것처럼
  됩니다. 장보기 화면의 광고 자리는 상품을 **가리키는** 것이라 진짜 이름을 썼지만,
  글은 **누가 썼다고 말하는** 것이라 다릅니다. 제휴가 붙으면 그때 바꾸면 됩니다
- 글은 여전히 `site-content.ts` 의 예시입니다. 수파베이스 `posts` 표에서 읽어 오는
  일은 아직입니다 — 그때 이 배열만 없어지고 화면은 그대로 갑니다

---

## 2026-08-22 — 걸음·타이머 오류 두 개, 홈 화면, 목소리 미리 듣기, 글씨와 면 다시 잡기

**요청**
(오류) "다음" 이라고 해도 "지금 할 일" 화면이 안 바뀐다 / 타이머가 우측에 안 생긴다.
(기능) 맨 위 쿡파일럿을 누르면 메인 홈(=커뮤니티)으로. 로그인 → 키 입력 → 홈.
홈 왼쪽에 내 프로필과 마이크 단추. 목소리를 남성/여성으로 나누고 고르면 샘플을 들려줄 것.
글씨가 전부 너무 작다. 상자가 전부 검정이라 안 나뉜다 — 호텔 같은 검정에 부드러운
그라데이션·디테일·반투명을 함께 쓸 것.

**오류 · 왜 그랬나**

추측하지 않고 **가짜 웹소켓을 끼워 재현**했습니다. 제미나이 자리에 우리가 조종하는
가짜를 넣고 "다음" 을 들려주니 곧바로 드러났습니다 — 두 번째 발화의 기록이
`"나 다음3분 타이머해줘"` 였습니다.

**모아 둔 말이 안 비워지고 있었습니다.** 말은 `turnComplete` 때 비우게 해 뒀는데,
그건 *모델의* 차례가 끝났다는 신호입니다. 바로 앞 작업에서 모델에게
"짧은 명령에는 대꾸하지 마라" 고 일러 두었으니 그 신호가 영영 안 옵니다.
그래서 두 번째 "다음" 은 `"다음다음"` 이 되어 명령으로 안 걸렸고,
타이머도 앞말이 들러붙은 채로 판정됐습니다. **오류 둘이 사실 하나였습니다.**

**오류 · 한 일**

- 말끝을 언제로 볼지를 **게이트웨이**로 옮겼습니다. 말을 모아 두는 자리가 거기라
  거기서 자르는 것이 맞습니다. 0.7초 동안 더 안 들어오면 한 마디로 칩니다
- 비우는 일을 올려 보내기 **전에** 합니다. 받는 쪽이 곧바로 또 말을 걸 수 있는데
  그때까지 옛 말이 남아 있으면 다음 마디에 들러붙습니다
- 마이크 자리에 있던 미루기 장치는 걷어 냈습니다. 두 군데서 같은 판정을 하면
  언젠가 어긋납니다

**기능 · 홈**

- `/community` 가 메인 홈이 됐습니다. 네 화면(`/pick`·`/shop`·`/cook`·`/cook/done`)의
  "쿡파일럿" 이 홈으로 가는 링크입니다 — 요리 도중에도 빠져나갈 길이 있어야 합니다
- 키를 넣고 나면 "홈으로 가기" 입니다. 곧바로 목소리 고르기로 몰아넣으면
  둘러볼 틈 없이 관문만 이어집니다. 급한 사람을 위한 지름길은 그 밑에 작게 뒀습니다
- 홈 왼쪽 맨 위에 내 자리(`HomeMe`)를 넣었습니다. 누구인지, 목소리·서재·키가 어떤지,
  그리고 큰 마이크 단추. **누르면 상태에 따라 갈 곳이 갈립니다** —
  키가 없으면 키 화면, 목소리를 안 골랐으면 고르는 화면, 다 됐으면 곧바로 부엌.
  매번 목소리를 다시 고르게 하면 두 번째부터는 성가신 관문이 됩니다
- 여기서만 `findCookSetup` 을 안 씁니다. 그 함수는 기본값을 채워 줘서 안 고른 사람도
  고른 것처럼 보입니다. 저장소를 직접 봐야 "아직 안 고름" 을 알 수 있습니다

**기능 · 목소리**

- 성별(여성/남성)을 더했습니다. 여섯 갈래가 제미나이의 미리 만들어 둔 목소리에
  이어집니다(Kore·Leda·Aoede / Charon·Puck·Orus). 성별은 제미나이가 문서로 못 박은
  값이 아니라 우리가 붙인 이름이라, 화면에서도 더 단정하지 않습니다
- 말투 이름을 차분한 / 밝은 / **부드러운** 으로 바꿨습니다(`low` → `soft`).
  예전 판에서 담아 둔 `low` 는 조용히 이어 줍니다 — 안 그러면 예전에 고른 사람이
  기본값으로 되돌아갑니다
- **미리 듣기를 진짜 그 목소리로 들려줍니다.** 브라우저 읽어 주기로 대신하면
  골라 놓고 정작 다른 목소리가 나옵니다. 라이브에 짧게 이어 한 문장만 읽히고 끊습니다.
  다른 카드를 누르면 앞엣것을 먼저 끊습니다 — 안 끊으면 둘이 겹칩니다

**기능 · 글씨와 면**

- CSS 의 글자 크기 **196군데를 한꺼번에 키웠습니다.** 작은 것일수록 많이 올렸습니다
  (9~11px → 12~12.5, 12~13.5 → 13.5~15, 14~17 → 16~18.5, 큰 제목은 1.1배).
  본문은 16 → 17px
- 상자를 **빛으로** 나눴습니다. 색을 밝게 바꾸는 대신 네 겹을 겹칩니다 —
  옅은 흰 그라데이션, 반투명 + 뒤 흐리기, 윗변의 머리카락 같은 선, 넓고 옅은 그림자.
  좋은 호텔의 어두운 로비가 그렇습니다. 벽은 다 검은데 결과 반사로 면이 갈립니다
- 값을 `--sheen`·`--edge`·`--lift` 로 한곳에 모아 뒀습니다. 상자마다 따로 적으면
  한 군데만 고쳤을 때 면들이 서로 다르게 보입니다
- 눌린 카드에는 잉걸불이 비치고, 접이칸을 펼치면 한 단계 뜨고, 광고 줄은 금빛 유리입니다
- 세 화면 뒤에 은은한 후광을 깔았습니다. **유리 면이 비칠 것이 있어야 유리로 보입니다**
- 뒤 흐리기를 못 쓰는 브라우저에서는 반투명을 걷고 불투명하게 둡니다.
  안 그러면 글자 뒤로 배경이 비쳐서 읽을 수가 없습니다

**확인**

- 가짜 웹소켓 시험 여섯 가지 **전부 통과**했습니다.
  "다음" → 01→02 / **"다음" 한 번 더 → 02→03**(이번 오류) / "3분 타이머해줘" → 타이머 생김 /
  2.5초 뒤 3:00→2:57 / 손으로 8분 → 두 개 나란히 / "다음에 뭐 넣어요?" → 안 넘어감
- `npx tsc --noEmit`, `npx eslint .`, `npx next build` 모두 통과
- 홈·목소리 고르기·장보기·요리를 눈으로 확인

**남긴 것**

- **미리 듣기는 진짜 키가 있어야 됩니다.** 키가 없으면 안내만 띄웁니다.
  그리고 들을 때마다 라이브를 짧게 부르므로 요금이 조금 듭니다
- 성별 대응(어느 목소리가 남성인지)은 들어 보고 정한 것입니다. 실제로 들어 보시고
  어색하면 `voiceFor` 표만 고치면 됩니다
- 프로필 사진을 올리는 길이 없어서 로고를 세워 뒀습니다
- 홈에 보이는 글은 여전히 `site-content.ts` 의 예시입니다. 표에서 읽어 오는 일은
  아직입니다

---

## 2026-08-22 — "다음" 이 안 먹히고 AI 가 딴말하던 것 고침

**요청**
"다음" 이라고 말했는데 화면이 안 바뀌고, AI 가 알려 주는 말이 화면과 다르다.
(실제로 쓰다가 나온 문제. 화면 갈무리를 받아 원인을 찾았습니다)

**왜 그랬나 · 셋**

1. **사람이 한 말을 `turnComplete` 때만 처리하고 있었습니다.**
   그건 *모델의* 차례가 끝났다는 신호입니다. 그래서 모델이 먼저 떠들고 나서야
   걸음이 옮겨졌고, **모델이 대꾸를 안 하면 신호가 안 와서 영영 안 옮겨졌습니다.**
   화면 갈무리에도 "다음" 뒤에 모델이 `붉은색 양념장이 골고루 섞이도록 저어주세요`
   라고 먼저 대꾸한 것이 그대로 남아 있었습니다
2. **모델이 짧은 명령에 스스로 대꾸했습니다.** "대꾸하지 말라" 고 적어 두긴 했는데
   부드럽게 적어서 안 지켜졌습니다
3. **`cut()` 이 아무것도 안 멈추고 있었습니다.** 줄의 시작 시각만 0으로 되돌릴 뿐,
   이미 `start()` 로 예약해 둔 소리는 시각이 되면 그대로 나갔습니다.
   그래서 끊었다고 생각한 말이 새 안내와 겹쳐 흘렀습니다

**한 일**

- 게이트웨이가 `hearing` 이라는 사건을 새로 올려 보냅니다. 알아들은 글자가 올 때마다
  **모델의 차례와 상관없이** 지금까지 들은 말을 통째로 올립니다
- 마이크 자리는 그 말이 **0.7초 동안 더 안 오면** 그때 명령인지 봅니다.
  올 때마다 바로 보면 "다음에 뭐 넣어요?" 가 "다음" 까지 왔을 때 걸립니다.
  기다렸다 보면 문장이 다 모인 뒤에 판정하므로 그 함정을 피합니다
- 명령을 알아들으면 **틀던 소리를 먼저 끊습니다.** 모델이 그 명령에 대꾸하는 중일 수
  있는데, 안 끊으면 곧 이어질 걸음 읽기와 겹칩니다
- `cut()` 이 예약해 둔 소리를 하나씩 진짜로 멈추게 고쳤습니다
- 걸음을 읽어 달라는 말을 **훨씬 세게** 바꿨습니다. 문장을 따옴표로 묶고
  "토씨 하나 바꾸지 말고", "앞에도 뒤에도 다른 말을 붙이지 마라",
  "'타이머를 걸까요?' 같은 말을 덧붙이지 마라" 까지 적었습니다 —
  마지막 것은 실제로 모델이 한 말이라 그대로 집어넣었습니다
- 사람이 한 말을 화면의 "주고받은 말" 에도 **곧바로** 올립니다. 모델이 대꾸를 안 하면
  `turnComplete` 가 안 와서, 전에는 사람 말이 화면에 안 나타났습니다.
  나중에 `said` 로 같은 말이 또 오면 건너뜁니다

**건드린 파일**

- `lib/adapter/gemini-live-gateway.ts` — `hearing` 사건
- `lib/adapter/browser-audio.ts` — `cut()` 이 예약된 소리를 진짜로 멈추게
- `lib/usecase/cook-along.ts` — `hearing` 타입, 읽기 지시를 세게
- `components/cook/live-console.tsx` — 0.7초 기다렸다 판정, 끊고 나서 옮기기

**확인**

- `npx tsc --noEmit`, `npx eslint .`, `npx next build` 모두 통과
- 가짜 마이크를 물린 크롬으로 `/cook` 을 다시 열어 화면이 멀쩡한 것을 확인

**남긴 것**

- **모델이 시킨 대로 읽는지는 진짜 키로 다시 들어 봐야 압니다.** 지시를 세게 했지만
  이런 것은 부탁이지 규칙이 아닙니다. 그래도 군말을 붙이면, 걸음 읽기만
  브라우저 읽어 주기(`speechSynthesis`)로 돌리는 길이 있습니다 —
  그러면 화면 글과 **한 글자도 안 다릅니다.** 대신 목소리가 말동무와 달라집니다
- 0.7초는 눈대중입니다. 너무 성급하면 말 끝을 자르고, 너무 느긋하면 답답합니다.
  써 보시고 알려 주시면 맞추겠습니다

---

## 2026-08-22 — 요리 화면: 들어오면 바로 읽어 주고, 말로 걸음을 옮김

**요청**
요리 화면에 들어오면 제미나이 라이브가 "지금 할 일" 을 먼저 읽어 주고, 마이크는
자동으로 켜진 상태로. "다음" 이라고 말하면 다음 걸음으로. "3분 타이머해줘" 로 타이머.

**한 일**

- **들어오면 알아서 마이크가 켜지고 지금 걸음을 읽어 줍니다.** 손이 젖은 채로
  단추를 찾는 것이 이 앱이 없애려는 바로 그 수고입니다
- 이어지자마자 `[읽기]` 알림을 보내 지금 걸음을 읽히고, **걸음이 바뀔 때마다**
  같은 알림을 보냅니다. 걸음 글을 알림에 함께 실어 보냅니다 — 처음에 레시피를
  통째로 알려 주긴 했지만 대화가 길어지면 모델이 몇 번째였는지 헷갈립니다
- `stepChangedNote`(읽지 말라던 것)를 `readStepNote`(읽어 달라는 것)로 바꿨습니다
- **"다음"·"이전"·"다시" 를 알아듣습니다.** 규칙은 도메인의 `readStepCommand` 에
  있습니다. **짧게 딱 그 말만 했을 때만** 명령으로 봅니다 —
  "다음에 뭐 넣어요?" 는 물음이지 명령인데, 낱말만 보고 걸러 내면 사람이 물어볼
  때마다 화면이 제멋대로 넘어갑니다. "넘어가 줘" 처럼 시키는 꼴이면 길어도 받습니다
- 모델에게 **"'다음' 같은 짧은 명령에는 스스로 답하지 말라"** 고 일러 두었습니다.
  안 그러면 모델의 "네, 다음 단계는…" 과 우리가 시킨 걸음 읽기가 겹쳐 두 번 말합니다
- 마이크가 내내 켜져 있어 부엌 소리와 혼잣말이 그대로 들어갑니다. 그래서
  **"자기에게 한 말이 아닌 것 같으면 아무 말도 하지 않는다"** 도 규칙에 넣었습니다
- **"3분 타이머해줘" 는 이미 되고 있었습니다.** 그래도 믿고 넘기지 않고 시험해
  확인했습니다(아래 확인 참고)
- "이 걸음 읽어 주기" 단추는 **말동무가 이어져 있으면 그쪽에 시킵니다.** 사람이 고른
  목소리로 읽히고, 브라우저 목소리와 번갈아 나와 둘이 말하는 것처럼 들리지 않습니다.
  안 이어져 있을 때만 브라우저 읽어 주기로 넘어갑니다
- "다음 단계 알려줘" 단추는 걸음만 옮깁니다. 옮기면 말동무가 알아서 읽어 주는데
  여기서 또 읽으라고 하면 같은 말이 두 번 나옵니다

**한 일 · 자동으로 켜면서 생긴 문제 둘**

- **브라우저가 소리를 막습니다.** 사람이 그 페이지에서 한 번도 아무것도 안 눌렀으면
  소리를 못 내게 하는데, 이 화면은 들어오자마자 시작하니 딱 그 상황입니다.
  막힌 줄 모르면 "말은 하는데 안 들리는" 상태가 되어 고장으로 보입니다.
  그래서 막혔는지 확인하고 **"소리 켜기" 단추**를 띄웁니다
- **효과가 두 번 돌면 웹소켓이 둘 열립니다.** React 는 개발 중에 효과를 두 번
  돌려 보는데, 그러면 같은 걸음을 두 번 읽습니다. 표시를 남겨 한 번만 부릅니다

**건드린 파일**

- `lib/domain/cook-progress.ts` — `readStepCommand` 추가
- `lib/usecase/cook-along.ts` — 읽어 주기 규칙, `readStepNote`
- `lib/adapter/browser-audio.ts` — 스피커 `blocked()`·`unblock()`
- `components/cook/live-console.tsx` — 자동 시작, 걸음 읽기, 명령 알아듣기
- `components/cook/cook-shell.tsx` — 명령 처리, 읽어 주기를 말동무에게
- `lib/cook-content.ts`, `app/globals.css` — 안내 문구와 "소리 켜기" 단추

**확인**

- **말 알아듣는 규칙을 시험으로 확인했습니다.** 도메인 파일만 따로 컴파일해
  20가지 문장을 넣어 봤고 전부 통과했습니다. 걸려야 하는 것("3분 타이머해줘",
  "다음", "이제 다음으로 넘어가 줘")과 **안 걸려야 하는 것**("중불에서 3분 볶으세요",
  "다음에 뭐 넣어요?")을 같이 넣었습니다 — 안 걸리는 쪽이 더 중요합니다
- 가짜 마이크를 물린 크롬으로 `/cook` 을 열어 **자동 시작이 실제로 도는 것**을
  봤습니다. 가짜 키라 라이브 연결에서 멈췄고, 그 실패가 안내로 제대로 떴습니다
- `npx tsc --noEmit`, `npx eslint .`, `npx next build` 모두 통과.
  린트가 잡은 것 둘(그리는 도중 ref 쓰기, 효과에서 곧바로 상태 바꾸기)은
  덮지 않고 구조를 바꿔 고쳤습니다

**남긴 것**

- **진짜 키로는 아직 못 돌려 봤습니다.** 자동 시작·마이크·오디오 배관까지는
  확인했지만, 제미나이가 `[읽기]` 알림을 시킨 대로 따르는지는 실제로 말을
  들어 봐야 압니다. 너무 딱딱하게 읽거나 앞에 군말을 붙이면
  `cookingBrief` 의 규칙을 다듬으면 됩니다
- 웹소켓은 왜 실패했는지 브라우저가 감춥니다. 키가 틀려도 인터넷이 끊겨도
  같은 안내가 뜹니다 — 그래서 짚어 볼 곳을 둘 다 적어 두었습니다
- 마이크를 내내 켜 두면 그만큼 요금이 나갑니다. 조용할 때 잠깐 끊는 장치는
  아직 없습니다

---

## 2026-08-22 — 장보기·요리·완성 세 화면과 제미나이 연결

**요청**
마이크로 요리를 말하면 다음 단계로 넘어가는 화면들을 순서대로. (1) 장보기 —
광고 라벨이 붙는 상품, 쿠팡·마켓컬리·네이버쇼핑 고르기, "요리 시작하기" 로 요리 화면.
(2) 요리 — 여기서부터 제미나이 라이브와 말하며 요리. (3) 완성 — 레시피 표지를 서재에.

**한 일 · 흐름을 이었습니다**

`/pick` 에서 말하면 → 레시피를 받아 `/shop` → `/cook` → `/cook/done` 으로 이어집니다.
세 화면이 같은 레시피를 봐야 해서 브라우저에 담아 두고(`browserRecipeDraftStore`)
꺼내 씁니다. 주소로 실어 나르기엔 레시피가 크고, 화면 상태로 들고 다니면
새로 고침 한 번에 사라집니다.

**한 일 · 제미나이**

- **레시피 받기(REST)** — 소리를 그대로 실어 보내고 레시피 JSON 을 받습니다.
  알아듣기와 레시피 만들기를 **한 번에** 시킵니다. 두 번 다녀오면 그만큼 늦고,
  중간에 알아들은 이름이 어긋나면 엉뚱한 레시피가 옵니다
- `responseSchema` 로 모양을 못 박았습니다. 안 그러면 "네, 제육볶음 레시피입니다!"
  같은 인사말이 붙어 JSON 으로 못 읽습니다. 그래도 모델은 한 번씩 어기므로
  도메인의 `readRecipe` 가 칸을 하나씩 다시 확인합니다
- 키를 주소(`?key=`)가 아니라 `x-goog-api-key` 머리말에 싣습니다. 주소에 실으면
  브라우저 기록과 중간 서버 로그에 키가 남습니다
- **라이브(웹소켓)** — 마이크를 16kHz PCM 으로 흘려보내고 24kHz PCM 을 받아 틉니다.
  받은 조각을 그냥 재생하면 겹치거나 사이가 벌어져 말이 끊겨서, 다음 조각이 시작될
  시각을 세어 가며 줄을 세웁니다. 사람이 말을 끊으면(`interrupted`) 줄을 비웁니다
- 라이브는 브라우저에서 머리말을 못 붙여 **키를 주소에 실을 수밖에 없습니다.**
  파일에 그 사실을 적어 두었습니다
- 마이크는 `AudioWorklet` 으로 받습니다. 소리 전용 쓰레드에서 돌아 화면이 버벅여도
  소리가 안 끊깁니다. `AudioContext({sampleRate:16000})` 로 브라우저가 알아서
  16kHz 로 바꾸게 했습니다 — 직접 칸을 솎으면 소리가 거칠어집니다

**한 일 · 화면**

- **장보기** — 집에 늘 있는 양념(`pantry`)은 체크가 빠진 채로 시작합니다.
  담긴 것이 아니라 **뺀 것**을 기억합니다 — 레시피가 바뀌어도 저절로 맞아떨어집니다.
  광고는 재료 바로 밑에 `AD` 딱지와 함께 붙습니다
- "N개 한 번에 열기" 를 **단추가 아니라 링크 목록**으로 두었습니다. 창을 여러 개
  띄우면 브라우저가 첫 번째만 열고 나머지를 팝업으로 보고 막습니다. 링크는 누른 것이
  반드시 열립니다. 여덟 개를 넘으면 잘라 내고 **잘랐다고 화면에 밝힙니다**
- **요리** — 걸음을 옮기면 말동무에게도 알립니다. 안 알리면 화면은 5번째인데
  말동무는 3번째를 붙들고 있어 대화가 어긋납니다
- 타이머는 끝나는 시각만 담아 두고 1초마다 다시 셈합니다. 1초씩 빼는 방식은
  탭이 쉬는 동안 멈춰서 늘 늦어집니다. 다 되면 그 자리에서 소리를 만들어 울립니다
- **완성** — 표지를 CSS 로 그리고, 같은 그림을 캔버스로 그려 PNG 로 내려받습니다

**남긴 것 · 반드시 읽어 주세요**

- **제미나이를 실제로 불러 보지 못했습니다.** 이 기계에 쓸 수 있는 키가 없습니다.
  주소·요청 모양·소리 형식은 구글 문서를 그때그때 확인해 맞췄지만,
  **처음 돌릴 때 오류가 날 수 있습니다.** 나면 메시지를 그대로 주세요
- **`/pick` 의 음성 인식은 라이브 모델이 아니라 글자 모델을 씁니다.**
  한 번 말하고 끝나는 자리라 `generateContent` 로 소리를 보내는 편이 맞습니다 —
  라이브는 주고받는 대화용이라 구조화된 JSON 을 받기 어렵습니다.
  라이브 모델은 말씀하신 대로 `/cook` 에서 씁니다. 다르게 하고 싶으시면 말씀해 주세요
- **화면 색을 시안(밝은 바탕)이 아니라 숯불 팔레트로 만들었습니다.** `/pick` 에서
  넘어오는 길이라 한 걸음마다 밝기가 뒤집히면 눈이 놀랍니다. 밝게 가려면 바꿔 드립니다
- **타이머를 제미나이 함수 호출로 걸지 않았습니다.** 오간 말을 글로 받아
  도메인에서 "3분 뒤에 알려줘" 를 읽어 냅니다. 타이머는 틀리면 요리가 타는
  기능이라, 규칙이 눈에 보이고 시험할 수 있는 편이 낫다고 봤습니다
- **걸음 읽어 주기는 브라우저 기능(`speechSynthesis`)입니다.** 제미나이 TTS 를
  또 부르면 그만큼 늦고 돈이 듭니다
- **AI 표지는 잠가 두었습니다.** 목소리 고르기 화면에서 이미 "체험 중에는 AI 표지
  대신 기본 표지가 만들어집니다" 라고 밝혀 두어서, 그 약속과 어긋나지 않게 했습니다.
  "레시피 카드로 만들기" 는 진짜로 됩니다
- **광고 상품의 용량과 값은 자리표시자입니다.** 브랜드 이름은 실재하지만 값은
  우리가 채운 예시이고 어느 쇼핑몰의 지금 값도 아닙니다. 제휴가 붙으면
  `lib/cook-content.ts` 의 `sponsoredItems` 를 갈아 끼웁니다
- 서재 화면이 아직 없어서 "서재 보기" 는 여전히 잠겨 있습니다

**건드린 파일**

새로 만듦
- 도메인 `lib/domain/recipe.ts`, `shopping.ts`, `cook-progress.ts`
- 유스케이스 `lib/usecase/plan-recipe.ts`, `cook-along.ts`
- 어댑터 `lib/adapter/gemini-recipe-gateway.ts`, `gemini-live-gateway.ts`,
  `browser-audio.ts`, `browser-recipe-draft-store.ts`
- `public/audio/pcm-worklet.js` — 소리 전용 쓰레드에서 도는 일꾼
- `lib/cook-content.ts` — 세 화면의 글과 광고 자리
- 화면 `app/shop/`, `app/cook/`, `app/cook/done/`,
  `components/shop/`, `components/cook/`

고침
- `components/pick/voice-console.tsx` — 진짜로 녹음하고 레시피를 받아 `/shop` 으로
- `lib/domain/recipe-shelf.ts`, `lib/usecase/keep-recipe-shelf.ts` — 서재에 레시피 전문
- `app/globals.css` — 세 화면 (약 430줄)

**확인**

- `npx tsc --noEmit`, `npx eslint .`, `npx next build` 모두 통과
- 린트가 잡은 것 셋을 **덮지 않고 고쳤습니다** — 그리는 도중의 `Date.now()`,
  그리는 도중의 ref 쓰기, 효과 안에서 곧바로 상태 바꾸기
- 세 화면을 실제로 띄워 눈으로 확인했습니다. 제품 코드에 시험용 뒷문을 내지 않으려고
  크롬 개발자 프로토콜로 바깥에서 레시피를 넣고 찍었습니다

---

## 2026-08-22 — 회원가입에 닉네임 칸 추가

**요청**
프로필을 만들어야 하니 회원가입할 때 닉네임을 정하는 칸이 있어야겠다.

**한 일**

- 가입 폼 맨 위에 닉네임 칸을 넣었습니다. 이메일·비밀번호보다 **위**에 둔 까닭은,
  "무엇으로 불릴지" 를 먼저 정하는 편이 순서로 자연스럽고 성격도 다르기 때문입니다
  (앞의 둘은 들어오기 위한 열쇠, 이것은 들어온 뒤 남에게 보이는 것)
- 규칙은 `lib/domain/display-name.ts` 에 새로 뒀습니다. `credentials.ts` 에 얹지
  않은 까닭은 같습니다 — 나중에 프로필 화면에서 이름만 고칠 때 비밀번호 규칙이
  딸려 오면 곤란합니다
- 계층을 그대로 타고 내려갑니다. 도메인이 검사하고 → 유스케이스가 순서를 정하고 →
  어댑터가 수파베이스에 넘기고 → 폼은 칸만 그립니다
- **어댑터는 프로필을 직접 만들지 않습니다.** `supabase.auth.signUp` 의
  `options.data.display_name` 으로 넘기면 그 값이 `auth.users.raw_user_meta_data`
  에 담기고, 마이그레이션의 트리거가 같은 트랜잭션 안에서 프로필을 만듭니다.
  앱이 두 번 나눠 부르면 계정은 생겼는데 프로필이 없는 사람이 생길 수 있습니다
- **데이터베이스의 길이 규칙을 도메인에 맞췄습니다.** 1~40 이었던 것을 2~20 으로
  바꿨습니다. 아직 push 안 한 마이그레이션이라 지금이 고칠 때였습니다.
  두 값이 다르면 화면은 통과시켰는데 데이터베이스가 거절하는 일이 생깁니다
- 규칙을 좁히니 **임시 이름 만들기가 걸렸습니다.** 닉네임 없이 만들어진 계정은
  이메일 앞부분을 쓰는데, `a@b.com` 은 한 글자라 짧고 긴 주소는 스무 글자를 넘깁니다.
  그대로 두면 **가입 자체가 데이터베이스 오류로 실패**합니다. 그래서 길이를 맞춰 주는
  `default_display_name()` 을 만들고 트리거·백필·시드 세 군데가 같은 것을 쓰게 했습니다
  (`rpad` 는 긴 글자를 자르기도 해서 짧을 때만 부르도록 조건을 걸었습니다)
- 틀렸을 때 닉네임을 되돌려줍니다. 비밀번호가 틀렸을 뿐인데 이름까지 다시 짓게
  하면 짜증납니다. 비밀번호는 물론 되돌려주지 않습니다
- `AuthField` 가 `type="text"` 도 받게 했습니다. 닉네임은 가릴 것도 자판을 바꿀
  것도 없는 평범한 글자 칸입니다

**건드린 파일**

새로 만듦
- `lib/domain/display-name.ts`

고침
- `lib/usecase/sign-up.ts` — 게이트웨이 약속에 name 추가, 검사 순서
- `lib/adapter/supabase-auth-gateway.ts` — `options.data.display_name`
- `app/actions/auth.ts`, `app/actions/auth-state.ts` — 폼에서 name 꺼내 되돌려주기
- `components/auth/auth-field.tsx` — `type="text"` 허용
- `components/signup/signup-form.tsx` — 칸 추가
- `lib/site-content.ts` — 안내 문구 셋과 예시 글자
- `supabase/migrations/20260822120000_create_profiles.sql` — 길이 2~20,
  `default_display_name()` 추가, 트리거·백필이 그것을 쓰게
- `supabase/seed.sql` — 같은 함수를 쓰게

**확인**

- `npx tsc --noEmit`, `npx eslint .`, `npx next build` 모두 통과
- SQL 세 장을 포스트그레스 문법기로 다시 파싱, 함수 본문까지 통과
- `/signup` 을 띄워 칸이 네 개로 늘고 `name="name"` 이 폼에 실리는 것을 확인

**남긴 것**

- **닉네임이 겹치는 것을 막지 않았습니다.** 유일해야 한다면 표에 unique 를 걸어야
  하는데, 그러면 트리거의 임시 이름이 부딪힐 때(`a@x.com` 과 `a@y.com` 이 둘 다 "a")
  가입이 알 수 없는 오류로 실패합니다. 겹쳐도 되게 두는 편이 지금은 낫다고 봤습니다
- 이미 가입한 사람이 이름을 바꿀 화면이 없습니다. 백필이 이메일에서 딴 이름이
  붙어 있을 텐데, 고치려면 프로필 화면이 필요합니다
- 마이그레이션은 여전히 안 올라가 있습니다

---

## 2026-08-22 — 커뮤니티 예시 글 시드 추가

**요청**
교재(챕터 09) 기준으로 어디까지 왔는지 대조한 뒤, 빠진 것 중 첫 번째인
시드 스크립트를 만들어 달라.

**한 일**

- `supabase/seed.sql` 을 만들었습니다. `config.toml` 의 `[db.seed]` 가
  이미 `./seed.sql` 을 가리키고 있어서 이름을 그대로 맞췄습니다
- 예시 글 아홉 편은 `lib/site-content.ts` 의 `communityPosts` 를 그대로 옮겼습니다.
  화면이 데이터베이스에서 읽도록 바뀌었을 때 **지금과 같은 화면이 나와야**
  무엇이 달라졌는지 견줄 수 있기 때문입니다
- **글을 전부 한 사람 앞으로 넣습니다.** 시안에는 셰프가 아홉 명이지만
  `profiles.id` 가 `auth.users.id` 를 참조하므로 셰프를 아홉 명 만들려면 진짜
  로그인 계정이 아홉 개 필요합니다. **남의 인증 표에 가짜 계정을 심는 것은
  연습용 시드가 할 일이 아니라서**, 가장 먼저 가입한 사람을 글쓴이로 삼았습니다
- 아무도 가입하지 않았으면 오류를 내지 않고 `raise notice` 로 안내만 하고 끝냅니다.
  잘못된 상황이 아니라 "먼저 가입하세요" 라는 안내라서 빨간 오류로 띄울 일이 아닙니다
- **여러 번 돌려도 안전합니다.** 글마다 번호(`5eed0000-…`)를 못 박아 두고
  `on conflict (id) do nothing` 으로 건너뜁니다. 교재는 SQL Editor 에 붙여넣어
  실행하라고 하는데, 잘못 두 번 누르면 글이 두 배가 되는 것을 막으려는 것입니다
- `popular`·`recent` 탭을 위해 `like_count` 와 `published_at` 을 실제 숫자로
  넣었습니다. 그래서 지금 하드코딩된 "인기" 목록과 차례가 조금 다릅니다 —
  하드코딩을 진짜 값으로 바꾼 결과라 잘못된 것이 아닙니다

**건드린 파일**

새로 만듦
- `supabase/seed.sql`

**확인**

- 진짜 포스트그레스 문법기(pglast = libpg_query)로 파싱했습니다. 바깥 문장과
  `do` 블록 본문 모두 통과
- **검증기가 헛돌지 않는지도 확인했습니다.** 일부러 망가뜨린 SQL 을 넣어
  `syntax error at or near "end"` 로 잡아내는 것을 보고 나서 결과를 믿었습니다
- 도커가 없어 실제로 돌려 보지는 못했습니다

**남긴 것**

- **아직 실행하지 않았습니다.** `supabase db push` 는 시드를 돌리지 않습니다 —
  시드는 로컬 `db reset` 때만 자동으로 돕니다. 대시보드 SQL Editor 에 붙여넣어야 합니다
- 마이그레이션 두 장도 여전히 안 올라가 있습니다. 시드보다 그게 먼저입니다
- 셰프 랭킹은 계속 `site-content.ts` 의 예시 값을 봅니다. 진짜 계정이 여럿
  생기기 전에는 표에서 뽑을 수 없습니다
- 커뮤니티 화면은 아직 표를 안 봅니다. 다음 걸음입니다

---

## 2026-08-22 — 한 줄 이름표의 줄 높이 정리 + 글쓰기 마이그레이션과 RLS

**요청**
한 줄짜리 이름표에 문단용 행간이 붙어 칸이 뜨는 것을 고치고, 로그인한 사용자가
블로그 글을 만들 수 있도록 수파베이스 마이그레이션과 RLS 정책을 만들어 달라.

**한 일 · 1) 줄 높이**

- `body` 에 걸어 둔 `line-height:1.75` 는 한글 문단을 위한 값인데, **한 줄짜리
  이름표에서는 행간을 벌리는 게 아니라 글자 위아래에 그냥 여백으로 붙는다.**
  이름과 한 줄 소개를 쌓아 두면 그게 두 배가 된다
- 셰프 순위 한 줄이 66.5px 였고 그중 45.5px 가 line-height 였습니다. 이름표 넷에
  제 값(1.4~1.45)을 주고 순위 칸 여백을 9px→8px 로 줄여 **51.5px** 이 됐습니다
- 고친 곳: `.st-t`·`.st-s`(시작 화면 네 걸음), `.cm-rank-name`·`.cm-rank-note`
  (셰프 순위), `.rail-radio`(왼쪽 기둥), `.pk-model`(카드 모델 표시)

**한 일 · 2) 마이그레이션과 RLS**

- `supabase init` 으로 프로젝트 뼈대를 만들고 마이그레이션 두 장을 넣었습니다
- **`profiles` 를 먼저 만들었습니다.** 글쓴이 이름을 보여 주려면 필요한데,
  `auth.users` 는 앱에서 읽을 수 없습니다 — 아무나 조회하면 회원 이메일이 통째로
  새어 나가기 때문에 수파베이스가 막아 둔 자리입니다. 남에게 보여도 되는 값만
  따로 담습니다. 가입 트리거로 자동 생성하고, **이미 가입한 사람은 백필**합니다
  (이 줄이 없으면 먼저 가입한 사람은 글을 쓰는 순간 외래 키에서 막힙니다)
- `posts` 의 칸 이름은 화면이 이미 쓰는 `CommunityPost` 에 맞췄습니다.
  `popular`·`recent` 는 **칸으로 두지 않았습니다** — 정렬만 다른 것이라
  저장할 값이 아닙니다. `kind` 는 `community`/`brand`/`trend` 셋뿐입니다
- RLS 는 이렇습니다. 펴낸 글은 누구나(로그인 안 해도) 본다 / 초안은 쓴 사람만 본다 /
  글은 로그인한 사람만, 늘 자기 이름으로 / 고치고 지우는 것은 자기 글만 /
  브랜드 레시피는 브랜드 계정만
- **`auth.uid()` 를 `(select auth.uid())` 로 감쌌습니다.** 그냥 쓰면 줄마다
  다시 부르고, 감싸면 한 번만 불러 돌려 씁니다. 줄이 많아질수록 차이가 커집니다
- SELECT 정책을 "펴낸 글"과 "내 초안" **둘로 나눴습니다.** 정책이 여럿이면 OR 로
  묶이므로, 하나로 합쳐 쓰면 로그인 안 한 사람에게도 `auth.uid()` 비교를 시킵니다
- **RLS 만으로는 부족한 곳을 열 단위 권한으로 막았습니다.** RLS 는 "이 줄에
  손대도 되나" 까지만 봅니다. 안 막아 두면 자기 글이기만 하면 `like_count` 를
  999999 로 고칠 수 있습니다. `revoke all` 로 걷어 낸 뒤 필요한 칸만 grant 했고,
  `author_id`·`like_count`·`published`·`published_at`·`is_brand` 는 뺐습니다
- `author_id` 는 기본값이 `auth.uid()` 이고 넣을 권한이 없습니다. 그래서
  **남의 이름으로 쓰는 것이 아예 불가능**합니다. 정책의 `with check` 는 나중에
  권한을 넓히더라도 남도록 둔 이중 방어입니다
- 펴내기는 `set_post_published()` 함수 하나로 모았습니다. `published` 와
  `published_at` 을 따로 열어 두면 언젠가 한쪽만 바뀌어 "펴냈는데 최신 탭에
  없는 글" 이 생깁니다. `security definer` 라 RLS 를 비켜 가므로 함수 안에서
  `author_id = auth.uid()` 를 손으로 검사합니다

**건드린 파일**

새로 만듦
- `supabase/config.toml`, `supabase/.gitignore` — `supabase init` 이 만든 것
- `supabase/migrations/20260822120000_create_profiles.sql`
- `supabase/migrations/20260822120100_create_posts.sql`

고침
- `app/globals.css` — 한 줄 이름표 넷의 `line-height`, 순위 칸 여백

**확인**

- SQL 을 진짜 포스트그레스 문법기(pglast = libpg_query)로 파싱했습니다.
  문장 39개와 PL/pgSQL 함수 본문 3개 모두 통과
- `npx next build` 통과, 셰프 순위 줄 간격이 66.5px → 51.5px 로 줄어든 것을
  헤드리스 크롬 2배 확대로 확인

**남긴 것**

- **마이그레이션을 아직 적용하지 않았습니다.** 남의 데이터베이스에 손대는 일이라
  물어보지 않고 밀지 않았습니다. `npx supabase link --project-ref <ref>` 뒤에
  `npx supabase db push`, 또는 대시보드 SQL Editor 에 두 파일을 차례로 붙여넣으면
  됩니다
- **문법만 검사했고 실제로 돌려 보지는 못했습니다.** 이 기계에 도커가 없어
  `supabase start` 로 로컬 데이터베이스를 띄울 수 없었습니다. 정책이 뜻대로 도는지는
  올린 뒤에 확인해야 합니다
- 글쓰기 화면과 도메인·유스케이스·어댑터는 만들지 않았습니다. 요청하신 것이
  마이그레이션과 RLS 까지였습니다. `posts` 를 다룰 유스케이스는 `sign-in` 과
  같은 짜임새로 붙이면 됩니다
- 좋아요는 아무도 못 올립니다. `like_count` 를 잠가 뒀고 `post_likes` 표를 아직
  안 만들었습니다. 누른 사람을 기록해야 두 번 누르는 것을 막을 수 있어서,
  표 하나를 더 만들고 트리거로 이 숫자를 채우는 것이 다음 걸음입니다
- 커뮤니티 화면은 아직 `lib/site-content.ts` 의 예시 글을 봅니다. 표에서 읽어
  오도록 바꾸는 일은 하지 않았습니다

---

## 2026-08-22 — 시안을 그대로 옮긴 견주기용 화면 /community2 추가

**요청**
`design/blogmain.png` 시안을 우리 디자인 규칙과 상관없이 **그대로** 옮겨서
`/community` 와 견주어 보고 싶다고 하셨습니다.

**한 일**

- `/community2` 를 만들었습니다. 우리 규칙을 일부러 셋 어겼습니다 —
  **영어 문안 그대로**, **사진 사용**, **색·자리 값을 `globals.css` 변수가 아니라
  시안 픽셀에서 집어 온 숫자로** 적었습니다
- 스타일을 `globals.css` 에 넣지 않고 `app/community2/community2.css` 로 따로
  뒀습니다. 견주기용 화면의 값이 우리 디자인 토큰에 섞여 들어가면 나중에 어느 쪽이
  진짜 규칙인지 알 수 없게 됩니다. 모든 규칙을 `.c2` 안에 가둬 뒀습니다
- **눈으로 맞추지 않고 시안 픽셀을 재서 옮겼습니다.** 파이썬으로 시안을 열어
  색을 뽑고(`#0B0A09` 바탕, `#131110` 카드, `#FFB5A8` 로고, `#F4C150` 디저트 배지),
  경계선을 훑어 자리 값을 얻었습니다(머리말 113px, 내용 폭 1028px,
  기둥 228 + 사이 23 + 카드 777, 큰 카드 582~903)
- **글꼴은 글자 폭을 재서 알아냈습니다.** 처음에 Playfair Display 로 짰다가,
  시안의 "Community" 가 폭 186px·높이 44px 인 것을 재 보니 훨씬 좁은 글꼴이었습니다.
  `design/index_dark_2.html` 의 구글 폰트 주소에 **Instrument Serif** 가 적혀 있었고
  폭이 맞았습니다. 이 글꼴은 굵기가 400 하나뿐이라, 시안의 굵은 제목은
  **브라우저가 만든 가짜 굵게**입니다. 그래서 진짜 굵은 글꼴을 받지 않고
  `font-weight:700` 만 지정했습니다 — 그래야 시안과 같아집니다
- 소개글과 카드 요약은 제목보다 글자가 30%쯤 넓었습니다. 제목과 다른 글꼴이라는
  뜻이라 **Noto Serif** 를 이 화면에서만 따로 받아 왔습니다. 크기는 줄바꿈 자리가
  시안과 같아지는 값(14.5px)으로 맞췄습니다
- 사진은 `design/blogmain.png` 에서 세 곳을 잘라 냈습니다. 아래 두 장은 요리 사진이
  아니라 **휴대폰 앱 화면을 찍은 그림**인데, 그대로 옮기는 것이 목적이라 손대지 않았습니다

**건드린 파일**

새로 만듦
- `app/community2/page.tsx` — 전부 서버에서 그려지는 정적 화면
- `app/community2/community2.css` — `.c2` 안에 가둔 별도 스타일
- `public/community2/hero-ribeye.png`, `card-carbonara.png`, `card-cacao.png`
  — 시안에서 잘라 낸 그림

**확인**

- `npx tsc --noEmit`, `npx eslint .`, `npx next build` 모두 통과
- 시안과 렌더를 같은 크기로 나란히 붙여 픽셀로 견주었습니다. 소개글 줄바꿈이
  시안과 다른 자리에서 끊겨서 글자 크기와 너비를 세 번 고쳐 맞췄습니다

**남긴 것**

- **이 그림들은 시안에 박혀 있던 것을 잘라 낸 것이라 우리에게 권리가 없습니다.**
  견주어 보려고 저장소에 넣어 둔 것이고, 서비스에 그대로 쓸 수 없습니다.
  이 화면을 없앨 때 `public/community2/` 도 같이 지워야 합니다
- 탭·검색칸·링크는 아무 데도 가지 않습니다. 시안에 그렇게 그려져 있을 뿐입니다
- 좁은 화면 규칙은 최소한만 넣었습니다. 시안이 넓은 화면 하나뿐이라 맞출 원본이 없습니다
- 카드 요약이 시안은 세 줄, 우리는 네 줄에서 끊깁니다. 글꼴이 완전히 같지 않아서
  남은 차이인데, 더 맞추려면 글자 크기를 시안보다 작게 줄여야 해서 그대로 두었습니다

---

## 2026-08-22 — mono 소제목의 한글이 시스템 글꼴로 떨어지던 것 고침

**요청**
왼쪽 기둥의 "답변 속도 / 도구 / 내 요리 서재" 글꼴이 무엇인지 묻고, 고쳐 달라고
하셨습니다. 다른 글꼴도 추천해 달라고 하셔서 넷 중 **Nanum Gothic Coding** 을
고르셨습니다.

**한 일**

- **원인**: 그 소제목들은 `--mono` 를 쓰는데, `--mono` 의 맨 앞 Roboto Mono 는
  `subsets: ["latin"]` 이라 **한글 글자가 아예 없습니다.** 글꼴 목록은 글자 하나하나마다
  앞에서부터 훑기 때문에 한글만 목록을 타고 내려가는데, 그다음이 `ui-monospace` 였고
  윈도우에서 그건 Consolas 라 여기도 한글이 없습니다. 결국 시스템 기본 한글 글꼴이
  제멋대로 튀어나오고 있었습니다
- `--mono` 목록 가운데에 Nanum Gothic Coding 을 끼워 넣었습니다. **라틴과 숫자는
  그대로 Roboto Mono 가 그리고, 한글만 이 글꼴이 그립니다.** 둘 다 고정폭이라
  "빠르게 · Flash-Lite" 처럼 섞여 있어도 결이 이어집니다
- 소제목 셋(`.rail-head`·`.setup-speed-label`·`.cm-side-head`)의 자간을
  `.14em`~`.18em` 에서 `.02em` 으로 줄였습니다. 그 값은 라틴 대문자 소제목을
  염두에 두고 넣은 것이라 한글에는 과했고, "답 변  속 도" 처럼 흩어져 읽혔습니다.
  한글이 고정폭이라 자간을 안 줘도 이미 고릅니다
- 같은 소제목들을 `font-weight:700` 으로 올렸습니다. 이 글꼴은 굵기가 400·700
  둘뿐인데 400 은 11px 에서 흐려집니다
- 이 고침 하나로 `--mono` 를 쓰면서 한글이 섞이던 다른 자리도 같이 나아집니다 —
  커뮤니티의 분류 딱지(`.cm-badge`)·시간(`.cm-min`), 카드의 모델 표시(`.pk-model`)

**건드린 파일**

- `app/layout.tsx` — `Nanum_Gothic_Coding` 추가 (`preload:false`, 굵기 400·700).
  한글은 조각 파일이 수백 개라 Noto Serif KR 과 같은 방식으로 미리 받지 않습니다
- `app/globals.css` — `--mono` 목록, 소제목 셋의 자간·굵기

**확인**

- `npx tsc --noEmit`, `npx eslint .`, `npx next build` 모두 통과
- 구글 글꼴 메타데이터에는 이 글꼴의 `subsets` 가 `latin` 뿐이지만, 실제로 받아 온
  CSS 에 **Nanum Gothic Coding 용 `@font-face` 가 186개** 들어 있는 것을 확인했습니다.
  한글이 unicode-range 조각으로 다 옵니다
- 헤드리스 크롬 3배 확대로 찍어 한글이 고정폭으로 그려지는 것을 눈으로 확인

**남긴 것**

- 라디오 줄의 "빠르게"·"꼼꼼하게" 는 `--sans`(Pretendard) 그대로입니다. 소제목이
  아니라 고르는 값이라 본문 글꼴이 맞습니다
- `.setup-mark`·`.rail-mark`(COOKPILOT) 처럼 라틴만 있는 자리는 넓은 자간을
  그대로 두었습니다. 거기서는 원래 의도대로 보입니다

---

## 2026-08-22 — 목소리·모델 고르기 화면과 음성 요리 화면 추가

**요청**
첫 번째 시안을 바탕으로 API 키를 넣은 뒤 넘어가는 모델 선택 화면. 음성 인식은
`gemini-3.1-flash-live-preview` 만 쓰므로 모델 고르기는 생략. 고르고 나면 두 번째
시안 같은 음성 인식 화면 — 가운데에 불꽃처럼 움직이는 오디오 비주얼라이저와 마이크
단추, 왼쪽 사이드바에 계량 환산 도구·내 요리 서재·서재 백업. 유튜브 레시피 가져오기와
냉장고를 부탁해는 `gemini-3.7-flash` 또는 `gemini-3.5-flash-lite`. 브랜드 레시피는
나중에 광고로 쓸 자리라 배치만. 색과 디테일은 첫 번째 시안 기준.

**한 일**

- **모델 이름을 화면에서 몰아냈습니다.** `lib/domain/gemini-model.ts` 하나만 세 모델
  이름을 압니다. 목소리를 알아듣는 `gemini-3.1-flash-live-preview` 는 고를 수 없어서
  상수로 박아 두고, 글자 일을 맡는 두 모델은 `quick`/`careful` 이라는 **속도**에
  매달아 두었습니다. 사람이 고르는 것은 모델 이름이 아니라 "얼마나 꼼꼼히" 입니다 —
  모델이 바뀌어도 저장된 값이 그대로 쓸모 있게 하려는 것입니다
- `/start/model` 을 만들고 시안의 배치(로고 → 큰 물음 → 말투 카드 셋 → 시작 단추 →
  답변 속도 → 안내 알약)를 그대로 옮겼습니다. **모델 카드를 늘어놓지 않았습니다.**
  음성 모델은 선택지가 하나뿐이고, 글자 모델은 두 개뿐이라 시안의 "답변 속도"
  토글이 그대로 모델 고르기가 됩니다
- `/pick` 을 만들었습니다. 두 번째 시안의 배치(왼쪽 기둥 + 가운데 마이크 + 아래 카드
  셋)를 옮기되 **색은 첫 번째 시안의 어두운 숯불 팔레트를 썼습니다.** 요리하는 사람은
  이 화면을 부엌에 세워 두고 오래 보는데, 앞 화면과 밝기가 다르면 넘어갈 때마다 눈이
  놀랍니다
- 비주얼라이저는 진짜로 마이크 소리를 받습니다. `AnalyserNode` 가 갈라 준 값을 막대
  41개 높이로 옮기는데, **가운데 막대에 가장 낮은 소리를 맡겼습니다.** 사람 목소리는
  낮은 쪽이 크니까 가운데가 제일 크게 솟아 불꽃 모양이 됩니다. 소리가 없을 때는 CSS
  애니메이션으로 일렁이기만 합니다
- 막대 높이는 React 상태가 아니라 `style.setProperty("--h", …)` 로 직접 씁니다.
  1초에 60번 다시 그리게 하면 브라우저가 버티지 못합니다
- **막대 기본 높이에 `Math.random()` 을 쓰지 않았습니다.** 서버가 그린 화면과 브라우저가
  그린 첫 화면의 막대가 다르면 하이드레이션이 어긋납니다. 막대 번호만 넣으면 늘 같은
  값이 나오는 셈(`sin`·`cos`)으로 대신했습니다
- 왼쪽 기둥에 답변 속도(라디오), 계량 환산, 내 요리 서재 권수, 서재 백업 내려받기·되돌리기,
  이번 세션 질문 횟수, 전부 지우기, API 키 바꾸기를 넣었습니다
- 계량 환산은 부피끼리만 바꿉니다. **무게(g)는 일부러 뺐습니다** — 밀가루 한 컵과 물 한
  컵은 무게가 전혀 다른데 재료를 모른 채 g 을 알려 주면 요리를 망칩니다. 한 컵은
  미국식 240ml 가 아니라 **한국식 200ml** 입니다
- 백업 파일은 도메인이 한 칸씩 다 확인한 뒤에야 서재에 손을 댑니다. 먼저 비우고 나중에
  넣으면 파일이 잘못됐을 때 있던 서재까지 날아갑니다
- `useSyncExternalStore` 가 무한히 다시 그리지 않게, 두 어댑터가 **읽은 글자가 그대로면
  지난번에 만든 객체를 그대로 돌려주도록** 했습니다. 부를 때마다 `JSON.parse` 로 새
  객체를 만들면 내용이 같아도 늘 "다른 값" 이 되어 되풀이가 멈추지 않습니다
- 시작 화면의 "고르기로 넘어가기 (준비 중)" 잠긴 단추를 `/start/model` 로 가는 링크로
  바꿨습니다. 단추처럼 보이지만 링크라 새 탭으로 열거나 주소를 복사할 수 있습니다
- 아이콘에 `flame` 을 더했습니다. 시안의 말투 카드 앞 그림이 불꽃입니다

**건드린 파일**

새로 만듦
- `lib/domain/gemini-model.ts` — 어떤 일에 어떤 모델을 붙일지
- `lib/domain/voice-tone.ts` — 말투 세 가지
- `lib/domain/measure.ts` — 계량 환산 규칙
- `lib/domain/recipe-shelf.ts` — 서재 한 권과 백업 파일의 생김새
- `lib/usecase/choose-cook-setup.ts` — 목소리·속도 골라 두기
- `lib/usecase/keep-recipe-shelf.ts` — 서재 건사하기
- `lib/adapter/browser-cook-setup-store.ts`
- `lib/adapter/browser-recipe-shelf-store.ts`
- `app/start/model/page.tsx`, `components/setup/setup-picker.tsx`
- `app/pick/page.tsx`, `components/pick/pick-shell.tsx`,
  `components/pick/pick-rail.tsx`, `components/pick/voice-console.tsx`,
  `components/pick/pick-cards.tsx`, `components/pick/measure-tool.tsx`

고침
- `app/globals.css` — `/start/model` 과 `/pick` 두 구역 추가 (약 410줄)
- `lib/site-content.ts` — 두 화면의 글과 카드 데이터
- `components/icons.tsx` — `flame` 추가
- `components/start/api-key-form.tsx` — 잠긴 단추를 링크로

**확인**

- `npx tsc --noEmit`, `npx eslint .`, `npx next build` 모두 통과
- `next start` 로 띄우고 `/start`, `/start/model`, `/pick` 모두 200
- 헤드리스 크롬으로 두 화면을 찍어 시안과 견주었습니다. 막대 41개가 서버 HTML 에
  그대로 들어 있어 자바스크립트가 오기 전에도 그림이 보입니다

**남긴 것**

- **Gemini 를 실제로 부르지 않습니다.** 마이크를 켜고 소리를 그리는 데까지만
  했고, `gemini-3.1-flash-live-preview` 와 웹소켓으로 말을 주고받는 일은 안 했습니다.
  모델 이름과 고른 속도는 도메인에 준비되어 있으니 어댑터만 붙이면 됩니다
- 말투 카드의 예문은 글씨로만 보여 줍니다. **눌러서 미리 듣는 기능은 없습니다** —
  그래서 시안의 "눌러서 미리 들어 보세요" 를 "나중에 왼쪽 메뉴에서 바꿀 수
  있습니다" 로 바꿔 적었습니다. 없는 기능을 있는 것처럼 적을 수는 없습니다
- 아래 카드 셋(유튜브·냉장고·브랜드)의 단추는 전부 "(준비 중)" 으로 잠가 두었습니다.
  브랜드 레시피는 요청대로 자리만 잡아 둔 광고 칸입니다
- "서재 열기" 도 잠겨 있습니다. 서재 화면이 아직 없습니다
- 인분 수와 고른 말투는 아직 아무 데도 안 쓰입니다. 요리를 정하는 일을 만들 때 붙습니다

---

## 2026-08-22 — 커뮤니티 페이지 추가

**요청**
`design/blogmain.png` 을 바탕으로 커뮤니티 게시글 화면. Community 아래 부제목
"쿡파일럿과 만들어낸 우리의 맛있는 순간들", 인기 / 최신 / 브랜드 레시피 /
SNS 유행 탭, 이번 주 인기 셰프 랭킹, 자주 찾는 태그, 최상단 헤더의 CookPilot
글씨 좌측에 로고를 항상 배치.

**한 일**

- `/community` 라우트를 만들고 시안의 배치(가운데 큰 제목 → 검색칸 →
  왼쪽 기둥 + 글 목록)를 그대로 옮겼습니다
- 시안의 Popular / Recent / Following 자리를 요청하신 네 갈래로 바꿨습니다.
  **탭을 단추가 아니라 링크로 만들었습니다** — `?tab=recent` 로 주소가 바뀌므로
  보고 있던 탭 그대로 링크를 복사해 줄 수 있고, 뒤로 가기가 탭 단위로 동작하며,
  자바스크립트를 기다리지 않아도 눌립니다. 덕분에 이 화면에는 `"use client"` 가
  한 군데도 없습니다
- 주소창은 아무 글자나 적을 수 있는 자리라, 모르는 탭 이름이 오면 조용히 첫 탭으로
  돌리는 규칙(`resolveTab`)을 도메인에 뒀습니다. 없는 탭에 404 를 띄울 일은 아닙니다
- 탭의 속이름(`popular`·`recent`…)은 도메인이, 부를 말(`인기`·`최신`…)은
  `site-content.ts` 가 정합니다. `credentialMessages` 와 같은 방식입니다
- 셰프 랭킹은 번호 매기는 목록(`<ol>`)으로 뒀습니다. 등수는 배열 차례에서 뽑으므로
  순서만 바꿔도 숫자가 어긋나지 않습니다. 1~3등만 불빛 색
- **사진을 쓰지 않았습니다.** 시안의 요리 사진은 우리 것이 아니고, 바깥 주소에서
  그림을 끌어오면 `next.config` 의 images 설정을 건드려야 합니다(교재 198쪽이
  바로 그 오류를 예시로 듭니다). 대신 쇼케이스 삽화처럼 CSS 로 접시를 그리고
  요리 갈래에 따라 색조 넷만 다르게 했습니다
- 로고를 커뮤니티 머리말과 **로그인·가입 머리말에도** 넣었습니다. "항상 배치"
  라는 말에 맞춰, 이름 글씨 왼쪽이라는 자리를 모든 화면에서 같게 맞췄습니다
  (소개 페이지 머리말에는 이미 있었습니다)
- 소개 페이지 메뉴에 커뮤니티를 넣어 닿을 수 있게 했습니다

**건드린 파일**

새로 만듦
- `app/community/page.tsx` — 구역 조립. `searchParams` 로 탭을 받는다
- `components/community/community-chrome.tsx` — 머리말 · 꼬리말
- `components/community/community-hero.tsx` — 제목 · 부제목 · 검색칸
- `components/community/community-side.tsx` — 탭 · 셰프 랭킹 · 태그
- `components/community/post-cards.tsx` — 큰 카드 · 작은 카드 · 빈 목록 안내
- `lib/domain/community-tab.ts` — 탭 속이름과 주소 걸러 내기

고침
- `lib/site-content.ts` — 커뮤니티 글·셰프·태그 데이터와 문구, 메뉴에 커뮤니티 추가
- `components/icons.tsx` — 아이콘 6종 추가 (돋보기·설정·도움말·기세·하트·꼬리표)
- `components/login/login-chrome.tsx` — 머리말에 로고 추가
- `app/globals.css` — 커뮤니티 구역 전체, `.login-logo` 를 가로 정렬로

**확인**
`tsc --noEmit` · `eslint` · `next build` 통과. 빌드 결과에 `/community` 가
`ƒ`(요청할 때 그리는 화면)로 잡히는 것 확인 — `searchParams` 를 읽기 때문입니다.
네 탭 모두 200 이고 첫 카드가 서로 다른 것(인기=등심, 최신=비빔국수,
브랜드=등심, SNS=까르보나라) 확인. `?tab=nonsense` 는 인기로 돌아가고,
`?tab=popular&tab=recent` 처럼 두 번 실리면 뒤엣것을 따르는 것까지 확인했습니다.
인기 탭에서 카드 4장 · 셰프 5명 · 태그 8개가 그려집니다.
`/login` · `/signup` 머리말에도 로고가 붙은 것 확인.

**남긴 것**
- **글 데이터가 아직 `site-content.ts` 안의 예시입니다.** 수파베이스 표를 아직
  안 만들었기 때문입니다. 화면이 배열을 직접 뒤지지 않고 `postsByTab()` 을
  거치게 해 뒀으니, 나중에 그 함수 자리만 조회로 바꾸면 화면은 그대로입니다
- 검색칸은 겉모습만 있고 아직 아무것도 찾지 않습니다
- 글 하나만 보는 화면(`/community/[id]`)이 없어 카드 제목은 `href="#"` 입니다
- 태그를 눌러 거르는 기능 없음, 글쓰기 화면 없음
- 좋아요 수는 고정된 글자입니다. 누를 수 있는 단추가 아닙니다

---

## 2026-08-21 — 오류 기록 파일 추가

**요청**
방금 난 오류가 무슨 오류인지 설명하고, 해결 방법을 md 파일로 정리해 달라.
앞으로 오류가 날 때마다 그 파일에 모아서 학습용으로 쓸 것.

**한 일**
`ERRORS.md` 를 만들고 `"use server"` 오류를 첫 항목으로 적었습니다.
형식은 증상 / 언제 / 왜 났나 / 어떻게 고쳤나 / 배운 것 다섯 칸입니다.
증상 칸에는 오류 메시지를 번역하지 않고 원문 그대로 넣기로 했습니다 —
나중에 검색해서 찾으려면 원문이 필요하기 때문입니다.

앞으로도 계속 쌓이게 하려고 `AGENTS.md` 에 규칙을 넣었습니다. 이 규칙을
대화 기억이 아니라 저장소에 두는 이유는 `CHANGELOG.md` 때와 같습니다 —
기억은 이 컴퓨터에만 남지만 저장소는 다른 기계에도 따라갑니다.

**건드린 파일**
- `ERRORS.md` — 새로 만듦
- `AGENTS.md` — "오류를 만나면 기록으로 남길 것" 규칙 추가

**남긴 것**
- 지난 작업에서 겪었던 오류들(포트 3000 에 남은 프로세스, Bash heredoc 이
  큰 파일에서 깨진 것 등)은 소급해서 적지 않았습니다. 지금부터 쌓습니다.

---

## 2026-08-21 — 수파베이스 인증 연결 (회원가입 + 로그인)

**요청**
수파베이스로 계정을 관리한다. 회원가입 화면은 로그인 화면과 비슷하게.
회원가입 · 로그인 · 소셜 로그인 없이 이메일 인증만 · Server Component 최대한 활용 ·
Supabase Auth 사용 · 한국어.

**한 일**

자리만 잡아 두었던 `pendingAuthGateway`("아직 준비 안 됐다" 고만 답하던 것)를
진짜 수파베이스 어댑터로 갈아 끼웠습니다. 유스케이스가 이미 `AuthGateway` 라는
약속만 알고 있었으므로 `sign-in.ts` 의 흐름은 손대지 않았습니다.

- **인증 로직을 전부 서버로.** 폼은 Server Action(`app/actions/auth.ts`)을
  부르고, 수파베이스와 이야기하는 일은 서버에서만 벌어집니다. 비밀번호가
  리액트 상태에도 자바스크립트 번들에도 담기지 않습니다
- 폼 컴포넌트는 `useActionState` 만 쓰는 얇은 클라이언트 컴포넌트로 줄였습니다.
  `useState` 넷(이메일·비밀번호·문구·대기)이 전부 사라졌습니다
- 회원가입 화면(`/signup`)은 로그인 화면과 같은 `.login-*` 스타일·같은 위아래
  테두리를 씁니다. 다른 점은 비밀번호 확인 칸 하나
- 두 폼이 같이 쓰는 입력 한 칸을 `components/auth/auth-field.tsx` 로 뽑았습니다.
  움직이지 않아서 서버에서만 그려집니다
- 가입 전용 검사 규칙(`checkNewCredentials`)을 도메인에 넣었습니다. 로그인은
  길이를 안 보고 가입만 봅니다 — 로그인에서 길이를 막으면 예전에 짧게 만든
  사람이 못 들어오기 때문입니다. 기준은 8글자(수파베이스 기본값 6보다 깐깐하게)
- 수파베이스 오류 코드를 우리 낱말로 옮기는 통역을 어댑터 한곳에 모았습니다.
  문구가 아니라 `error.code` 로 갈래를 나눕니다 — 문구는 수파베이스가 언제든
  바꿀 수 있어서입니다
- `proxy.ts` 로 요청마다 로그인 표를 갱신합니다. Next.js 16 에서 `middleware.ts`
  가 `proxy.ts` 로 이름이 바뀌었고 export 이름도 `proxy` 입니다
- `/login` 의 회원가입 링크가 `href="#"` 였던 것을 `/signup` 으로 이었습니다

**건드린 파일**

새로 만듦
- `app/actions/auth.ts` — 로그인 · 가입 · 로그아웃 서버 액션
- `app/actions/auth-state.ts` — 폼과 서버가 주고받는 쪽지의 생김새.
  `"use server"` 파일은 async 함수만 내보낼 수 있어 따로 뺐습니다 (`ERRORS.md`)
- `app/signup/page.tsx` — 가입 페이지
- `components/signup/signup-form.tsx` — 가입 카드
- `components/auth/auth-field.tsx` — 두 폼이 같이 쓰는 입력 한 칸
- `lib/usecase/sign-up.ts` — 가입 유스케이스
- `lib/adapter/supabase-server-client.ts` — 서버 전용 수파베이스 손잡이
- `lib/adapter/supabase-auth-gateway.ts` — 게이트웨이 구현 + 로그아웃 + 현재 사용자
- `proxy.ts` — 로그인 표 갱신

고침
- `components/login/login-form.tsx` — 서버 액션 방식으로 다시 씀
- `lib/domain/credentials.ts` — `checkNewCredentials`, `MIN_PASSWORD_LENGTH` 추가
- `lib/usecase/sign-in.ts` — `unconfirmed` 갈래 추가 (그 밖에는 그대로)
- `lib/site-content.ts` — `signupCopy` 추가, 오류 문구 6개 추가
- `app/login/page.tsx` — 가입 링크 연결
- `app/globals.css` — `.login-notice` 추가
- `ARCHITECTURE.md`, `README.md` — "인증 없음 / 환경변수 없음" 이 틀린 말이 됨
- `package.json` — `@supabase/supabase-js`, `@supabase/ssr`

**확인**
`tsc --noEmit` · `eslint` · `next build` 모두 통과. 빌드 결과에 `/signup` 라우트와
Proxy 가 잡히는 것 확인. `/login` · `/signup` 200 응답, 가입 폼에 입력칸 셋
(`email` · `password` · `confirm`) 확인. 수파베이스 `/auth/v1/settings` 를
읽어 열쇠가 살아 있고(HTTP 200), 이메일 가입이 열려 있으며,
`mailer_autoconfirm` 이 켜져 있어(= Confirm Email 꺼짐) 가입 즉시 로그인되는
상태임을 확인했습니다.

세 검사를 다 통과했는데도 `/signup` 을 실제로 열었을 때 오류가 났습니다
(`"use server"` 파일에서 객체를 export). 서버 액션 로더는 페이지를 여는
시점에 도는 것이라 빌드가 못 잡습니다. 고친 뒤 다시 세 검사와 200 응답을
확인했습니다. 자세한 내용은 `ERRORS.md`.

**남긴 것**
- **가입·로그인을 실제로 눌러 보지는 않았습니다.** 계정을 만드는 일이라
  직접 해 보셔야 합니다. 가입 후 수파베이스 `Authentication → Users` 에
  이메일이 보이면 성공입니다 (교재 190쪽)
- 로그인해도 **화면이 달라지지 않습니다.** 머리말은 그대로 "로그인" 을 보여 주고
  `/start` 도 로그인 여부를 묻지 않습니다. `currentUserEmail()` 과
  `signOutAction()` 은 만들어 두었지만 아직 아무 화면도 부르지 않습니다
- 비밀번호 재설정 없음 ("비밀번호를 잊으셨나요?" 는 아직 `href="#"`)
- 데이터베이스 표와 RLS 정책 없음. 인증만 붙였고 `supabase/` 폴더도 아직입니다
- `.env.local` 은 깃에 안 올라갑니다. 다른 기계에서는 다시 만들어야 합니다

---

## 2026-08-21 — 로그인 페이지 추가

**요청**
`design/login.png` 을 바탕으로 로그인 페이지를 만들고, 랜딩페이지에서
로그인 화면으로 넘어가게 해 달라.

**한 일**
`/login` 라우트를 새로 만들고, 소개 페이지의 시작 단추를 전부 그쪽으로 돌렸습니다.

물어보고 정한 것 세 가지:

- **모든 시작 단추가 로그인으로 갑니다.** 랜딩 → 로그인 → `/start`(키 입력) 순서가
  됐습니다. 머리말 로그인·머리말 시작 단추·서랍 안 둘·첫 화면 단추·마지막 구역
  단추·"맛보기" 요금제 단추까지 일곱 자리입니다.
- **검증까지만 합니다.** 이 저장소에는 서버가 없어서 실제로 로그인시킬 수단이
  없습니다. 가짜로 성공 처리하지 않고, 검사를 통과하면 "아직 준비 중" 이라고
  밝힙니다. `/start` 의 "고르기 (준비 중)" 와 같은 태도입니다.
- **Google·Apple 단추는 뺐습니다.** 시안에는 있었지만 붙일 계획이 정해지지
  않아 화면에서 아예 뺐습니다. "또는" 구분선도 같이 뺐습니다.

계층을 나눠 넣었습니다.

- `lib/domain/credentials.ts` — "받아 줄 만한 이메일·비밀번호인가" 만 압니다.
  이메일은 **골뱅이가 있고 앞뒤가 비지 않았는지만** 봅니다. 더 촘촘한 규칙을
  세우면 실제로 쓰이는 주소가 막히기 때문입니다. 비밀번호는 길이도 글자 구성도
  보지 않습니다 — 그 규칙은 가입할 때 정해지는 것이고, 여기서 막으면 예전에
  짧게 만든 사람이 못 들어옵니다. `api-key.ts` 와 같은 도장(브랜드 타입) 방식이라
  검사를 건너뛴 값은 유스케이스로 흘러들 수 없습니다.
- `lib/usecase/sign-in.ts` — `AuthGateway` 를 **약속으로만** 적어 두고 순서만
  정합니다. 검사에서 걸리면 서버에 물어보지도 않습니다.
- `lib/adapter/pending-auth-gateway.ts` — 그 약속을 지키되 늘 "아직 없다" 고
  답합니다. 서버가 생기면 **이 파일 하나만** 갈아 끼우면 됩니다.
- `components/login/login-form.tsx` — 이 페이지의 유일한 클라이언트 컴포넌트.
  검사도 로그인도 하지 않고 유스케이스만 부릅니다.
- `components/login/login-chrome.tsx` — 맨 윗줄과 맨 아랫줄. 서버 컴포넌트입니다.

정한 것 몇 가지를 더 적어 둡니다.

- **비밀번호는 앞뒤 공백을 떼지 않습니다.** 이메일은 뗍니다. 빈칸으로 끝나는
  비밀번호를 쓰는 사람이 실제로 있어서, 떼면 맞는 비밀번호가 틀리게 됩니다.
- **"이메일 또는 비밀번호가 맞지 않습니다"** 로 뭉뚱그립니다. 어느 쪽이
  틀렸는지 알려 주면 남의 계정이 있는지 없는지 떠보는 데 쓰입니다.
- **`autoComplete` 를 켜 두었습니다.** 비밀번호 자동완성을 막으면 사람들이
  오히려 외우기 쉬운 비밀번호를 씁니다.
- **입력칸에 `name` 을 붙이지 않았습니다.** 자바스크립트가 붙기 전에 단추를
  누르면 폼이 그냥 보내지는데, `name` 이 있으면 **비밀번호가 주소창에 남습니다.**
- 카드 안에 "로그인 없이 바로 시작하기" 를 남겼습니다. 로그인이 아직 안 되는
  동안 이 화면에 온 사람이 갈 데가 없어지기 때문입니다.

색은 `--ember:#F0A492` 하나를 새로 넣었습니다. 시안의 로고와 제목이 주황이
아니라 살구빛입니다. 검정 위에서 약 9:1 이라 제목 글자로 써도 안전합니다.

**건드린 파일**
새로 만듦 — `app/login/page.tsx`, `components/login/login-form.tsx`,
`components/login/login-chrome.tsx`, `lib/domain/credentials.ts`,
`lib/usecase/sign-in.ts`, `lib/adapter/pending-auth-gateway.ts`
고침 — `app/globals.css`(`.login-*` 한 벌), `components/icons.tsx`(mail·lock·
arrow-right·user 넷 추가), `lib/site-content.ts`(로그인 문구·오류 문구·꼬리말
링크, "맛보기" 단추 주소), `components/site-header.tsx`, `components/hero.tsx`,
`components/closing.tsx`

**확인**
`npx tsc --noEmit` 통과, `npx next build` 통과 — `/login` 이 정적 페이지로
생성됩니다(모두 일곱 장). `npx eslint app components lib` 지적 없음.
도메인·유스케이스는 Node 로 직접 불러 갈래를 전부 확인했습니다 — 빈 이메일,
공백만 있는 이메일, 골뱅이 없음, 골뱅이 앞이 빔, 골뱅이 뒤가 빔, 빈 비밀번호,
정상값, 게이트웨이 예외(→ `unreachable`), 서버 거절(→ `rejected`).
이메일 앞뒤 공백이 떼어지고 비밀번호 공백은 보존되는 것도 확인했습니다.
화면은 브라우저에서 띄워 시안과 대조했습니다.

**남긴 것**
- **단추를 눌렀을 때의 화면 변화를 브라우저에서 눈으로 확인하지 못했습니다.**
  브라우저 자동화 도구가 클릭 뒤 계속 시간 초과가 나서 중단했습니다. 로직은
  위처럼 따로 확인했지만, 오류 문구가 실제로 화면에 뜨는 모습은 못 봤습니다.
  개발 서버를 띄워 직접 눌러 봐 주세요.
- 비밀번호 찾기(`#`)와 회원가입(`#`) 은 화면이 없어 자리만 잡아 두었습니다.
- 맨 윗줄 오른쪽 계정 표시는 누를 수 없는 그림입니다. 계정 화면이 없어서입니다.
- `ARCHITECTURE.md` 는 갱신하지 않았습니다. 로그인이 붙었으니 계층 그림과
  "지금 없는 것" 절이 낡았습니다. 따로 요청해 주시면 맞추겠습니다.

## 2026-08-21 — 아키텍처 그림을 Mermaid 로 교체

**요청**
로그인 페이지를 붙이기 전에, 지금까지 구현한 내용을 바탕으로 아키텍처를
Mermaid 로 그려 달라.

**한 일**
`ARCHITECTURE.md` 안의 ASCII 그림 세 개를 Mermaid 로 다시 그리고, 없던
그림 하나를 새로 넣었습니다.

1. **누가 누구를 import 하는가** (flowchart) — 라우트 → 폼 → 어댑터·유스케이스·
   도메인. 화살표마다 무엇을 가져다 쓰는지 라벨을 달았습니다. 안쪽 계층과
   바깥 계층을 색으로 갈라 두어 화살표가 전부 안쪽으로만 향하는 게 보입니다.
2. **화면 조립** (flowchart) — layout·page·components·site-content. Next.js 가
   감싸 주는 관계는 import 가 아니라서 점선으로 구분했습니다.
3. **계층 순서** (flowchart BT) — domain ← usecase ← adapter ← components.
4. **키를 넣었을 때 벌어지는 일** (sequenceDiagram, 새로 추가) — 사용자부터
   localStorage 까지. 빈 값·저장 실패·성공 세 갈래를 모두 그렸습니다.
   저장 완료가 화면에 닿는 길이 두 갈래(반환값 / `notify()` → `useSyncExternalStore`)
   라는 것도 여기서 드러납니다.

그림을 고치는 김에 문서에서 낡은 값도 맞췄습니다. 행별 주석 작업으로 모든
파일이 길어져서 파일별 줄 수 표가 전부 틀려 있었고(예: `api-key-form.tsx`
170 → 199), `site-content.ts` 표에 `apiKeyPlaceholder` 가 빠져 있었습니다.

**건드린 파일**
고침 — `ARCHITECTURE.md`

**확인**
Mermaid 블록 네 개가 모두 `flowchart`/`sequenceDiagram` 으로 열리는지 확인했습니다.
코드는 건드리지 않았습니다.

**남긴 것**
- 로그인 기능은 구현하지 않았습니다. 요청하신 대로 그림까지만 그렸고,
  다음 요청을 기다립니다.
- 그래서 그림에도 로그인 자리를 미리 그려 넣지 않았습니다. 이 문서는 "실제로
  들어 있는 것" 만 적기로 한 문서라, 아직 없는 것을 그리면 규칙이 깨집니다.
  앞으로 갈 자리는 지금도 5장에 글로 적혀 있습니다.
- `app/page.tsx`·`app/start/page.tsx` 의 구역 순서 그림은 ASCII 로 두었습니다.
  위에서 아래로 한 줄인 목록이라 Mermaid 로 바꿔도 나아지지 않습니다.

## 2026-08-21 — 주석을 쉬운 말로 다시 씀

**요청**
주석을 초등학생도 쉽게 읽을 수 있을 정도로 달아 달라.

**한 일**
바로 앞 작업에서 단 주석이 어른 개발자용 말투였습니다. 18개 파일의 주석을
전부 쉬운 말로 다시 썼습니다. 코드는 한 글자도 건드리지 않았습니다.

바꾼 방식은 이렇습니다.

- **어려운 낱말을 풀어 썼습니다.** 그라데이션 → "색 번짐", 파형 → "물결무늬",
  삽화 → "그림", 머리말/꼬리말 → "맨 윗부분/맨 아랫부분", 차림표 → "메뉴",
  좌표 → "모눈종이 위 자리", key → "이름표", 구독 → "알려 달라고 부탁하기".
- **"왜" 를 한 번 더 풀었습니다.** 예를 들어 `y={height - h}` 에는 "컴퓨터 그림은
  맨 위가 0 이다. 그래서 바닥에서 높이를 빼야 막대가 아래에 선다" 처럼,
  결과만이 아니라 그 앞의 사정까지 적었습니다.
- **문장을 짧게 끊었습니다.** 한 줄에 뜻 하나만 들어가게 했습니다.

계층 이름(도메인·유스케이스·어댑터)과 라이브러리 이름(React, localStorage)은
그대로 두었습니다. 이건 찾아볼 수 있어야 하는 이름이라 바꾸면 오히려 헷갈립니다.

**건드린 파일**
고침 — `app/layout.tsx`, `app/page.tsx`, `app/start/page.tsx`,
`components/brand.tsx`, `components/closing.tsx`, `components/faq.tsx`,
`components/features.tsx`, `components/hero.tsx`, `components/icons.tsx`,
`components/pricing.tsx`, `components/showcase.tsx`, `components/site-header.tsx`,
`components/start/api-key-form.tsx`, `components/start/start-head.tsx`,
`lib/adapter/browser-api-key-store.ts`, `lib/domain/api-key.ts`,
`lib/site-content.ts`, `lib/usecase/enter-with-api-key.ts`

**확인**
`npx tsc --noEmit` 통과, `npx next build` 통과 — 여섯 페이지 모두 그대로
생성됩니다. 주석만 바꿨으니 화면은 조금도 달라지지 않았습니다.

**남긴 것**
- `app/globals.css` 는 이번에도 손대지 않았습니다.
- 파일 맨 위의 계층 설명(도메인이 뭘 알고 뭘 모르는지) 같은 큰 덩어리 주석은
  쉬운 말로 풀되 내용은 줄이지 않았습니다. 짧게 줄이면 왜 그렇게 나눴는지가
  사라지기 때문입니다.

## 2026-08-21 — 모든 소스 파일에 행별 한국어 주석

**요청**
지금 있는 코드들에 행별로 다 주석을 달아 달라.

**한 일**
`app/`·`components/`·`lib/` 의 TypeScript·TSX 18개 파일 전부에 AGENTS.md 의
주석 규칙(줄 바로 위, 한국어, 무엇이 아니라 왜)을 적용했습니다.

- 화면 쪽(`app/`, `components/`)은 주석이 하나도 없어 거의 다시 썼습니다.
  JSX 요소마다 그 자리에 왜 그 태그·속성이 붙었는지를 적었습니다 — `aria-hidden`
  을 붙인 이유, `type="button"` 을 눌러 둔 이유, `key` 로 그 값을 고른 이유,
  줄바꿈을 그 자리에서 끊은 이유 같은 것들입니다.
- `lib/` 세 계층은 이미 주석이 붙어 있어 빠진 줄(이른 반환, 정리 함수, 저장소
  호출)만 채웠습니다.
- SVG 는 경로마다 무슨 도형인지 적었습니다(`icons.tsx`, `brand.tsx`).
- `lib/site-content.ts` 는 값이 대부분이라 타입의 각 필드에 그 값이 화면 어디에
  쓰이는지를 붙였습니다.

작업 중에 빌드가 깨져 있는 것을 발견해 함께 고쳤습니다. `api-key-form.tsx` 가
`apiKeyPlaceholder` 를 import 하는데 `site-content.ts` 에 그 export 가 없었습니다.
입력칸 예시 문구이므로 `site-content.ts` 에 추가했습니다.

**건드린 파일**
고침 — `app/layout.tsx`, `app/page.tsx`, `app/start/page.tsx`,
`components/brand.tsx`, `components/closing.tsx`, `components/faq.tsx`,
`components/features.tsx`, `components/hero.tsx`, `components/icons.tsx`,
`components/pricing.tsx`, `components/showcase.tsx`, `components/site-header.tsx`,
`components/start/api-key-form.tsx`, `components/start/start-head.tsx`,
`lib/adapter/browser-api-key-store.ts`, `lib/domain/api-key.ts`,
`lib/site-content.ts`, `lib/usecase/enter-with-api-key.ts`

**확인**
`npx tsc --noEmit` 통과. `npx next build` 통과 — 여섯 페이지 모두 정적으로
생성됩니다. 주석만 넣었으므로 화면에 나가는 결과는 그대로입니다.

**남긴 것**
- `app/globals.css` 는 손대지 않았습니다. 주석 규칙이 코드를 가리키고 CSS 는
  이미 구역별 주석이 붙어 있어, 행별로 다 달면 오히려 읽기 나빠집니다. 원하시면
  따로 해 드리겠습니다.
- `hero.tsx` 의 주석 처리된 "카드 등록 없이 바로" 문구와 `site-content.ts` 의
  주석 처리된 FAQ 항목은 그대로 두었습니다. 왜 접어 두었는지만 적었습니다.
- `apiKeyMessages` 에는 `prefix`·`length`·`shape` 가 남아 있지만 지금 도메인은
  `empty` 만 돌려줍니다. 형식 검사를 일부러 뺀 자리라 문구도 지울지 여쭙고
  나서 정하겠습니다.

## 2026-08-21 — 아키텍처 의존 관계 그림 수정

**요청**
아키텍처 문서의 데이터 흐름 그림이 이상하다는 지적.

**한 일**
`ARCHITECTURE.md` 의 "데이터가 흐르는 방향" 절이 틀려 있어 다시 그렸습니다.
시작 페이지 작업 때 기존 그림에 계층을 급히 끼워 넣으면서 사실과 어긋났습니다.

틀렸던 곳 다섯 가지:

1. **화살표 방향이 반대.** `components → app/page` 에 "import" 라벨이 붙어
   있었는데, 실제로는 `app/page.tsx` 가 컴포넌트를 import 합니다.
2. **"lib/usecase 는 아무것도 import 하지 않음" 이 거짓.** usecase 는 domain 을
   import 합니다. import 가 하나도 없는 것은 domain 뿐입니다.
3. **"lib/site-content.ts 는 어디에도 의존하지 않음" 이 거짓.**
   `components/icons` 에서 타입 `IconName` 을 가져옵니다.
4. **adapter 가 site-content 아래 체인에 있었음.** adapter 는 domain·usecase 만
   알고 site-content 와 아무 관계가 없습니다.
5. **`app/page → app/layout → globals.css` 체인이 거짓.** layout 이 globals.css 를
   import 하고, layout 과 page 사이에는 import 관계가 없습니다 — Next.js 가
   파일 이름을 보고 감쌉니다.

각 파일의 `import` 문을 전부 뽑아 대조한 뒤, 그림을 셋으로 나눠 다시 썼습니다 —
"누가 누구를 import 하는가"(계층), "화면은 어떻게 조립되는가"(렌더링),
"한 군데 어긋난 곳"(site-content 가 icons 의 타입을 빌리는 예외).
**화살표는 `A → B` 가 "A 가 B 를 import 한다"** 는 뜻임을 그림 위에 못 박았습니다.

**건드린 파일**
- `ARCHITECTURE.md` — 2절의 그림과 설명 전면 교체

**확인**
`grep -n '^import'` 로 여덟 개 파일의 import 문을 전부 뽑아 그림과 대조했습니다.

**남긴 것**
이 그림은 손으로 관리하는 것이라 파일이 옮겨지면 또 어긋납니다. import 관계를
자동으로 검사하는 장치(ESLint 의 의존 방향 규칙 등)는 넣지 않았습니다.

---

## 2026-08-21 — 학습 노트 에이전트(code-tutor) 추가

**요청**
코드를 물어보면 기본 문법부터 우리 코드에서의 동작까지 쉽고 단계적으로
설명해 주는 에이전트를 만들 것. 터미널에 바로 답하지 말고 학습용 문서로
정리할 것.

**한 일**
`code-tutor` 서브에이전트와 그것이 쓰는 문서 틀을 만들었습니다.
코드에 대한 질문이 오면 터미널에 답하는 대신 `docs/learn/` 에 학습용 HTML
문서를 한 장 만들고 경로만 알려 줍니다.

문서는 세 부분으로 고정했습니다 — `1부 기본 문법`(저장소와 무관한 최소
예제로 문법 자체를 먼저), `2부 우리 코드에서는`(같은 문법이 이 저장소 어디서
어떻게 쓰이는지, 파일과 줄 번호를 붙여), `3부 직접 확인해보기`(손으로 해 볼
실험). 읽기만 해서는 안 남기 때문에 3부를 넣었습니다.

**결과물을 PDF 가 아니라 HTML 로 한 이유**
처음에는 PDF 로 뽑을 생각이었고 헤드리스 Chrome 으로 변환하는 방법까지
알아봤지만, PDF 는 바이너리라 git 이 바뀐 곳을 비교하지 못하고 문서를 고칠
때마다 파일이 통째로 새로 쌓입니다. HTML 로 하면 텍스트라 diff 가 되고,
브라우저로 바로 읽히며, 인쇄가 필요하면 Ctrl+P 로 PDF 를 뽑을 수 있어
잃는 것이 없습니다. 그래서 변환 단계를 통째로 뺐습니다.

**만들면서 정한 제약**
외부 자원(CDN 글꼴·이미지·스크립트)을 쓰지 않습니다. 인터넷이 없거나 다른
컴퓨터에서 열어도 모양이 같아야 하기 때문입니다. 윈도우·맥에 다 있는 시스템
글꼴만 씁니다.

색은 사이트의 잉걸불 팔레트를 쓰되 **역할이 뒤집힙니다.** 문서는 흰 바탕이라
`#F6C453` 은 명암비가 1.6:1 밖에 안 나와 글자색으로 못 씁니다(테두리·채움
전용). 검은 바탕에서 가장 잘 보이던 색이라 그대로 가져오면 안 보입니다.
이 점을 `learn.css` 맨 위 주석에 적어 두었습니다.

**건드린 파일**
- `.claude/agents/code-tutor.md` — 새로 만듦. 에이전트 규칙
- `docs/learn/_assets/learn.css` — 새로 만듦. 공용 스타일 + 인쇄 규칙
- `docs/learn/_assets/template.html` — 새로 만듦. 문서 골격
- `docs/learn/index.html` — 새로 만듦. 목차
- `docs/learn/01-export-const-type.html` — 새로 만듦. 첫 문서
- `AGENTS.md` — 코드 설명은 문서로 남기라는 절 추가
- `ARCHITECTURE.md` — 문서 표에 `docs/learn/` 추가

**확인**
브라우저로 첫 문서와 목차를 열어 확인했습니다. 한글·코드 블록·콜아웃·표가
의도대로 나옵니다.
문서에 적은 줄 번호를 `sed` 로 전부 대조해 네 곳을 고쳤습니다
(`site-header.tsx:5→6`, `:32→29`, `hero.tsx:44→51`, `tsconfig.json:22→21`).
CSS 버그도 하나 잡았습니다 — 콜아웃 제목 규칙이 자손 선택자라 문장 중간의
`<strong>` 까지 제목처럼 키우고 있었습니다. 자식 선택자(`>`)로 바꿨습니다.

**남긴 것**
에이전트가 실제로 자동 호출되는지는 아직 확인하지 못했습니다. 다음 질문이
왔을 때 터미널에 길게 답하지 않고 `02-*.html` 을 만드는지 봐야 합니다.
코드 하이라이팅은 넣지 않았습니다. 색을 입히려면 자바스크립트 라이브러리가
필요한데 "외부 자원을 쓰지 않는다" 는 제약과 부딪힙니다. 지금은 고정폭 글꼴과
`.hl` 형광펜으로만 구분합니다.

---

## 2026-08-21 — 입력칸 안내 문구를 형식 없는 말로

**요청**
입력칸 안내 문구에 그냥 구글 API 키를 넣으라고 안내할 것.

**한 일**
"AIza... 로 시작하는 키를 붙여넣으세요" 를 **"구글 API 키를 붙여넣으세요"** 로
바꿨습니다. 형식을 검사하지 않기로 한 마당에 안내만 형식을 말하고 있으면
넣어도 되는 키를 못 넣는 것처럼 보입니다.

문구는 컴포넌트에 박아 두지 않고 `apiKeyPlaceholder` 로 `lib/site-content.ts`
에 두었습니다. 글은 한 곳에 모으는 것이 이 저장소 규칙입니다.

안내가 한국어 문장이 되었으므로 `::placeholder` 에서 고정폭 글꼴과 넓은 자간을
벗겼습니다. 입력한 키 자체는 그대로 고정폭이라 자릿수를 세기 쉽습니다.

**건드린 파일**
- `lib/site-content.ts` — `apiKeyPlaceholder` 추가
- `components/start/api-key-form.tsx` — 박아 둔 문구를 그 값으로 교체
- `app/globals.css` — `.start-input::placeholder` 를 본문 글꼴로

**확인**
`npx tsc --noEmit`, `npm run lint` 통과.
서버가 그린 HTML 에서 `placeholder="구글 API 키를 붙여넣으세요"` 를 확인했습니다.
브라우저 화면으로는 확인하지 않았습니다 — 그 브라우저에 실제 키가 저장되어
있어서 입력칸 대신 저장된 모습이 나오는 상태였고, 지우면서까지 볼 일은
아니라고 보았습니다.

**남긴 것**
읽어 주는 기기용 라벨은 "구글 Gemini API 키" 그대로입니다. 화면에는 안 보이고
어떤 서비스의 키인지 분명히 해 두는 편이 나아 두었습니다.

---

## 2026-08-21 — 키 형식 검사 걷어냄

**요청**
키 형식 검사는 안 해도 된다. 진짜 API 키를 넣었는데 오류가 나는 것 같다.

**한 일**
`checkApiKey` 에서 형식 검사를 전부 뺐습니다. 이제 앞뒤 공백을 떼고 빈 값인지만
봅니다.

빼기 전에는 네 가지를 봤습니다 — 빈 값, `AIza` 접두사, **39자 길이**, 글자 구성.
실제 키를 막은 것은 길이 조건일 가능성이 큽니다. 구글이 키 길이를 39자로
약속한 적이 없는데 제가 그렇게 단정했습니다. 접두사와 글자 구성도 같은 이유로
함께 뺐습니다 — 근거가 없는 규칙은 멀쩡한 키를 막기만 합니다.

`ApiKeyProblem` 은 이제 `"empty"` 하나뿐이라 `apiKeyMessages` 에서 prefix ·
length · shape 문구 셋을 지웠습니다. `maskApiKey` 에는 12자 이하인 키를 통째로
덮는 조건을 넣었습니다 — 길이를 더 이상 가정할 수 없게 되어, 짧은 키에 앞 6자
뒤 4자를 남기면 거의 다 드러나기 때문입니다.

**건드린 파일**
- `lib/domain/api-key.ts` — 형식 검사 제거, `maskApiKey` 에 짧은 키 처리 추가
- `lib/site-content.ts` — `apiKeyMessages` 에서 문구 셋 제거
- `ARCHITECTURE.md` — 도메인 설명과 "지금 없는 것" 갱신

**확인**
`npx tsc --noEmit`, `npm run lint`, `npm run build` 모두 통과했습니다.
브라우저에서 44자짜리 키를 넣어 통과하는 것을 보았습니다. 예전 규칙이면
"길이가 맞지 않습니다" 로 막혔을 값입니다.

**남긴 것**
입력칸의 안내 문구는 "AIza... 로 시작하는 키를 붙여넣으세요" 그대로 두었습니다.
검사하지는 않지만 어떤 값을 넣는 자리인지 알려 주는 힌트로는 쓸모가 있고,
`design/screen.png` 시안과도 같습니다.
키가 진짜 쓸 수 있는 것인지는 여전히 확인하지 않습니다. 구글에 짧은 요청을
보내 확인하는 일은 어댑터 계층에 넣을 자리인데, 이번에도 넣지 않았습니다.
지금은 틀린 키를 넣어도 저장되고, 실제로 쓰는 화면이 생겨야 드러납니다.

---

## 2026-08-21 — 시작 페이지(/start) 추가

**요청**
"무료로 시작하기" 를 누르면 우리 디자인에 맞는 시작 페이지로 넘어가게 할 것.
`design/screen.png` 를 바탕으로 만들고 색·폰트는 기존과 어울리게 할 것.

**한 일**
`/start` 를 새로 만들고 홈의 모든 "무료로 시작하기" 단추를 그쪽으로 이었습니다.

이번에 처음으로 비즈니스 규칙(키 검증)이 생겼으므로 `AGENTS.md` 의 클린
아키텍처 규칙을 그대로 적용해 계층을 나눠 넣었습니다.

- `lib/domain/api-key.ts` — 키가 올바른지만 판단합니다(AIza 접두사, 39자, 글자 구성).
  React 도 브라우저도 모릅니다. 검증을 통과한 값에만 `ApiKey` 표식을 붙여
  검증을 건너뛴 문자열이 유스케이스로 흘러들 수 없게 했습니다.
- `lib/usecase/enter-with-api-key.ts` — "키를 넣고 들어가기". 저장소는 `ApiKeyStore`
  인터페이스로 선언만 하고 구현은 밖에서 받습니다.
- `lib/adapter/browser-api-key-store.ts` — 그 인터페이스를 localStorage 로 채운 구현.
  localStorage 를 아는 유일한 파일입니다. 쿠키가 아닌 localStorage 를 쓴 것은
  쿠키는 요청마다 서버로 딸려가기 때문입니다 — 요금제에 적어 둔 "키는 브라우저
  안에만 있고 서버로 보내지 않는다" 는 약속을 지키려면 쿠키를 쓰면 안 됩니다.

화면은 시안을 따라 로고 · 이름 · 한 줄 약속 · 네 걸음(준비/고르기/장보기/요리) ·
접어 둔 사용법 · 키 입력 카드 · 키 받으러 가는 길 순서로 놓았습니다.
새 색이나 글꼴을 만들지 않고 `:root` 토큰과 `.btn`·`.rule`·`.glow` 를 그대로
썼습니다. 가로폭만 720px 로 좁혔습니다 — 할 일이 하나뿐인 화면이라 시선이
갈라지면 안 되기 때문입니다.

키 입력 카드만 클라이언트 컴포넌트입니다. 저장된 키를 읽을 때 `useEffect` +
`useState` 대신 `useSyncExternalStore` 를 썼습니다. localStorage 는 React 밖에
있는 값이라 이쪽이 맞고, 서버 몫이 언제나 null 이라 서버가 그린 화면과
브라우저의 첫 화면이 어긋나지 않습니다. 처음에 `useEffect` 로 썼다가
ESLint 가 잡아서 바꾼 것입니다.

새 규칙대로 새로 쓴 코드에는 행마다 주석을 달았습니다.

**건드린 파일**
- `lib/domain/api-key.ts` — 새로 만듦
- `lib/usecase/enter-with-api-key.ts` — 새로 만듦
- `lib/adapter/browser-api-key-store.ts` — 새로 만듦
- `app/start/page.tsx` — 새로 만듦
- `components/start/start-head.tsx` — 새로 만듦 (서버 컴포넌트)
- `components/start/api-key-form.tsx` — 새로 만듦 (이 페이지의 유일한 클라이언트 컴포넌트)
- `app/globals.css` — `.start-*` 규칙 추가. 그리고 FAQ 의 `details`·`summary` 규칙을
  `.faq` 안으로 가두었습니다 (아래 참고)
- `components/icons.tsx` — `eye`·`eye-off`·`key`·`spark` 네 개 추가, `Icon` 에 `size` 옵션
- `lib/site-content.ts` — 시작 페이지 글 추가, `Plan` 에 `href` 필드 추가
- `components/hero.tsx`·`closing.tsx`·`site-header.tsx`·`pricing.tsx` — CTA 를 `/start` 로

**확인**
`npx tsc --noEmit`, `npm run lint`, `npm run build` 모두 통과했습니다.
`/start` 는 정적으로 미리 그려집니다.
브라우저에서 직접 눌러 보았습니다 — 틀린 키(`sk-...`)는 "AIza 로 시작합니다"
안내가 뜨고, 형식이 맞는 키는 저장된 뒤 가린 형태(`AIzaSy········0abc`)로
바뀌며, "다른 키로 바꾸기"로 다시 입력칸으로 돌아갑니다.
머리말의 "무료로 시작하기" 를 눌러 `/start` 로 넘어가는 것까지 보았습니다.

**남긴 것**
키는 형식만 보고 실제로 살아 있는지는 확인하지 않습니다. 구글에 짧은 요청을
보내 확인하는 일은 어댑터 계층에 넣을 자리인데, 이번에는 넣지 않았습니다.
다음 걸음인 "고르기" 화면이 없어서, 키를 넣은 뒤의 단추는 눌리지 않는
"준비 중" 상태로 두었습니다. 키만 받아 두고 갈 데가 없는 상태입니다.
머리말의 "로그인", 요금제의 홈셰프·키친 단추는 여전히 `href="#"` 입니다.
맛보기만 `/start` 로 이었습니다.

작업 중 FAQ 의 `details`·`summary` CSS 가 맨 selector 라 새 페이지의
`<details>` 까지 끌어가는 것을 발견해 `.faq` 안으로 가두었습니다. 홈페이지
FAQ 모양은 그대로입니다. 같은 식으로 맨 selector 를 쓴 곳이 `globals.css` 에
더 있는지는 따로 훑어보지 않았습니다.

---

## 2026-08-21 — 아키텍처 안내 문서 추가

**요청**
지금 폴더의 아키텍처가 어떻게 되는지, 파일마다 무슨 기능을 하고 어떤 내용이
담겼는지 md 파일로 정리할 것.

**한 일**
`ARCHITECTURE.md` 를 새로 썼습니다. 여섯 절 구성입니다.
정적 랜딩 페이지 한 장이라는 요약, 글(`lib/site-content.ts`)·배치(`components/`)·
모양(`app/globals.css`) 세 축으로 나뉜 관심사와 그 의존 방향,
`app/`·`components/`·`lib/`·`design/`·`public/`·설정·문서 순의 파일별 설명,
지금 없는 것(API·DB·테스트·환경변수), 새 클린 아키텍처 규칙과의 거리,
실행 명령.
파일별 설명에는 읽어야 알 수 있는 것들을 적었습니다 — `site-header.tsx` 가
유일한 클라이언트 컴포넌트라는 점, `--fire-1` 은 명암비 3.7:1 이라 글자색으로
못 쓴다는 점, SVG 그라데이션 id 를 부르는 쪽이 넘겨주는 이유,
`showcase.tsx` 의 `satisfies` 가 삽화 종류 누락을 컴파일 타임에 잡아 준다는 점,
`b.side` 가 좁은 화면의 읽는 순서를 결정한다는 점.

**건드린 파일**
- `ARCHITECTURE.md` — 새로 만듦

**확인**
`app/`·`components/`·`lib/` 의 모든 소스와 설정 파일을 직접 읽고 썼습니다.
줄 수는 `wc -l` 값입니다. 코드는 바꾸지 않아 실행 검증은 없습니다.

**남긴 것**
`app/globals.css` 는 458줄이라 절 단위 개요만 적고 규칙 하나하나는
풀지 않았습니다.
`README.md` 의 구조 표와 내용이 일부 겹칩니다. README 는 시작하는 사람용,
ARCHITECTURE 는 코드를 고칠 사람용으로 두었는데, 겹치는 표를 정리할지는
따로 정해야 합니다.
문서를 쓰다 발견한 두 가지는 손대지 않았습니다 — `public/` 의
create-next-app 잔여 SVG 다섯 개가 안 쓰이고 있고,
`lib/site-content.ts` 의 `faqs` 에 음성 저장 관련 항목이 주석으로 남아 있습니다.

---

## 2026-08-21 — 클린 아키텍처·행별 주석 규칙 추가

**요청**
앞으로 코딩할 때 클린 아키텍처를 바탕으로 짜고, 행마다 주석을 달라는 규칙을 만들 것.

**한 일**
`AGENTS.md` 에 규칙 두 개를 덧붙였습니다.
"클린 아키텍처를 지킬 것" 은 계층 순서(도메인 → 유스케이스 → 어댑터 → 프레임워크),
의존 방향은 안쪽으로만, 바깥 의존은 인터페이스로 뒤집기, 컴포넌트는 얇게,
폴더도 계층을 따를 것을 정했습니다.
"행마다 주석을 달 것" 은 한 줄마다 그 줄 위에 한국어 주석을 달되
코드를 옮겨 적지 말고 이유를 적고, 닫는 괄호·빈 줄·import 묶음은 빼고,
코드를 고치면 주석도 같이 맞추도록 했습니다.
`CLAUDE.md` 가 `@AGENTS.md` 를 그대로 불러오므로 따로 고치지 않았습니다.

**건드린 파일**
- `AGENTS.md` — 규칙 두 절 추가

**확인**
문서만 바뀌어 실행 검증은 없습니다. 다음 코드 작업부터 적용됩니다.

**남긴 것**
기존 `app/`·`components/`·`lib/` 코드는 새 규칙에 맞춰 손대지 않았습니다.
지금 구조를 계층대로 다시 정리할지는 따로 정해야 합니다.
ESLint 로 강제하는 장치(의존 방향 검사 등) 도 넣지 않았습니다.

---

## 2026-08-21 — README 를 프로젝트 문서로 교체

**요청**
다른 기계에서 시작할 때 필요한 git 세 줄을 저장소가 알려주게 할 것.

**한 일**
create-next-app 기본 README (없어진 Geist 글꼴을 안내하고 있었음) 를 지우고
프로젝트 문서로 새로 썼습니다. 맨 위에 clone → cd → npm install 세 줄을 두고,
새 기계에서는 `claude -c` 가 아니라 `claude` 로 시작해야 하는 이유
(대화 기록은 저장소가 아니라 `~/.claude/projects/` 에 로컬로 쌓임) 를 적었습니다.
그 아래에 폴더 구조, 문서 안내, 명령 목록.

**건드린 파일**
- `README.md` — 전면 교체

**확인**
`.gitignore` 로 `node_modules`·`.next`·`next-env.d.ts` 가 빠지는 것,
`process.env` 사용처가 없어 `.env` 가 필요 없는 것 확인 후 문서에 반영.

---

## 2026-08-21 — FAQ 항목 하나 내림

**요청**
직접 수정 (사용자가 편집기에서 고침).

**한 일**
"제 목소리가 저장되나요?" 문답을 주석 처리해 화면에서 뺐습니다.
지우지 않고 주석으로 남겼으니 되살리기 쉽습니다. 자주 묻는 질문이 6개 → 5개.

**건드린 파일**
- `lib/site-content.ts` — `faqs` 배열

**확인**
렌더된 HTML 에서 `<details>` 5개, "제 목소리가" 문구 사라진 것 확인.

---

## 2026-08-21 — 디자인 시안을 홈페이지로 구현

**요청**
`design/index_dark_2.html` 와 `design/logo.svg` 를 바탕으로 홈페이지 구성.

**한 일**

정적 HTML 한 장을 Next.js 16 App Router 구조로 옮겼습니다. 시안의 색·여백·반응형
분기는 값 그대로 유지했고, 마크업만 구역별 컴포넌트로 쪼갰습니다.

- 시안의 `<style>` 블록을 `app/globals.css` 로 이식 (숯불 팔레트, 잉걸불 후광,
  양끝이 사라지는 구분선, 반응형 분기 전부 원본 값)
- Tailwind v4 는 남겨 두고 `@theme inline` 으로 팔레트·글꼴을 유틸리티에 연결
- 시안에 박혀 있던 문구·요금·FAQ 를 `lib/site-content.ts` 한 곳으로 분리
- 글꼴은 `next/font/google` 로 셀프호스팅 (Instrument Serif / Noto Serif KR /
  Roboto Mono). 한글 파일은 수가 많아 `preload: false`. Pretendard 만 CDN
- 로고를 `app/icon.svg` · `app/apple-icon.png` 로 넣고 create-next-app 기본
  `favicon.ico` 삭제
- 시안에서 동작하지 않던 햄버거 버튼을 실제 서랍 메뉴로 구현
  (Esc 로 닫기, 링크 누르면 자동 닫힘, `aria-expanded`, 열리면 X 자로 변형)
- 본문 바로가기 링크와 `:focus-visible` 테두리 추가
- 장바구니 삽화의 "3개만 사면 됩니다" 를 데이터에서 자동 계산

**건드린 파일**

새로 만듦
- `lib/site-content.ts` — 문구·요금·FAQ·기능 목록
- `components/brand.tsx` — 로고, 파형 (그라데이션 id 를 prop 으로 받음)
- `components/icons.tsx` — 기능 칸 선 아이콘 6종
- `components/site-header.tsx` — 머리말 + 모바일 서랍 (유일한 클라이언트 컴포넌트)
- `components/hero.tsx` — 첫 화면
- `components/features.tsx` — 큰 문장 + 기능 6칸
- `components/showcase.tsx` — 좌우로 번갈아 놓인 3덩어리와 삽화 3종
- `components/pricing.tsx` — 요금제 3장
- `components/faq.tsx` — 자주 묻는 질문
- `components/closing.tsx` — 마무리 권유 + 꼬리말
- `app/icon.svg`, `app/apple-icon.png` — 로고

고침
- `app/page.tsx` — 구역 조립
- `app/layout.tsx` — 글꼴, 메타데이터, `lang="ko"`, `themeColor`
- `app/globals.css` — 디자인 시스템 전체

지움
- `app/favicon.ico` — create-next-app 기본 아이콘

**확인**
`tsc --noEmit` · `next build` · `eslint` 통과. 렌더된 HTML 에서 기능 6칸 /
요금제 3장 / FAQ 6개 / 쇼케이스 3덩어리 / 글꼴 변수 3종 확인.

브라우저 스크린샷은 못 찍었습니다. 연결된 Chrome 이 개발 머신이 아닌 다른
기기라 그쪽 `localhost:3000` 의 다른 프로젝트 페이지가 떴습니다.

**남긴 것**
- 로그인·이용약관 등 링크는 시안과 같은 `href="#"` 자리표시자.
  주요 CTA 만 `#pricing` 으로 연결
- OG 이미지 파일 없음 (필요하면 `app/opengraph-image` 로 추가)
- `public/` 의 create-next-app 기본 SVG 는 손대지 않음
