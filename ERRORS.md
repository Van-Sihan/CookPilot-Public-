# 오류 기록

막혔던 오류와 푼 방법을 모읍니다. 최신 항목이 위로 옵니다.

같은 오류를 두 번 겪지 않는 것도 목적이지만, 더 중요한 것은 **왜 그런 규칙이
있는지** 알아 두는 것입니다. 오류 메시지는 대개 "무엇이 잘못됐다" 만 말하고
"왜 그렇게 정해 두었는지" 는 말해 주지 않습니다. 그 부분을 여기에 적습니다.

각 항목은 이렇게 적습니다.

- **증상** — 화면에 뜬 메시지를 그대로. 나중에 검색해서 찾으려면 원문이 필요합니다
- **언제** — 무엇을 하다가 났는지
- **왜 났나** — 진짜 원인. "무엇을 고쳤다" 가 아니라 "왜 그게 문제였나"
- **어떻게 고쳤나** — 실제로 한 일
- **배운 것** — 다음에 같은 함정을 피하는 법

---

## 고정폭 글꼴만 지정하면 한글이 두부(□)로 나온다

**증상**

전체 흐름 지도 SVG 의 작은 글씨에서 한글 한 글자가 빈 네모로 그려졌다.

```
/start 로 간다
?next= □ 읽지 않는다      ← "를" 자리가 네모
```

오류 메시지는 없다. 콘솔도 조용하다. 그림으로만 드러난다.

**언제**

`docs/flow/map.html` 의 플로차트 도형에 붙는 작은 부제를 고정폭으로 그릴 때.

```
MONO = "Consolas,'Courier New',monospace"
```

**왜 났나**

`font-family` 는 **글꼴 목록이지 글자 목록이 아니다.** 브라우저는 글자 하나마다
목록을 왼쪽부터 훑어 그 글자의 글리프를 가진 첫 글꼴을 쓴다. Consolas 와
Courier New 에는 한글 글리프가 없다. 마지막 `monospace` 는 시스템이 정한
기본 고정폭으로 풀리는데 그것도 라틴 글꼴이다. 그래서 **목록 어디에도
그 글자가 없어** 대체할 곳이 없고, 브라우저는 두부를 그린다.

한글이 다 깨지지 않고 한 글자만 깨진 것이 헷갈리는 지점이다. 대체는
글자 단위로 일어나므로, 어쩌다 다른 글꼴이 잡힌 글자는 멀쩡히 나오고
못 잡은 글자만 네모가 된다. "한글이 깨진다" 가 아니라 "글자마다 다르다" 로
보이는 까닭이다.

`monospace` 를 뒤에 붙였으니 괜찮겠지 하는 것이 함정이다. 그것은
"고정폭이면 아무거나" 라는 뜻이지 "무슨 글자든 그려 준다" 는 뜻이 아니다.

**어떻게 고쳤나**

목록 안에 한글 글꼴을 끼워 넣었다. 라틴은 앞의 Consolas 가 가져가고,
한글만 뒤로 넘어간다.

```
MONO = "Consolas,'Malgun Gothic','Courier New',monospace"
```

**배운 것**

- 한글이 섞이는 자리에 고정폭을 쓸 때는 **한글 글꼴을 목록에 반드시 함께** 적는다.
  `monospace` 는 대비책이 아니다.
- SVG `<text>` 는 CSS 상속을 안 받는 자리가 많아 `font-family` 를 태그마다
  적게 되는데, 그럴수록 한 곳에 상수로 모아 두어야 이런 누락이 한 번에 잡힌다.
- 글꼴 문제는 **오류를 안 낸다.** 눈으로 봐야 보인다. 문서를 만들면
  화면 사진을 꼭 한 번 찍어 본다.


## 2026-08-24 — JSX 안에 넣은 `//` 주석이 화면에 글자로 찍힘

**증상**

```
components/community/community-side.tsx(102,70): error TS17008: JSX element '태그' has no corresponding closing tag.
```

그런데 **오류가 안 난 파일이 더 위험했다.** `youtube-source.tsx` 는 컴파일을
멀쩡히 통과했는데, 화면에 이런 글자가 그대로 찍혔다.

```
// [F3][분기] thumb 이 있나? [true] 그림 자리를 만든다 / [false] 아예 안 만든다
```

**언제**

흐름 추적 주석을 파일 스무 개에 한꺼번에 넣던 중. 앵커 줄 위에 `//` 주석을
끼워 넣는 스크립트를 썼는데, 그 앵커가 JSX 안쪽인 경우가 섞여 있었다.

**왜 났나**

**JSX 자식 자리에서 `//` 는 주석이 아니다. 그냥 글자다.**

```tsx
<a href={url}>
  // [F3][분기] thumb 이 있나?      ← 주석이 아니라 텍스트 노드
  {thumb && <span>…</span>}
</a>
```

`{}` 안이 아니면 JSX 는 그 자리를 **화면에 그릴 내용**으로 읽는다.
그래서 주석이 그대로 렌더링된다. 주석을 넣어야 하는 자리라면
`{/* … */}` 로 감싸야 한다.

community-side 만 컴파일 오류가 난 까닭은 그 줄에 `<태그>` 가 들어 있었기
때문이다. 꺾쇠가 있으니 JSX 태그로 읽혔고, 닫는 태그가 없다고 걸렸다.
꺾쇠가 없는 줄은 **오류 없이 그냥 글자가 됐다** — 그쪽이 더 나쁘다.

**어떻게 고쳤나**

스크립트로 훑어서 고쳤다. 판별은 간단하다 —
`// [F…]` 주석 덩어리 **바로 다음 줄**이 `{` 나 `<` 로 시작하면 JSX 자리다.

```python
if nxt.startswith("{") or nxt.startswith("<"):
    # // 주석 → {/* 주석 */} 로 바꾼다
```

스물세 줄이 걸렸다. 고친 뒤 컴파일러로 한 번, **열여섯 개 화면을 실제로 받아 와
`[F` 라는 글자가 섞여 나오는지**로 또 한 번 확인했다(둘 다 0건).

**배운 것**

- **컴파일이 통과했다고 맞는 게 아니다.** 이번 것은 타입도 문법도 멀쩡한 채로
  화면에만 쓰레기 글자가 찍혔다. 눈이나 렌더 결과로 봐야 잡힌다.
- 같은 언어 안에서도 **자리에 따라 주석 문법이 다르다.** TSX 는 한 파일 안에
  JS 자리(`//`)와 JSX 자리(`{/* */}`)가 섞여 있다. 코드를 프로그램으로 고칠 때
  이 경계를 모르면 조용히 망가진다.
- 대량 편집을 했으면 **컴파일러 한 번 + 실제 출력 한 번**, 두 가지로 확인한다.
  앞엣것은 문법을, 뒤엣것은 "사람이 보는 것" 을 본다.

---

## 2026-08-24 — 주석 한 줄을 넣었다가 파일이 문법 오류로 깨짐

**증상**

```
lib/adapter/csv-reviews.ts(33,7): error TS1127: Invalid character.
lib/adapter/csv-reviews.ts(33,46): error TS1002: Unterminated string literal.
```

**언제**

흐름 추적 주석을 파이썬 스크립트로 끼워 넣던 중. 다른 파일 스무 개는 멀쩡했는데
이 파일 하나만 깨졌다.

**왜 났나**

넣으려던 주석이 이랬다.

```
// 입력: pageContent('id: 1
dish: …' 모양) → 처리: …
```

주석 **안에** `
` 을 적었는데, 스크립트를 heredoc 으로 넘기는 과정에서
`\n` 이 `
` 으로 한 번 줄어들고, 파이썬이 그것을 **진짜 줄바꿈**으로 읽었다.
그래서 한 줄짜리 주석이 두 줄로 쪼개졌다.

```
// 입력: pageContent('id: 1
dish: …' 모양) → 처리: …          ← 이 줄은 주석이 아니라 코드다
```

`//` 가 없는 둘째 줄이 그대로 코드가 되면서 문법이 깨졌다.

요점은 heredoc 이 아니다. **주석은 "무시되는 글" 이 아니라 첫 `//` 부터 줄 끝까지만
주석**이라는 것이다. 주석 문자열 안에 줄바꿈이 들어가는 순간 그 뒤는 코드다.
주석을 프로그램으로 생성할 때 이 경계가 가장 쉽게 무너진다.

**어떻게 고쳤나**

그 줄을 줄바꿈 없는 한 문장으로 바꿨다.

```
// 입력: pageContent(줄마다 '칸이름: 값' 인 글) → 처리: 줄마다 첫 콜론에서 가름 → 출력: {칸: 값}
```

그리고 나머지 파일에도 같은 일이 없는지 `tsc --noEmit` 으로 전부 확인했다.

**배운 것**

- **주석을 자동으로 넣을 때는 넣는 글에 줄바꿈이 섞이지 않았는지부터 본다.**
  한 줄 주석에서 줄바꿈은 곧 "여기부터 코드" 라는 뜻이다.
- 코드 예시를 주석에 적을 때 `
` 같은 이스케이프를 그대로 쓰지 않는다.
  말로 풀어 적으면("줄마다 …") 도구를 몇 겹 거쳐도 안 깨진다.
- 파일을 여러 개 한꺼번에 고쳤을 때 **컴파일러를 한 번 돌려 보는 것**이
  가장 싸게 전부를 확인하는 방법이다. 눈으로는 스무 개 중 하나를 못 찾는다.

---

## 2026-08-24 — 칸 하나를 더 달라고 했다가 커뮤니티 목록이 통째로 비어 버림

**증상**

프로필 사진을 붙이려고 읽어 오는 칸에 `avatar_url` 을 하나 더했다.
그러자 "최신" 탭에서 **사람이 쓴 글 열 편이 전부 사라졌다.** 오류는 안 났다.
화면은 멀쩡했고 예시 글 넉 장만 남아 있었다.

브라우저로 세어 보고서야 알았다.

```
카드 수: 4        (고치기 전에는 14)
사진 있는 카드: 4
```

**언제**

프로필 사진을 넣으려고 `supabase-post-reader.ts` 의 읽어 올 칸 목록을
이렇게 고친 직후. 마이그레이션 SQL 은 아직 안 돌린 상태였다.

```
profiles(display_name)  →  profiles(display_name,avatar_url)
```

**왜 났나**

`profiles.avatar_url` 은 방금 쓴 마이그레이션에 들어 있고, 그 SQL 은
아직 데이터베이스에 안 돌아갔다. 그러니 그 칸은 **없는 칸**이었다.

수파베이스(PostgREST)는 없는 칸을 달라고 하면 **그 칸만 빼고 주지 않는다.
물음 전체를 거절한다.** 사진 한 칸 때문에 제목도 본문도 좋아요도 다 안 온다.

그런데 우리 코드가 실패를 이렇게 받고 있었다.

```ts
if (error || !data) return [];
```

"표가 아직 없거나 못 다녀왔으면 예시 글만 보인다" 는 뜻으로 써 둔 줄이다.
그 자체는 맞다 — 커뮤니티가 오류 화면으로 죽는 것보다 낫다. 다만 그 너그러움이
**칸 이름 하나 틀린 것까지 조용히 삼켰다.** 화면은 멀쩡해 보이는데 내용만 없었다.

진짜 문제는 순서다. 코드는 배포하면 곧바로 도는데 SQL 은 사람이 손으로 돌린다.
그 사이가 몇 분이든 며칠이든, 그동안 **커뮤니티에 글이 하나도 없는 서비스**가 된다.
"코드와 스키마를 같이 올리면 되지" 는 실제로 잘 안 지켜진다.

**어떻게 고쳤나**

칸 목록을 두 벌 두고, 없는 칸 때문에 거절당했을 때만 한 번 더 물어본다.

```ts
const COLUMNS_NO_AVATAR = COLUMNS.replace(",avatar_url", "");

let { data, error } = await ask(COLUMNS);
if (missingColumn(error)) ({ data, error } = await ask(COLUMNS_NO_AVATAR));
```

`missingColumn` 은 오류 번호를 본다 — postgres 의 `42703`("그런 칸 없다") 과,
붙여 오는 표의 칸이 틀렸을 때 PostgREST 가 내는 `PGRST200`.
**다른 까닭으로 실패한 것까지 다시 물어보지 않는다.** 그러면 같은 실패를 두 번 한다.

**배운 것**

- `if (error) return []` 는 편한 만큼 위험하다. **"못 다녀왔다" 와 "잘못 물어봤다" 는
  다른 일인데** 한 줄로 묶으면 뒤엣것이 영영 안 보인다. 삼킬 거면 어떤 오류를
  삼키는지 정도는 갈라 두는 편이 낫다.
- 스키마에 칸을 더하는 변경은 **코드가 먼저 도는 시간**을 늘 염두에 둬야 한다.
  칸을 더하는 쪽은 대개 안전하지만, **읽는 쪽에서 그 칸을 요구하기 시작하는 순간**
  안전하지 않게 된다. 요구는 나중에, 아니면 없어도 되게.
- 화면이 안 깨졌다고 멀쩡한 것이 아니다. 이번 것은 오류도 경고도 없었고
  **개수를 세어 보고서야** 잡혔다.

---

## 2026-08-23 — 유튜브에서 옮겨 온 레시피가 "쿡파일럿이 만든 기본 레시피" 로 바뀜

**증상**

유튜브 주소를 넣어 레시피를 가져왔는데, 장보기 화면 출처 자리에 이렇게 떴다.

```
쿡파일럿이 만든 기본 레시피입니다
```

브라우저 저장소를 열어 보면 담긴 값에는 출처가 멀쩡히 들어 있었다.

```
"source":{"kind":"youtube","channel":"백종원 PAIK JONG WON","url":"https://..."}
```

담긴 것과 보이는 것이 달랐다.

**언제**

유튜브 출처를 그림·제목까지 붙여 크게 보여 주는 카드를 만들고 나서,
브라우저를 몰아 확인하던 중.

**왜 났나**

`readRecipe()` 가 마지막 줄에서 출처를 **늘 새로 써 버렸다.**

```ts
recipe: { title, servings, ingredients, steps, source: { kind: "ai" } },
```

이 함수는 원래 "모델이 보낸 값을 받아 줄지 살펴보는" 자리였다. 모델이 보내는
JSON 에는 출처 칸이 없으니 기본값을 박아 두는 것이 맞았고, 유튜브에서 올 때는
어댑터가 그 뒤에 `sourceOf` 로 덮어썼다. 거기까지는 잘 돌았다.

그런데 같은 함수를 **브라우저 저장소에서 꺼낼 때도** 쓴다
(`browser-recipe-draft-store.load()`). 저장소에 담긴 값에는 출처가 있는데,
꺼내면서 그 칸을 통째로 `{kind:"ai"}` 로 덮어썼다. 그래서 **화면을 한 번
옮기는 순간 원작자가 사라졌다.**

핵심은 이것이다. 검사 함수가 **모르는 칸을 버리는 것**과 **아는 칸을
기본값으로 덮는 것**은 다른 일이다. 앞엣것은 안전이고, 뒤엣것은 값을 잃는 것이다.
`readRecipe` 는 뒤엣것을 하고 있었고, 부르는 자리가 하나였을 때는 티가 안 났다.

원작자를 밝히겠다고 만든 기능이 조용히 "우리가 만들었다" 고 말하고 있었으니,
화면이 깨지는 것보다 나쁜 종류의 버그였다.

**어떻게 고쳤나**

도메인에 `readSource(value)` 를 만들고 `readRecipe` 가 그것을 쓰게 했다.

```ts
recipe: { title, servings, ingredients, steps, source: readSource(bag.source) },
```

`readSource` 는 갈래마다 꼭 있어야 할 칸을 본다. 유튜브인데 주소가 없으면
`{kind:"ai"}` 로 떨어뜨린다 — 확인할 수 없는 채널 이름만 남기는 것보다
출처를 안 밝히는 편이 낫기 때문이다. 어댑터의 `sourceOf` 덮어쓰기는 그대로
두었다. 모델이 보낸 값에는 여전히 출처 칸이 없다.

**배운 것**

- **검사 함수가 값을 만들어 내면 안 된다.** 없는 칸을 채우는 것과 있는 칸을
  덮는 것을 한 줄 안에서 하면, 부르는 자리가 늘어나는 순간 값이 사라진다.
- 같은 검사 함수를 **바깥에서 들어올 때**와 **우리가 담아 둔 것을 꺼낼 때**
  같이 쓰고 있다면 한 번 의심해 볼 만하다. 두 자리는 믿을 수 있는 정도가 다르다.
- 저작자 표시처럼 **틀려도 화면이 안 깨지는 값**은 눈으로 못 잡는다.
  담긴 값과 보이는 값을 나란히 찍어 보는 확인이 필요하다.

---

## 2026-08-23 — 한국어 질문이 엉뚱하게 검색됨 (사실은 터미널 탓)

**증상**

```
0.076 흑임자 카카오 스피어
0.063 하얀설탕으로 굽는 홈메이드 약과
0.058 하얀설탕으로 굽는 홈메이드 약과
```

"계란찜이 안 부푸는 이유" 를 물었는데 계란찜 후기가 하나도 안 나왔다.
점수도 0.076 로 비정상적으로 낮았다(정상이면 0.35~0.5).

**언제**

파인콘에 후기 101건을 올린 직후, `curl` 로 검색 API 를 두드려 확인하던 중.

**왜 났나**

**앱은 멀쩡했다. `curl` 에 한국어를 그대로 적어 넘긴 것이 문제였다.**

Git Bash 는 명령줄 인자를 시스템 코드페이지(한국어 윈도우면 cp949)로 넘긴다.
Next.js 는 그 바이트를 UTF-8 로 읽으므로 글자가 깨진다. 깨진 글자를 임베딩하면
아무 뜻도 없는 벡터가 나오고, 그 벡터와 가장 가까운 것을 고르니 결과가
무작위처럼 보인다. 점수 0.07 대는 "거의 직각인 벡터", 즉 **아무 관련 없음**이라는 뜻이다.

여기서 시간을 크게 버렸다. 원인을 랭체인 쪽에서 찾았기 때문이다.
`PineconeEmbeddings.embedQuery` 가 `this.params.inputType` 을 영구히 바꿔 놓는
진짜 버그가 있어서 더 그럴듯해 보였다(문서는 passage, 질문은 query 로
임베딩해야 하는데 뒤섞일 수 있다). 하지만 그건 이 증상의 원인이 아니었다.

**어떻게 고쳤나**

앱을 고칠 것이 없었다. 확인 방법을 고쳤다.

```
Q=$(python -c "import urllib.parse;print(urllib.parse.quote('계란찜이 안 부푸는 이유'))")
curl "http://localhost:3000/api/kitchen/index?q=$Q"
```

퍼센트 인코딩해서 보내니 0.487/0.401/0.396 으로 전부 계란찜이 나왔다.

**배운 것**

- **"앱이 이상하다" 로 넘어가기 전에 입력이 제대로 도착했는지부터 본다.**
  응답에 받은 값을 그대로 되돌려 담아 보면 한 번에 갈린다. 나는 이걸
  마지막에 했는데 처음에 했어야 한다.
- 윈도우 셸에서 한국어를 인자로 넘기는 것은 믿을 수 없다. **URL 인코딩하거나
  파일에 적어 `--data-binary @파일` 로 보낸다.**
- 점수를 읽을 줄 알아야 한다. 코사인 유사도가 0.05~0.08 이면 "덜 비슷한 것" 이
  아니라 **"아무 관계 없는 것"** 이다. 순위가 이상한 것과 점수가 바닥인 것은
  원인이 다르다 — 전자는 자료나 질문의 문제, 후자는 벡터 자체가 엉망이라는 뜻이다.
- 그럴듯한 용의자(랭체인 버그)를 찾았다고 수사를 멈추면 안 된다.
  **저장된 벡터를 직접 꺼내 견줘 보는 것**이 가장 빨랐다 — 자기 유사도 0.985,
  직접 쿼리 0.487 이 나온 순간 "저장도 검색도 멀쩡한데 앱만 이상하다" 로 좁혀졌다.
---

## 2026-08-23 — 랭체인 파인콘 패키지가 SDK 최신판을 거부

**증상**

```
npm error code ERESOLVE
npm error ERESOLVE could not resolve
npm error
npm error While resolving: @langchain/pinecone@1.0.3
npm error Found: @pinecone-database/pinecone@8.2.0
npm error node_modules/@pinecone-database/pinecone
npm error   @pinecone-database/pinecone@"^8.2.0" from the root project
npm error
npm error Could not resolve dependency:
npm error peer @pinecone-database/pinecone@"^5.0.2" from @langchain/pinecone@1.0.3
npm error node_modules/@langchain/pinecone
npm error   @langchain/pinecone@"^1.0.3" from the root project
npm error
npm error Conflicting peer dependency: @pinecone-database/pinecone@5.1.2
```

**언제**

챕터 10 RAG 를 붙이려고 랭체인 묶음을 깔던 중. 파인콘 SDK 를 `npm install
@pinecone-database/pinecone` 로 깔았더니 최신인 8.2.0 이 들어왔고, 그다음
`@langchain/community` 를 깔려는 순간 설치가 통째로 멈췄다.

처음에는 d3-dsv 버전이 문제인 줄 알고 그것만 세 번 고쳤는데 계속 튕겼다.
오류문을 끝까지 읽고서야 진짜 원인이 파인콘 SDK 라는 걸 알았다.

**왜 났나**

`@langchain/pinecone` 같은 **연동(integration) 패키지**는 상대 SDK 를
`dependencies` 가 아니라 `peerDependencies` 로 잡는다. 자기가 SDK 를 안고
들어가면 프로젝트에 SDK 가 두 벌 깔리고, 두 벌은 서로 다른 물건이라
한쪽에서 만든 인덱스 객체를 다른 쪽이 못 알아본다.

그래서 SDK 는 프로젝트가 한 벌만 깔고, 연동 패키지는 "이 판 범위면 내가
안다" 고 적어 둔다. 문제는 **연동 패키지가 SDK 를 따라가는 속도가 느리다는
것**이다. 파인콘 SDK 는 8까지 갔는데 랭체인 쪽은 아직 5 를 본다.
새 SDK 의 함수 이름이나 응답 모양이 바뀌었을 수 있어서, 범위를 넘겨 쓰면
설치는 되더라도 부르는 순간 터진다.

`--force` 나 `--legacy-peer-deps` 로 넘길 수 있다고 npm 이 친절히 알려 주는데,
그건 "터질 걸 알면서 깔겠다" 는 뜻이다. 지금처럼 우리가 SDK 를 직접 쓸 이유가
없는 경우엔 낮추는 쪽이 맞다.

**어떻게 고쳤나**

SDK 를 연동 패키지가 보는 범위로 낮췄다.

```
npm install @pinecone-database/pinecone@^5.1.2
```

그다음 `@langchain/community` 와 `d3-dsv@^3.0.1` 이 한 번에 깔렸다.
d3-dsv 는 애초에 원인이 아니었고, 앞의 충돌 때문에 같이 실패하던 것이었다.

**배운 것**

- **오류문은 "While resolving" 줄부터 읽는다.** 거기 적힌 것이 불평하는 쪽,
  "Found" 가 실제로 깔린 것, "Could not resolve dependency" 아래가 원하는 것이다.
  나는 마지막 줄만 보고 d3-dsv 를 세 번 고쳤다.
- 연동 패키지를 쓸 때는 **연동 패키지를 먼저 깔고** SDK 를 npm 이 골라 주게
  두는 편이 낫다. SDK 를 먼저 최신으로 깔면 이 충돌을 반드시 만난다.
- `--legacy-peer-deps` 는 해결이 아니다. peerDependencies 는 "두 벌 깔리면
  안 되는 물건" 을 한 벌로 묶으려는 장치라서, 무시하면 런타임에 값을 주고받다
  깨진다. 설치가 조용해질 뿐이다.
---

## 2026-08-21 — `"use server"` 파일에서 객체를 export

**증상**

```
A "use server" file can only export async functions, found object.
Read more: https://nextjs.org/docs/messages/invalid-use-server-value

.next-internal\server\app\signup\page\actions.js (server actions loader) (1:1)

> 1 | export {signUpAction as '6072156b...'} from 'ACTIONS_MODULE0'
  2 | export {emptyAuthState as '7f4fc2e0...'} from 'ACTIONS_MODULE0'
```

**언제**

수파베이스 인증을 붙인 뒤 `/signup` 에서 회원가입을 시도했을 때.
페이지 자체가 안 열리고 위 오류가 떴습니다.

**왜 났나**

`app/actions/auth.ts` 맨 위에 `"use server"` 가 붙어 있는데, 그 파일에서
async 함수가 아닌 것을 하나 내보내고 있었습니다.

```ts
"use server";

// 이건 괜찮다 — async 함수
export async function signUpAction(prev, formData) { ... }

// 이게 문제 — 그냥 객체
export const emptyAuthState = { reason: null, email: "" };
```

**왜 그런 규칙이 있는가.** `"use server"` 파일에서 내보낸 것은 전부
**바깥에서 부를 수 있는 서버 입구**가 됩니다. Next.js 는 각각에 주소 같은
아이디(`'6072156b...'`)를 붙이고, 브라우저에는 실제 코드 대신 그 아이디만
내려보냅니다. 폼을 보내면 그 아이디로 서버에 POST 가 가고 서버가 진짜 함수를
실행합니다.

그런데 **객체에는 그 취급을 할 수가 없습니다.** "이 객체를 POST 로 실행한다"
는 말이 성립하지 않기 때문입니다. 그래서 Next.js 가 아예 빌드 단계에서 막습니다.

오류 메시지에 딸려 온 생성 코드를 보면 사정이 그대로 보입니다 —
`signUpAction` 과 `emptyAuthState` 를 **똑같은 방식으로** 내보내려 하고 있습니다.
Next.js 입장에서는 둘을 구별할 방법이 없습니다.

한 가지 헷갈리는 점 — `export type AuthFormState = {...}` 는 같은 파일에 있어도
괜찮습니다. 타입은 TypeScript 가 컴파일하면서 지워 버려서 실제 파일에는
남지 않기 때문입니다. 지워지지 않는 것(값)만 문제가 됩니다.

**어떻게 고쳤나**

값과 타입을 `"use server"` 가 없는 평범한 파일로 옮겼습니다.

```
app/actions/auth-state.ts   ← 새로 만듦. AuthFormState 타입 + emptyAuthState 객체
app/actions/auth.ts         ← "use server". async 함수 세 개만 남김
```

`emptyAuthState` 를 쓰던 두 폼의 import 도 새 파일 쪽으로 바꿨습니다.

```ts
// 전
import { emptyAuthState, signInAction } from "@/app/actions/auth";

// 후
import { signInAction } from "@/app/actions/auth";
import { emptyAuthState } from "@/app/actions/auth-state";
```

**배운 것**

- `"use server"` 파일에는 **async 함수만** 둡니다. 상수·객체·클래스는 옆 파일로
- 타입(`export type`)은 같이 둬도 됩니다. 컴파일하면 사라지니까
- 서버 액션은 "브라우저에서 부를 수 있는 서버 함수" 가 아니라
  **"누구나 POST 할 수 있는 입구"** 입니다. 그래서 화면에서 이미 검사했더라도
  액션 안에서 한 번 더 검사해야 합니다
- `npx tsc --noEmit` 과 `next build` 는 이걸 못 잡았습니다. 실제로 그 페이지를
  열었을 때 서버 액션 로더가 도는 시점에 걸립니다. **타입 검사와 빌드가
  통과했다고 화면이 뜨는 것은 아닙니다** — 한 번은 눌러 봐야 합니다
