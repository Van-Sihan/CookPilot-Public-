현재 프로젝트의 전체 파일 구조와 코드를 분석한 뒤, **HTML과 JavaScript/TypeScript를 처음 배우는 사람도 위에서부터 차례대로 읽으면서 프로젝트의 동작 원리를 이해할 수 있는 상세한 학습용 HTML 문서**를 만들어줘.

단순히 코드를 요약하지 말고, 내가 실제 코드를 따라 읽으면서 다음과 같이 이해할 수 있도록 설명해줘.

> "아, 이 파일은 이런 역할을 하는구나."  
> "이 코드는 이런 순서로 실행되는구나."  
> "이 함수는 이 데이터를 받아서 이렇게 처리하는구나."  
> "이 변수가 여기서 만들어지고 저 함수로 전달되는구나."  
> "이 코드 때문에 화면에 이런 결과가 나타나는구나."

---

# 1. 먼저 프로젝트 전체를 분석해줘

우선 현재 프로젝트의 전체 파일 구조를 확인하고, **실제로 프로젝트 동작에 사용되는 파일을 빠짐없이 파악**해줘.

다음과 같이 분류해줘.

```text
프로젝트
│
├── 설정 파일
├── 페이지
├── 컴포넌트
├── 스타일/CSS
├── 데이터
├── 함수/유틸리티
├── API/서버 관련 코드
└── 기타 중요한 파일
```

각 파일에 대해 먼저 다음 내용을 간단히 설명해줘.

- 파일명
- 파일 경로
- 이 파일의 역할
- 이 파일이 다른 어떤 파일과 연결되는지
- 이 파일을 읽기 전에 먼저 알아야 하는 파일이 있는지

---

# 2. 내가 코드를 읽어야 하는 순서를 먼저 정해줘

파일을 폴더 순서대로 나열하지 말고, **프로젝트의 동작 원리를 이해하기 가장 쉬운 순서**로 정리해줘.

예를 들어 실제 프로젝트 구조에 따라 다음과 같은 방식으로 구성해줘.

```text
1단계. 프로젝트 전체 구조 이해

↓

2단계. 가장 먼저 실행되는 파일 확인

↓

3단계. 페이지 구조 확인

↓

4단계. 페이지에서 불러오는 컴포넌트 확인

↓

5단계. 각 컴포넌트 내부 코드 확인

↓

6단계. 함수와 이벤트 처리 확인

↓

7단계. 데이터가 생성되고 전달되는 과정 확인

↓

8단계. CSS가 화면에 적용되는 과정 확인

↓

9단계. 전체 실행 흐름 다시 정리
```

단, 위 순서를 그대로 강제로 사용하지 말고 **현재 프로젝트의 실제 구조에 맞게 가장 이해하기 쉬운 순서를 직접 판단해서 구성**해줘.

각 단계마다:

- 왜 이 파일을 먼저 읽어야 하는지
- 이 파일을 읽고 나면 무엇을 이해할 수 있는지
- 다음에 어떤 파일을 읽어야 하는지

를 설명해줘.

---

# 3. 각 파일은 "초보자가 위에서부터 코드를 따라 읽는 방식"으로 설명해줘

각 파일을 설명할 때 단순히 기능만 요약하지 말고, **실제 코드의 위에서 아래 순서대로** 설명해줘.

예를 들어 다음 코드가 있다면:

```tsx
export function Hero() {
  return (
    <section className="hero">
      <h1>쿡파일럿</h1>
    </section>
  );
}
```

단순히

> Hero 컴포넌트입니다.

라고 설명하지 말고 다음과 같은 방식으로 설명해줘.

### ① 먼저 이 파일의 전체 역할

이 파일이 프로젝트에서 왜 필요한지 설명.

### ② 코드가 시작되는 부분

```tsx
export function Hero()
```

- `export`는 무엇인지
- `function`은 무엇인지
- `Hero`는 무엇인지
- 이 함수가 실행되면 무엇이 만들어지는지
- 왜 다른 파일에서 사용할 수 있는지

를 설명.

### ③ 그다음 코드

```tsx
return (
```

- `return`이 무엇인지
- 왜 화면을 `return`하는지
- 괄호가 왜 필요한지

를 설명.

### ④ HTML/JSX 구조

```tsx
<section>
```

- `section`은 어떤 역할인지
- `div`, `p`, `span`, `h1` 등의 차이
- 부모와 자식 요소가 어떻게 연결되는지
- 여는 태그와 닫는 태그를 어떻게 읽어야 하는지

를 설명.

그리고 필요하면 다음과 같이 **트리 구조로 시각화**해줘.

```text
section
│
├── h1
│   └── "쿡파일럿"
│
└── p
    └── 설명 문구
```

---

# 4. 모든 함수는 반드시 데이터 흐름 중심으로 설명해줘

프로젝트 안에 있는 함수는 다음 형식으로 설명해줘.

```text
[함수 이름]

1. 언제 실행되는가?
2. 누가 이 함수를 실행시키는가?
3. 어떤 값을 입력받는가?
4. 입력받은 값은 어디에서 오는가?
5. 함수 내부에서 어떤 순서로 처리되는가?
6. 처리 결과는 무엇인가?
7. 결과는 어디로 전달되는가?
8. 화면에서는 어떤 변화가 발생하는가?
```

예를 들어:

```text
사용자가 버튼 클릭
        ↓
onClick 실행
        ↓
handleSubmit() 호출
        ↓
사용자 입력값 가져오기
        ↓
데이터 처리
        ↓
결과 반환 또는 상태 변경
        ↓
화면 업데이트
```

처럼 **함수 호출 흐름을 단계별로 설명**해줘.

---

# 5. 변수와 데이터는 "어디서 와서 어디로 가는지" 설명해줘

각 변수에 대해 단순히:

> `name`은 이름을 저장하는 변수입니다.

라고만 하지 말고 다음을 설명해줘.

```text
변수 생성
   ↓
값 저장
   ↓
어디에서 사용?
   ↓
다른 함수/컴포넌트로 전달?
   ↓
화면에 표시?
```

예를 들어:

```tsx
const name = "쿡파일럿";
```

이라면:

- `const`가 무엇인지
- `name`이라는 이름은 왜 붙였는지
- `"쿡파일럿"`은 어떤 값인지
- 이 값이 이후 어디에서 사용되는지

를 설명해줘.

---

# 6. 컴포넌트끼리 연결되는 과정도 반드시 설명해줘

React/Next.js에서 특히 헷갈리는 부분이므로 다음을 자세히 설명해줘.

```text
부모 컴포넌트
      ↓
props 전달
      ↓
자식 컴포넌트
      ↓
화면 표시
```

예를 들어 어떤 페이지가:

```tsx
<Hero />
<Features />
<Footer />
```

를 사용한다면:

```text
page.tsx
│
├── Hero 컴포넌트 불러오기
│
├── Features 컴포넌트 불러오기
│
└── Footer 컴포넌트 불러오기
```

처럼 설명해줘.

그리고 **각 컴포넌트가 실제로 어느 파일에 있는지**도 연결해서 알려줘.

---

# 7. import/export를 만날 때마다 연결 관계를 설명해줘

예를 들어:

```tsx
import { Hero } from "./Hero";
```

가 나오면 반드시 설명해줘.

```text
현재 파일
    │
    │ import
    ↓
Hero.tsx 파일
    │
    │ export
    ↓
현재 파일에서 사용 가능
```

그리고:

- `import`가 무엇인지
- `export`가 무엇인지
- 왜 파일을 나누는지
- 실제로 어느 파일과 연결되는지

를 초보자 기준으로 설명해줘.

---

# 8. HTML 태그와 JSX 문법은 처음 보는 사람 기준으로 설명해줘

다음과 같은 태그나 문법이 나오면 처음 등장할 때 자세히 설명해줘.

```text
div
section
main
header
footer
p
span
h1 ~ h6
button
a
Link
img
input
form
```

그리고 JSX에서 자주 나오는:

```tsx
className
{}
()
[]
<>
</>
/>
```

등도 처음 등장할 때 설명해줘.

특히 다음을 강조해줘.

### 묶음 구조

```tsx
<div>
  <p>
    내용
  </p>
</div>
```

를 다음처럼 설명.

```text
div 시작
│
└── p 시작
    │
    └── 내용
    │
    p 종료
│
div 종료
```

즉 **코드의 들여쓰기와 태그의 시작/끝을 보고 부모-자식 관계를 읽는 방법**을 계속 알려줘.

---

# 9. CSS도 별도의 언어처럼 쉽게 설명해줘

CSS 파일이나 스타일 코드가 있다면:

```css
.hero {
  display: flex;
  gap: 20px;
}
```

다음과 같이 설명해줘.

- `.hero`가 무엇을 선택하는지
- `display`가 무엇인지
- `flex`가 화면 배치에 어떤 영향을 주는지
- `gap`은 무엇인지
- `20px`은 무엇인지

그리고 반드시:

```text
JSX

<div className="hero">

        ↓ 연결

CSS

.hero {
  ...
}
```

처럼 **JSX의 `className`과 CSS가 어떻게 연결되는지** 설명해줘.

---

# 10. 데이터가 실제로 흐르는 전체 과정을 그림으로 정리해줘

각 파일을 모두 설명한 뒤, 마지막에는 현재 프로젝트의 전체 실행 흐름을 정리해줘.

예:

```text
사용자 접속
    ↓
Next.js 시작
    ↓
page.tsx 실행
    ↓
Hero 컴포넌트 불러오기
    ↓
다른 컴포넌트 불러오기
    ↓
HTML/JSX 생성
    ↓
CSS 적용
    ↓
브라우저 화면 표시
```

만약 사용자 클릭이나 입력이 있다면:

```text
사용자 입력
    ↓
input 값 변경
    ↓
state에 저장
    ↓
버튼 클릭
    ↓
함수 실행
    ↓
데이터 처리
    ↓
state 변경
    ↓
React 화면 다시 렌더링
```

처럼 **실제 프로젝트의 코드에 맞춰서 설명**해줘.

---

# 11. HTML 문서 결과물 형식

최종 결과물은 **하나의 독립적인 HTML 파일**로 만들어줘.

파일명은 예를 들어:

```text
project-code-guide.html
```

처럼 알아보기 쉽게 만들어줘.

HTML 문서 안에는 다음 목차를 포함해줘.

```text
1. 이 프로젝트는 무엇을 하는 프로젝트인가?

2. 프로젝트 전체 구조

3. 전체 코드 읽기 순서

4. 1단계 - 첫 번째 파일

5. 2단계 - 다음 파일

6. 컴포넌트 연결 구조

7. 데이터 흐름

8. 사용자 행동에 따른 실행 흐름

9. 자주 등장하는 문법 정리

10. 전체 프로젝트 실행 흐름
```

---

# 12. HTML 문서 디자인

학습하기 편하도록 다음 기능을 포함해줘.

- 좌측 또는 상단 목차
- 각 파일별 섹션 구분
- 코드 블록
- 코드 설명
- "초보자 핵심 포인트" 박스
- 파일 간 연결 관계 다이어그램
- 함수 실행 흐름 다이어그램
- 데이터 흐름 다이어그램
- 어려운 용어가 처음 등장할 때 쉬운 설명
- 긴 문서는 목차를 클릭하면 해당 위치로 이동
- 코드와 설명을 명확하게 구분
- 라이트 모드
- 가독성 좋은 폰트와 여백
- 긴 문서라도 계속 읽기 편한 구조

---

# 13. 가장 중요한 설명 원칙

이 문서는 **HTML, JavaScript, TypeScript, React, Next.js를 거의 모르는 사람이 현재 프로젝트의 코드를 직접 공부하기 위한 문서​**다.

따라서 전문 용어를 사용하고 바로 넘어가지 말고 반드시 쉬운 말로 설명해줘.

예를 들어:

> "컴포넌트입니다."

라고 끝내지 말고:

> "컴포넌트는 화면의 일부를 하나의 부품처럼 분리한 것입니다. 예를 들어 Hero는 메인 화면의 큰 소개 영역을 담당하는 부품입니다."

처럼 설명해줘.

또한 **현재 프로젝트에 실제로 존재하지 않는 코드를 예시로 설명의 중심으로 사용하지 말고, 반드시 현재 프로젝트의 실제 코드를 기준으로 설명​**해줘.

코드를 임의로 생략하지 말고, **각 파일을 위에서 아래로 따라가면서 중요한 코드뿐 아니라 처음 배우는 사람이 헷갈릴 만한 문법과 구조까지 설명**해줘.

단, 정말 반복되는 동일한 패턴은 처음에 자세히 설명한 뒤 이후에는:

> "이 구조는 앞에서 설명한 ○○ 패턴과 동일합니다."

라고 연결해도 된다.

최종 목표는 내가 이 HTML 문서를 옆에 띄워놓고 실제 VS Code/Antigravity의 코드를 위에서부터 따라 읽으면서 **“이 줄은 왜 존재하는지, 어떤 데이터가 어디로 이동하는지, 어떤 파일과 연결되는지”를 하나씩 이해할 수 있게 만드는 것​**이다.

문서 작성이 끝난 뒤에는 HTML 파일을 저장하고, 생성된 파일 경로를 알려줘.